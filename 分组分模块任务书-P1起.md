# 分组分模块任务书（P1 起 · 2026-09-15 版）

> **怎么用这份文档**：先看 §1 找自己组，再看 §2 找自己的模块，然后照着 §3 的"每个模块六件事"做。
> 卡在门禁上就查 §4。**开工前先读 `scaffold/SKELETON-GUIDE.md`**（里面有那条"以错误理由被拒"
> 的 bug 完整案例，是本项目最重要的一条教训）。

---

## 1. 四个组：你们各自负责什么

| 组 | 名称 | 主责模块 | 在 P1 的动作 |
|---|---|---|---|
| **A** | 数据底座 | **M1 基础设施**、**M3 数据访问层**、**M4 真数据门** | M1/M3 已落地；**P1 唯一关键缺口是 M4** ——A 组集中做 M4 |
| **B** | 感知接入 | **M2 数据接入与设备自管** | ✅ **写入收口已完成**（4 条裸 SQL + 自带连接池已移除，端到端回归通过）；本轮转做**真实数据摸底**的接入侧准备 |
| **C** | 融合诊断 | **M5 融合辨析引擎** | ⛔ **P1 不得启动**。M5 的门禁是 M4（依赖质量门筛出的可信数据）。C 组这段时间做**真实数据摸底**（见 §5） |
| **D** | 应用决策 | **M6 API 服务**、**M8 应用层**、**M9 平台管理台**、**M10（二期）** | M6 已落地；本轮为 M8/M9 准备**假数据 demo 的输入契约**，并协助 A 组做 M4 的对外出口 |
| **待指派** | — | **M7 语义中枢** | ⚠️ **仍没有人**。建议导师在 P1 结束前指派，否则 P4 会变成无人认领的模块 |

**关键路径**：`M1 → M3 → M4 → M5 → M6 → M8`
**可并行**：`M2 ‖ M3`、`M7 ‖ M8`

---

## 2. 十个模块：现在各是什么状态、下一步做什么

状态三级：**已落地**（有实现且实跑验证）/ **骨架已就位**（机械层齐备、业务层空）/ **规划中**。

| 模块 | 主责 | 状态 | **下一步第一件事** |
|---|---|---|---|
| **M1** 平台基础设施与编排 | A | 已落地 | 无。三库分工里 **IoTDB 未接入**——列为 P1 收尾第一优先（见 §6） |
| **M2** 数据接入与设备自管 | B | 已落地 | ✅ 写入收口已完成。转做**真实数据摸底**的接入侧准备（数据来源/格式/频率/历史回灌）＋ 设备自管逻辑 |
| **M3** 对象域数据访问层 | A | 已落地 | ✅ **已完成**：写方法与表级写权守卫已落地（`rpdao/write.py`），ingest 已收口 |
| **M4** 数据治理·真数据门 | A | **骨架已就位** | ★ **本阶段最重要的一件事**：真实数据到位后标定阈值 → `truth_flag` 晋升真正跑通 |
| **M5** 融合辨析引擎 | C | 骨架已就位 | ⛔ 等 M4。先做真实数据摸底，**不要写业务逻辑** |
| **M6** API 服务与指标语义层 | D | 已落地 | 动作层仍是 501 占位；配合 M4 把质量门状态暴露到 `/openapi.json` |
| **M7** 语义中枢 | **待指派** | 骨架已就位 | 指派后：结构探查 → 规则 → LLM 候选 → **人工确认** → `mapping_set` 落库 |
| **M8** 应用层 | D | 骨架已就位 | 准备假数据 demo；等 M6 的出口契约稳定 |
| **M9** 平台管理台 | D | 骨架已就位 | 现在就能做：模块状态看板（`/v1/modules/status` 已实现，10 个模块都在册） |
| **M10** Agent 执行引擎 | — | 骨架已就位 | 二期，暂不动 |

### 2.1 为什么 M4 是唯一的关键缺口

`P1 的验收条件是「真数据门上线」`，而现状：

- `data_import_batch.truth_flag` **全为 false** → **真值晋升从未发生过**
- `quality_rules.yaml` 里 11 条规则，**只有 2 条**标了 `calibrated: true`（两条结构性跨字段规则，
  不需要真实数据就能定）；其余 9 条是统计类阈值，**必须用真实数据标定**
- 因此 M4 的接口在门禁关闭时**返回 503**（这是刻意设计：宁可报错也不给假通过）

> **A 组的第一个动作不是写代码，是拿到真实数据。** 见 §5。

---

## 3. 每个模块的"六件事"（开工模板，照做即可）

机械层已由导师搭好（**现有 4 个跑通的模块就是范本**），各组只需按同一模式补业务逻辑：

| # | 事项 | 说明 | 范本 |
|---|---|---|---|
| 1 | 读 README | `scaffold/modules/M<n>-<name>/README.md`：我是谁／我认哪份契约／怎么跑／五件套自检／TODO／别踩的坑 | 每个模块都有一份 |
| 2 | 认契约 | 契约①报文 ②表结构 ③DAO ④服务接口 ＋ **本模块的产出契约** `contracts/<模块>/` | — |
| 3 | 填配置 | 所有阈值/系数写在 `modules/<模块>/config/*.yaml`，**代码零硬编码**；标定前留 `null` ＋ `calibrated: false` | `modules/M4-governance/config/quality_rules.yaml` |
| 4 | 实现业务 | 把 `_todo()`（501 + owner）换成真实现；**门禁未开时仍返回 503** | `modules/M4-governance/app.py` |
| 5 | 写契约测试 | `tests/contract/test_<模块>.py`，**离线可跑**，覆盖**应通过**与**应拒绝**两侧 | `test_governance_contract.py` |
| 6 | 自检五件套 | 容器（含 healthcheck）／`/healthz` 逐项报依赖／`/metrics`／配置外置／契约文件已提交 | `run_contract_tests.sh` |

**五件套自检命令**：

```bash
cd scaffold
./run_contract_tests.sh --list          # 看全部契约测试
./run_contract_tests.sh                 # 跑全部（离线）
docker compose -f docker-compose.skeleton.yml --profile m4 up -d   # 起自己的模块
curl -s localhost:8024/healthz | jq     # 逐项报依赖
```

---

## 4. 门禁：谁能动、谁必须等

> **唯一原则：下游不启动，直到上游的两个契约测试都绿。**

```
        M1 (A) ──┐
                 ├──► M4 (A) ──► M5 (C) ──► M6→M8 (D)
        M3 (A) ──┘        ▲
                          │
        M2 (B) ───────────┘（M2 提供接入侧数据）
        M7 (待指派) ‖ M8 (D)      ← 可并行，但 M7 依赖 M2/M3/M4
```

| 组 | 现在能做 | 必须等 | 等的条件 |
|---|---|---|---|
| **A** | ✅ M4 全部工作、M3 写方法 | 无 | — |
| **B** | ✅ 契约③写入收口重构 | **等工单确认**（Q7–Q9） | 字段设计经导师确认 |
| **C** | ✅ 真实数据摸底、`alarm_rule`/阈值调研 | ⛔ **M5 业务逻辑** | M4 的两个契约测试都绿 |
| **D** | ✅ M9 看板、M8 的假数据 demo 输入契约 | M8 的完整实现 | M6 出口契约稳定 |

---

## 5. 开工前必做：真实数据摸底（**A 组与 C 组共同**）

这一条不是可选项。**真实数据没到位前，M4 的阈值和 M5 的标定系数都不许编。**

| # | 动作 | 负责 | 产出 |
|---|---|---|---|
| 1 | 拿到 G228 滨海大道试验段的**真实 WIM 数据**（至少一个完整月的过车记录） | A | 原始文件 + 来源说明 |
| 2 | 统计真实违约分布：超速/轴数不符/总重偏差/缺字段/越界各占多少 | A | 分布表（**与合成数据的分布必然不同**） |
| 3 | 用真实分布定 `quality_rules.yaml` 的阈值（分位数法，不用均值±3σ 拍脑袋） | A | 每条规则的 `calibrated: true` + `evidence`（样本量/分位数/来源） |
| 4 | 跑一次真实数据晋升：`truth_flag` false → true | A | `data_import_batch` 出现 `truth_flag=true` 的行 |
| 5 | 调研 G228 的 RE 域（应变/振动）与 WE 域（气象）数据源 | C | 数据源清单 + 可用性评估 |
| 6 | 调研 IRI→RQI 反演所需的标定数据与规范依据 | C | 标定方案草案（**给导师审，别自己定**） |

**摸底结果直接决定下一阶段的工时估算**——现在给不出可信数字，就是因为缺这份摸底。

---

## 6. 三条纪律（违反会返工）

1. **照图施工、契约先行**。学生**不参与设计决策**，只做"实现、验证、造数、写文档"；
   确需变更走**契约变更工单**由导师把关。现有工单模板见 `scaffold/MODULE-ONBOARDING.md` §5。
2. **不要拿合成数据当验证依据**。造数器只覆盖有限的违约类型；它曾有一个极隐蔽的 bug
   （`axle_num` 分支两个改动互相抵消，那类违约**从未真正触发过**校验，报文还被**另一条规则**
   以错误的理由拦下，于是"50/50 被拒"的断言照样全绿）。**断言必须落到"为什么"上，
   不能停在"有没有"。** 详见 `scaffold/SKELETON-GUIDE.md` §6。
3. **不确定就返 503 + owner，不要返回"通过"**。一个永远不会失败的检查，比没有检查更糟。

---

## 7. 本轮（P1 收尾）的交付清单

| # | 交付物 | 负责 | 验收方式 |
|---|---|---|---|
| 1 | **M4 真数据门上线**（P1 唯一验收条件） | A | `truth_flag` 出现 true；门禁开启后 `POST /v1/batches/{no}/validate` 返回 200 而非 503 |
| 2 | ~~契约②扩展落库（`quality_rule` / `mapping_set`）~~ ✅ **已完成** | A（表）＋ M7 主责（用） | DDL v0.2 可重复执行；三处表数一致（DDL 32 ＝ 字典 32 ＝ catalog 32） |
| 3 | ~~契约③写入收口~~ ✅ **已完成** | A（M3）＋B（M2） | `ingest` 无裸 SQL、无自带连接池；越权写入报 `WriteGuardError`；孤立主记录 0 条 |
| 4 | 真实数据摸底报告 | A＋C | §5 的 6 项产出齐备 |
| 5 | 图 A/C/D 同步更新（表数 30→32 等） | 导师 | 双审计 PASS ＋ 全文扫描旧口径残留 0 |
| 6 | **M7 责任人指派** | 导师 | 落到人头 |
| 7 | IoTDB 接入决策（P1 末决策、P2 初接入） | 导师 | 书面决议 |

---

## 8. 不确定时的求助顺序

1. 先查 `scaffold/modules/M<n>-<name>/README.md` 的"别踩的坑"
2. 再查 `scaffold/SKELETON-GUIDE.md`
3. 契约本身有疑问 → **开工单**，不要在代码里绕过
4. 仍不确定 → 问导师，**不要自己定设计**

> 记住本项目已经有过的教训：**一个以错误理由通过的检查，比没有检查更糟——它给出的是虚假的覆盖信心。**

---

## 9. 已落地的基础设施（各组**直接用**，不必自己造）

2026-09-15 由导师完成，已过契约测试与端到端验证：

| 能力 | 在哪 | 你该怎么用 |
|---|---|---|
| **契约③写入收口** | `modules/M3-rpdao/write.py` | 写库一律 `dao.insert(表, 行, writer="M<你>")`；多表写用 `dao.write_txn()` |
| **表级写权守卫** | `catalog.TABLE_OWNER` | 先查你的表登记在谁名下；不在你名下就**开工单**，别绕 |
| **`quality_rule` 表**（M4） | DDL v0.2 G5 | 规则与**标定状态**入库；`code` 前缀必须等于 `layer`（库约束强制） |
| **`mapping_set` 表**（M7） | DDL v0.2 H1 | 字段对齐既有 JSON Schema；「已确认」必带确认人+时间（库约束强制） |
| **三处表数一致性测试** | `tests/contract/test_ddl_dict_catalog.py` | 你改 DDL / 字典 / catalog 任一处，它都会失败——**按提示把三处改齐** |
| **写权守卫测试** | `tests/contract/test_write_guard.py` | 你给新表加写权，在这里补一条断言 |

> ⚠ **写库前必读** `scaffold/SKELETON-GUIDE.md` §8–§9：
> 那里记了收口时踩的三个坑（**事务边界 / 累加语义 / 去重键对应真实约束**），
> 三个都**能通过编译、能让单测全绿，只在真跑或数据出错时暴露**。
