# M7 语义中枢 —— 映射配置
#
# ⚠️ 本文件**不含任何映射规则**，这是刻意的。
#    映射规则必须从真实表格里长出来；预先编一套规则再拿去"验证"，
#    只会得到一个自我印证的结论。责任人拿到真实样表后在此登记。
#
# 已知的「不对应」形态（来自设计文档，待真实数据确认与扩充）：

version: "0.1-skeleton"
calibrated: false

# 表头不对应：真实列名 ≠ DDL 字段名
header_aliases: []          # 待真实样表填充
# 例（**示例格式，非真实规则**）：
#   - raw: "路面平整度(m/km)"
#     target_field: "iri_m_per_km"
#     unit: "m/km"

# 桩号不对应：真实里程表达 ≠ 库内规范桩号
station_formats: []         # 待真实样表填充
# 例（**示例格式，非真实规则**）：
#   - pattern: "^K(\\d+)\+(\\d+)$"
#     normalized: "{km}.{m:03d}"      # K12+345 → 12.345

remark: >-
  本文件为空是**预期状态**：M7 的输入是真实业务表格，骨架期没有真实表格，
  任何预先编写的映射规则都是猜测。责任人（待指派）拿到真实样表后填充。
