# 路面性能数据库 —— 数据字典 v0.5

> 项目：2025Y095《智慧公路路面结构断面监测与数据融合养护管理技术研究》研究内容（3）
> 配套：`scaffold/sql/10_ddl_v0.5.sql`（PostgreSQL 方言。v0.1 的 30 表已在本地 postgres 容器 road_ddl_test 库验证可执行：全建、分区插入、复合 FK 全通；
> v0.2 新增 2 表、v0.3 新增 10 表、v0.4 新增 9 表、v0.5 新增 2 表，均已在 rp-pg 实库执行并验证幂等（重复执行无 ERROR））
> **v0.5 变更**：新开 **J 节「逐桩土方与路基设计断面」2 张表** —— 纬地教程 §13.9（土方数据 `.tf`）与 §13.6（路基设计中间数据 `.lj`）。建表原则是**照数据文件的样式，好追溯**：文件里有的列全建（`.tf` 74 列里 44 列本工程全 0 也建），列名用文件自带表头逐条译，与 `.DMX`/`.ZDM` 重复的量照样存。
> **v0.4 变更**：新开 **I 节「设计控制参数（.CTR）」9 张表** —— 纬地教程 v5.88 §13.10 的设计参数控制文件接入。
> 为什么单开一节：A 节是「空间与档案」（几何实体本身），I 节是**设计控制参数**（边坡/边沟/路槽/构造物等「怎么修」的控制量）。两者都由 .STA 桩号寻址，但性质不同。纯新增，不改 v0.3 任何一列。
> **v0.3 变更（契约变更工单 #2）**：① 桩号列 `stake_*` → `station_*`（9 列 + 2 索引）—— stake 是"物理标桩"，station 才是"桩号值"；
> ② GE 域新增 10 张设计数据表（纬地设计工程文件接入）；③ `station_sequence` 立为全线桩号基准（一等实体）。
> 命名规范：表=snake_case 复数名词；字段=snake_case；字典表以 `dict_` 前缀；主键统一 `id bigint identity`；时间统一 `timestamptz`；中文说明对齐 T/CECS《高速公路基础设施数据交换技术规程》数据字典风格（字段名/中文名/说明/数据格式）。

---

## 0. 库与双库分工

| 库 | 用途 | 内容 |
|---|---|---|
| `road_pavement`（关系库，本字典） | 档案/字典/事件/聚合/质量/业务 | **55 张表**（A 空间档案 18 + B 字典 5 + C 轴载交通 3 + D 表观病害影像 4 + E 试验 3 + F 模型诊断决策 5 + G 质量反馈 5 + **H 语义映射 1** + **I 设计控制参数 9** + **J 逐桩土方与路基设计断面 2**；v0.3 较 v0.2 新增 GE 域 12 张，v0.4 新增 I 节 9 张，v0.5 新增 J 节 2 张） |
| 时序库（独立，IoTDB/TimescaleDB 待选型） | 高频原始序列 | 应变/振动/土压/加速度/挠度/声学/气象分钟流；与关系库按 `channel_id + data_import_batch.id` 对账 |

**分区**：`wim_axle_record`（按月）；高频时序不进关系库（128kHz 单通道日 ≈8.6 亿点，关系库不可行）。特征值/统计聚合落关系库（`traffic_daily_stat`、`ts_feature` 预留）。

---

## A. 空间与档案（静态基础数据）

### A0. design_project 设计项目
| 字段 | 说明 |
|---|---|
| project_uid | .PRJ 项目ID（UUID），UNIQUE |
| project_name / project_type | 项目名 / 公路主线 |
| station_interval_m | 桩号间隔 m（20）—— 决定逐桩数据的默认密度 |
| earthwork_method | 土方计算方式（平均断面法）|
| designer / design_org / design_stage | 设计人 / 设计单位 / 设计阶段 |
| source_file | 来源 .PRJ 文件名 |

> **v0.3 新增**（契约变更工单 #2）。设计期数据的血缘起点：一套纬地(HintCAD)工程文件 ＝ 一条 design_project。

### A1. road_line 路线
| 字段 | 类型 | 说明 |
|---|---|---|
| line_code | varchar(32) UNIQUE | 路线代码（G228）|
| line_name | varchar(128) | 路线名称（国道 G228 福清滨海大通道）|
| admin_region / road_class / design_speed | varchar/varchar/smallint | 行政区域 / 公路等级 / 设计速度 |
| design_load | varchar(64) | 设计荷载等级 |
| lane_count / lane_width_m | smallint / numeric | 车道数 3 / 单车道宽 3.75 |
| manage_org | varchar(128) | 管养单位 |

### A2. road_section 路段
`line_id→road_line`；start/end_station_text（K4635+000 文本桩号）+ start/end_station_km（4635.000 数值桩号，便于区间检索）；`design_project_id→design_project`（v0.3 新增，设计血缘）；length_m；direction；pavement_type；climate_zone。

### A3. structure_layer 路面结构层（断面分层核心）
`section_id` + `layer_no`（自上而下 1..n）+ layer_name（AC-13C 改性沥青抗滑表层）+ layer_role（面层/基层/底基层/路基）+ material + thickness_cm。
> 实测例：4cm AC-13C / 6cm AC-20C / 12cm ATB-25 / 16cm 级配碎石 / 1cm 沥青表处 / 32cm 5%水稳底基层 —— 数字孪生分层可视化的结构依据。

### A4. monitor_cross_section 监测断面
| 字段 | 说明 |
|---|---|
| section_id / station_text / station_km | 路段 / 桩号 K4635+710 / 4635.710 |
| lane_no / lon / lat | 车道 / CGCS2000 坐标 |
| purpose | 用途（轴载调查/结构响应/应变断面）|
| install_date / status | 埋设日期（2025）/ active |

### A5. sensor_install 传感器布点
| 字段 | 说明 |
|---|---|
| cross_section_id | 所属断面 |
| sensor_type_code | →dict_sensor_type（石英称重/线圈/应变计/土压力盒/三向加速度/钢筋计/温湿度/风速风向/雨量/辐射/紫外/传声器/视觉相机/视频球机/光纤）|
| sensor_model / manufacturer / serial_no | 型号（ZDG-40-SY-2）/厂家/出厂编号 |
| install_mode | 埋入式/表面式/车载式/非接触 |
| structure_layer_id / install_depth_cm | 埋设层位（内观定位）/ 深度 |
| install_date / status | 安装日期 / active-fault-lost-retired |

### A6. sensor_channel 监测通道（逻辑测点）
| 字段 | 说明 |
|---|---|
| install_id + channel_no | 布点 + 通道号（三向加速度=3 通道），UNIQUE |
| quantity_code | →dict_quantity（应变/应力/土压/加速度/温度/湿度/雨量/挠度/声压级）|
| unit / range_low / range_high | 单位 / 量程（应变计 ±5000με）|
| sample_rate_hz | 采样率（1k~128k 分档）|
| acquire_mode | continuous 连续 / trigger 自动触发 / periodic 定时 |
| storage_policy | full 全存 / feature 特征值 / ratio 稀释 |
| dilution_rule jsonb | 稀释规则（PHM「分析点数/稀释比例自定义」落地）|

> ⚠️ 关系库↔时序库关联键：`sensor_channel.id`。

### A7. design_file 设计文件台账
| 字段 | 说明 |
|---|---|
| design_project_id → design_project | 所属设计项目 |
| file_kind_code / file_kind_name | .PRJ〔文件名〕键号 / 类型名（平面线形文件(*.PM)）|
| file_name / rel_path | 文件名 / 相对工程目录路径 |
| **coverage_from_station_km / coverage_to_station_km** | ★该文件覆盖的桩号区间 |
| parse_status / parse_note | ok 明文可解析 / blocked 二进制或专有 / pending |

> ★`coverage_*` 让"某文件覆盖哪一段桩号"成为**可查询事实**：实测 `.WID` 只到 5701.461 而全线 5805.421，末段 104 m 无路幅数据。
> 实测：明文可解析 14 个文件、二进制/压缩 5 个、MS Access 1 个（.TSF 需 mdbtools）。

### A8. section_design_attr 路段设计属性
| 字段 | 说明 |
|---|---|
| section_id → road_section | UNIQUE，一路段一份 |
| road_grade / design_speed_kmh | 公路等级（二级公路）/ 计算车速（60）|
| cross_section_form / roadway_width_m | 横断面形式（2车道）/ 路幅宽度（10.000）|
| carriageway_crossfall_pct / shoulder_crossfall_pct | 行车道横坡 2.0% / 土路肩横坡 3.0% |
| median_width_m / max_superelev_pct | 中间带 0.00 / 最大超高 8% |
| superelev_rotate_mode / superelev_gradient_mode | 超高旋转方式（绕曲线内侧行车道边缘旋转）/ 渐变方式（线性）|
| widening_mode / widening_gradient_mode | 加宽类型（不设置加宽）/ 加宽渐变方式 |

> 来源 `.PRJ〔项目分段〕`。独立成表而非并入 road_section：这些属性随设计变更而变，塞进路段档案会让改线形＝改档案。

### A9. station_sequence 桩号序列 ★全线桩号基准（一等实体）
| 字段 | 说明 |
|---|---|
| section_id → road_section | 所属路段 |
| station_seq_no | .STA 原始编号 |
| **station_local_km** | 相对桩号（0 起，设计口径；纬地工程 0.000→5805.421）|
| **station_absolute_km** | 绝对桩号（路网统一口径，如 4635.710）|
| station_text | K 格式文本（K4+635.000 / K4635+710）|
| station_type / is_integer_station | 整桩 / 加桩 / 断链点 / 起终点；是否整桩（20m 整桩 vs 加桩）|

> **★桩号作为一等实体**：本表是全线桩号基准，其余逐桩数据表以 `station_id` **FK 锚定其上**——
> 锚定关系是数据库约束，不是文档里的口头约定。
> **相对/绝对双列显式物化**，不存单个 offset 由读时计算：一旦有断链，`绝对＝相对＋offset` 的线性假设**不成立**。
> 来源 `.STA`（实测 332 个桩号，间隔 20 m）。UNIQUE(section_id, station_local_km)。

### A10. station_equation 断链（长链/短链）
| 字段 | 说明 |
|---|---|
| section_id → road_section | 所属路段 |
| equation_station_km | 断链点（相对桩号）|
| station_before_km / station_after_km | 断链前 / 后桩号（绝对）|
| equation_type / equation_len_m | long 长链 / short 短链；链长 m |
| station_text | 断链标注（K10+000=K10+050）|

> ★断链使**同一物理位置有两个合法桩号**。缺此表则：里程统计静默多算/少算、新旧数据按桩号 join 在断链处错位，**且不报错**。
> 本毕设工程无断链（空表运行）；G228 试验段有复测修正，必用。

### A11. alignment_pi 平面交点 ★**派生表**
`section_id` + `pi_seq`（.JD 交点个数＝10）+ `pi_type`（QD 起点/ZD 终点/JD 交点）+ `x_coord`/`y_coord`（大地坐标，起点 2789225.876/534559.218）+ `radius_m`（圆曲线半径 450）+ `spiral_ls1_m`/`spiral_ls2_m`（缓和曲线长 Ls＝60）+ `spiral_a1`/`spiral_a2`（缓和曲线参数 A＝√(R·Ls)，**生成列，不可手填**）+ `prev_tangent_len_m`（前段直线长）+ `tangent_len_m`/`tangent_len2_m`（切线长 117.54242652）+ `arc_len_m`（圆弧长 112.80867934）+ `curve_len_m`（曲线总长 232.80867934）+ `deflection_deg`（转角 +22.002684）+ `external_m`（外距 8.76412020）。

> ★**派生表**：全部字段可由 A12 的单元链推出（实测交点坐标偏差 3×10⁻⁸ m），**不得手工填写**。
> `.JD` 文件只作**验算**（独立来源的第二意见），不作数据入口 —— 折点是线的摘要，两条相邻切线求交即得。
> ⚠ 旧版字典把 `spiral_a1` 的 164.31676725 记成「切线长」，把 Ls 的 60 记成「转角」。
> 错值源头是 DDL 注释 —— 两处「互相印证」其实同源，所以谁也没发现。真正的切线长是 **117.54242652**，转角是 **+22.002684°**。

### A12. alignment_element 平面线形单元 ★**真源表**
`section_id` + `pi_id→alignment_pi`（可空：引道/末端直线没有对应交点）+ `element_seq`（.PM 共 33 段）+ `element_type`（line 直线/circular 圆曲线/transition 缓和曲线）+ `start_station_km`/`end_station_km` + 起终点坐标 + `center_x`/`center_y`（圆心）+ `azimuth_deg`（起点方位角）+ `end_azimuth_deg`（终点方位角）+ `radius_start_m`/`radius_end_m`（起终点曲率半径，NULL＝∞ 即直线端）+ `length_m`（单元长，**生成列**＝桩号差）。

> ★平面几何的**真源表**：A11 的交点与 A15 的 κ(s) 都由本表推出；本表不是任何东西的摘要。
> ⚠ 原 `curvature_1pm` 单列**已删**：缓和曲线单元内 κ 由 0 连续变到 1/R，**不是常数**，一列必然失真。
> 逐桩的 `A15.curvature_1pm` 是单值，正确，不受影响 —— 这个区别是关键。

### A13. profile_grade_point 纵断面变坡点
`section_id` + `vpi_seq`（.ZDM 共 12 个）+ `station_km`（变坡点桩号 300.000）+ `elevation_m`（高程 58.822）+ `vertical_curve_radius_m`（竖曲线半径 6000）+ `grade_in_pct`/`grade_out_pct`（前后坡）+ `grade_len_m`（坡长）+ `offset_station_km`/`offset_elev_m`（**标高错台**位置桩号与高差 m，向上为正；一般公路主线为 0）。
> **G(s) 纵坡函数**由本表竖曲线推导。
> 错台两列依据纬地教程 §13.3：`.ZDM` 每行第 4、5 项是「标高错台位置的桩号及错台的标高差值」，针对**互通立交匝道**；一般公路主线输 0。原表**静默丢弃**了这两项。

### A14. profile_ground_point 纵断面地面线
`station_id → station_sequence`（UNIQUE）+ `ground_elev_m`（地面高程）。
> 来源 `.DMX` 逐桩地面高程；与设计高程对比得填挖深度。

### A15. geometry_point 逐桩号线形 ★核心（κ(s)/G(s)/E(s) 函数库）
| 字段 | 说明 |
|---|---|
| **station_id → station_sequence** | ★锚在桩号基准上，UNIQUE |
| x_coord / y_coord / azimuth_deg | 平面坐标 / 方位角（.PM）|
| **curvature_1pm** | ★**曲率 κ**（1/m，.PM＋.JD 推导）|
| design_elev_m / ground_elev_m | 设计高程（.ZDM 竖曲线）/ 地面高程（.DMX）|
| **grade_pct** | ★**纵坡 G** %（.ZDM 推导）|
| h_radius_m / v_radius_m | 平曲线半径（.JD）/ 竖曲线半径（.ZDM）|
| **earth_shoulder_left_pct** | 左侧**土路肩**横坡 %（.SUP）|
| **hard_shoulder_left_pct** | 左侧**硬路肩**横坡 %（.SUP）|
| **lane_left_pct** | ★左侧**行车道**横坡 % ＝ 左半幅超高（.SUP）|
| **lane_right_pct** | ★右侧**行车道**横坡 % ＝ 右半幅超高（.SUP）|
| **hard_shoulder_right_pct** | 右侧**硬路肩**横坡 %（.SUP）|
| **earth_shoulder_right_pct** | 右侧**土路肩**横坡 %（.SUP）|

> **课题甲/乙共同消费接口**：κ(s) 来自 `.PM`+`.JD`，G(s) 来自 `.ZDM`，E(s) 来自 `.SUP`——三者对齐到 `station_sequence` 同一桩号基准。
> 对应 ASAM OpenDRIVE 的 `s ＝ station_absolute_km × 1000`（米），故可直供仿真。
> ⚠ **本表存的是插值到逐桩后的结果**；超高过渡的**原始变化点**见 A16。
> ⚠ 原先的两列 `superelev_pct`（单列）与 `crossfall_pct` **已删除**：教程 §13.5 规定超高是
> **左右各一个行车道横坡**，单列必丢一侧；而 `crossfall_pct` 的来源注写成 `.WID`，
> 但 `.WID`（教程 §13.4）**只有宽度、没有坡度** —— 该列自 v0.3 起就没有真实来源。

### A16. superelev_transition 超高过渡 ★**设计输入**（来自 .SUP）
`section_id` + `transition_seq`（行序，从 1 起）+ `station_km`（.SUP 第 4 项，本工程 **76 个**变化点）+ 六列横坡 `earth_shoulder_left_pct` / `hard_shoulder_left_pct` / `lane_left_pct` / `lane_right_pct` / `hard_shoulder_right_pct` / `earth_shoulder_right_pct`。

> 依据纬地教程 §13.5：「每一行前三项数据与后三项数据绕第四项数据呈对称位置排列。分别为
> 左侧土路肩的横坡值、左侧硬路肩的横坡值、左侧行车道（路面）的横坡值、桩号、右侧行车道横坡值、
> 右侧硬路肩的横坡值、右侧土路肩横坡值。其中数据 9999 表示**可以忽略此数据**，横坡渐变至此位置时，
> 系统**跳过此数据的计算继续进行横坡的超高渐变**。」
> ★**9999 → NULL**：`.SUP` 每格非数即 9999，故 NULL 与"缺值"无歧义，正是"此点不约束该列"。
> ★**为何与 A15 分表**：本表是设计输入（变化点 76 行），A15 是派生结果（逐桩 332 行）；
> 粒度不同，且 76 个桩号里只有 34 个落在逐桩上。合成一张会把"过渡过程"压没，
> 且 9999 的"此处不约束"信息无法保留。
> 本表列在 v0.4 待办中原属"超高路幅"一项，因读教程发现 A15 存法有误而**提前落地**。

---

## B. 字典表

| 表 | 主键 code | 内容 |
|---|---|---|
| dict_sensor_type | varchar(32) | 传感器类型；category 分类（交通荷载/气象环境/结构响应/表观巡检/声学）|
| dict_quantity | varchar(32) | 物理量 + 默认单位 |
| dict_disease_type | varchar(32) | 病害（crack 裂缝/pothole 坑槽/rutting 车辙/patching 修补/loose 松散）|
| dict_axle_type | varchar(16) | A2 2轴汽车 / T3~T5 货车 / T6 6轴货车（G228 实测口径，6轴占比 60.5%）|
| dict_stat_metric | varchar(16) | max/min/mean/rms/std/p95（特征值指标）|

**种子数据建议**：axle_type 5 行、disease_type ≥5 行、quantity ≥15 行（上表所列），随建库脚本下发。

---

## C. 轴载与交通（WIM 事件数据）

### A17. roadbed_width

**路幅宽度（`.WID`）★设计输入** —— **一行 = 一侧的一个桩号**，与 A16 `superelev_transition` 同形。

| 字段 | 中文名 | 说明 |
|---|---|---|
| `section_id` | 所属路段 | → `road_section.id` |
| `side` | 侧别 | `left` / `right`。教程 §13.4 用一行 `Z`/`Y` 引出其后数据；**实测 6.00 版用 `[LEFT]`/`[RIGHT]` 段标题行，教程未覆盖这一写法**，故适配器两种都认 |
| `seq_no` | 行序 | 该侧第几个数据行（源文件行序，从 1 起） |
| `group_seq` | 组号 | 该侧第几个桩号区间（教程「每两行为一组」的组号）。同一 `group_seq` 的两行 = 这个区间的起、终点 |
| `station_km` | **桩号** | ★本行自己的桩号。**键的一部分** |
| `median_width_m` | 中央分隔带宽度 | 0 = 无中央分隔带 |
| `half_carriageway_width_m` | 半侧路面宽度 | 行车道＋内侧路缘带。本工程 3.500 m |
| `extra_lane_flag` | 附加车道标识 | 0 无 / 1 或 2 有；为 2 时下一行的 0 表示主线外侧路缘带宽度。★**有意**允许同组两行不同 |
| `hard_shoulder_width_m` | 硬路肩宽度 | 折线法标注时该列是楔形端部鼻端半径 |
| `earth_shoulder_width_m` | 土路肩宽度 | 同上 |
| `extra_lane_file` | 附加车道文件名 | 无附加车道时源文件写 `0`，此处存 `NULL` |

**唯一键 = `(section_id, side, station_km)`** —— **键是桩号**，与 A16 一致。
左右侧可以同桩号（本工程 0.000 两侧都有），故 `side` 必须进键。

**★ 为什么不折叠成区间**（第一版折叠成 `start/end` 区间，是错的）：
- GE 域其余逐桩数据表**都按桩号寻址**（A9 `station_sequence` 注释写明「其余逐桩数据表以 `station_id` FK 锚定本表」）。折叠成区间 = 在 GE 域**另立一套区间寻址**，查「某桩号的宽度」得做范围查询，与查横坡/高程的点查不一致。
- 组内两行**本来就可以不同**（列 4，教程原文），折叠成区间**必然丢一个值**。
- A15 `geometry_point` 要按桩号取宽度，点查才能直接对齐。

区间起终点由**同侧相邻两行推得**，不落库。取值是**分段常量**：自本桩号起保持到同侧下一个桩号（与 A16 同一个模型，**不是插值**）。

**⚠ 不挂 `station_id` 外键**（与 A16 同理，已用实库数据验证）：`.SUP` 的 76 个变化点里只有 **34** 个落在 `station_sequence` 里，`.WID` 的 5701.461 也不在 —— **设计变化点的桩号本来就不是 `.STA` 那个桩号序列**，硬挂外键就得往一等实体里塞「不是桩号序列的点」。

**为什么必须有这张表**：`section_design_attr.roadway_width_m` 是**一个标量**，而路幅宽度**本来就随桩号变**（加宽段、匝道、交叉口、变速车道）—— 一个标量装不下分段变化，那是**会丢数据的简化**。按本项目「**存设计输入、导出派生量**」的规矩：`.WID` 是设计输入（本表），`roadway_width_m` 是由它导出的派生标量。

⚠ **本表不存路基总宽**：总宽 = 中央分隔带 + 2×(半侧路面 + 硬路肩 + 土路肩)，是**跨左右两行**的派生量，`GENERATED` 列也表达不了（生成列不能跨行），故不落库。

### C1. wim_axle_record 车辆轴载记录 ⭐分区表（按月）
| 字段 | 说明 |
|---|---|
| id + pass_time | PK (id, pass_time)——分区表 PK 含分区键 |
| cross_section_id | 监测断面（轴载调查站位）|
| lane_no / direction | 车道 / up-down |
| axle_type_code →dict_axle_type | 轴型 |
| axle_num / speed_kmh | 轴数 / 车速（1~200km/h，±1km/h）|
| gross_weight_kg | 总重（单轴 30t，过载 200%）|
| overload_flag / overload_rate | 超载标记 / 超载率（30%/50% 统计口径）|
| esal | 当量轴次 |
| plate_no / video_media_id | 车牌（与抓拍匹配）/ 影像 |
| data_source / quality_code | 采集设备溯源 / OK-ERR-UNCERTAIN |

索引：`(pass_time)`、`(axle_type_code, pass_time)`、UNIQUE `(id, pass_time)`（供 FK）。
⚠️ 生产需**脚本化提前建 12 个月分区 + 默认分区**，否则新月份数据插入报「no partition found」。

### C2. wim_axle_detail 轴组明细
`record_id + pass_time` 复合 FK→主表（分区表引用必须带分区键）；axle_seq 轴序；axle_weight_kg 轴重；group_weight_kg 轴组重；axle_dist_mm 轴距（±100mm）。UNIQUE(record_id, axle_seq)。

### C3. traffic_daily_stat 交通量日统计
按断面×日期 UNIQUE；total/truck/heavy/overload/overload30/overload50 计数；esal_total；by_axle_type jsonb 分轴型计数（{"A2":2434,"T6":8805}）；overload_by_axle jsonb 超载占比（对齐开题表 5/6 统计口径，科研免扫明细）。

---

## D. 表观普检 / 病害 / 影像

### D1. inspect_task 巡检任务
task_type（scan3d 三维高精扫描 / ai_vehicle 车载 AI / manual / uav）；task_time；vehicle_no；route_desc 覆盖范围；org；status。

### D2. disease_record 病害记录
task_id→D1（可空=历史导入）；section_id + station_text/station_km + lane_no 定位；disease_code→字典；severity light/moderate/severe；length/width/area 尺寸；image_media_ids jsonb；source；status pending/processing/done；first_seen（病害演变观测起点）。
索引：`(section_id, station_km)`、`(disease_code)`。

### D3. scan3d_model 三维扫描模型
section_id；scan_time；file_path（**文件外置，库存索引**）；precision_mm（毫米级）；coverage_from/to 覆盖桩号；size_bytes。

### D4. media_file 影像/文件元数据（通用附件）
biz_table + biz_id 多态关联；media_type image/video/model/doc；file_path/file_name/size；width/height/duration；taken_time；station_text；md5。
> 对应「图像数据」全量管理：病害图、巡检视频、过车抓拍、监测视频、扫描模型。

---

## E. 试验检测

| 表 | 说明 |
|---|---|
| test_project | 试验项目：test_no 编号 / test_type（承载力静载·动载/力学/材料/耐久性振动）/ title / org_name（**多机构对比**）/ standard_ref / research_ref（2025Y095）/ site_desc / 起止时间 |
| test_sample | 试样：project_id / sample_no / source_desc（现场芯样桩号）/ material_desc / spec_desc |
| test_result | 指标结果：indicator_code（弯沉 mm·模量 MPa·承载力 kN·共振频率 Hz·失效判据）/ value / unit / method_desc / result_file（DHDAS 原始记录路径）|

> 对应手稿「试验数据入库」+ 传感器耐久性振动试验（DHDAS 动态信号采集分析系统）。

---

## F. 模型输出 / 诊断 / 预警 / 养护决策

### F1. model_output 模型/仿真输出
object_type + object_id 多态指向（section/cross_section/channel/network）；model_code（FEA_static / FEA_dynamic / LLM_pred / life_pred）；output_type（theory_strain / theory_stress / deflection / pci / 寿命 / 风险）；calc_time；value 标量 + value_json 复杂结果；params jsonb 工况；source_batch。
> ⭐「测试值与有限元计算值相结合」预警机制的理论值来源表。

### F2. diagnosis_result 融合诊断结果
object_type/object_id；diagnose_time；method_code（融合辨析/健康诊断模型）；conclusion_code healthy/watch/warning/risky；conclusion 结论文本；confidence 置信度；basis_summary 依据摘要；report_media_id。

### F3. maintenance_advice 养护决策建议
diagnosis_id→F2；advice_type（preventive 预防性养护 / repair / strengthen **补强强化** / monitor 加密监测）；content；priority 1~5；cost_est 万元；status；effect_feedback（养护效果实时监测反馈→优化计划）。
> 手稿「强化/承载力评估」+ 预防性养护调控输出落点。

### F4. alarm_rule 预警规则
channel_id（通道级可空）；rule_type overrun/rate/trend/spectrum/ml（超限/变化率/趋势/图谱对比/机器学习）；params jsonb（阈值——应变预警值来自 FEA 对照）；push_channel。

### F5. alarm_record 预警记录
rule_id；channel_id；alarm_time；level **yellow/red**（黄红二级）；actual_value/threshold；status unconfirmed/confirmed/handled。
索引：`(alarm_time)`、`(channel_id, alarm_time)`。

---

## G. 数据质量 / 校准 / 反馈 / 接入

| 表 | 说明 |
|---|---|
| data_quality_log | 通道质量日志：period_start/end、raw_count/valid_count、issue_code（missing 缺失/overrange 超量程/step 阶跃/drift 漂移/abnormal）、action_code（drop/interpolate/calibrate/mark）——「真数据」质量门 |
| calibration_log | 校准：calib_type（deformation 变形/stress 应力/temp_humidity 温湿度场/zero）、value_before/after、factor 系数 —— 开题「数据处理层校准」落地 |
| feedback_record | **反馈闭环**：from_module（监测/诊断/养护/大模型/工程应用）→ to_module（design 设计/采集配置/database/标准）；content；status；effect_desc —— 手稿第 8 条「发送、反馈信息→设计数据库」 |
| data_import_batch | 接入批次：source_type（mqtt/file/api/manual）、raw_count/valid_count、**truth_flag 是否晋升「真数据」**、import 起止、quality_code —— MQTT 上传溯源 + 「挖掘→真数据」双轨 |
| **quality_rule**〔v0.2 新增〕 | **数据质量规则库（M4 真数据门）**：`code` 稳定规则码（UNIQUE，**前缀必须等于 `layer`**，使跨层命名漂移在库里就写不进）、`layer`（schema/missing/range/enum/cross/time）、`suite` 套件、`target_table/field` 作用对象、`rule_type`（range/enum/missing/outlier/cross/trend）、`severity`（block 拒收/warn 告警/mark 仅标记）、`params jsonb` 阈值·窗口（**标定前为 `{}`**）、`calibrated` 是否已用真实数据标定、`calibrated_at/by`、`evidence jsonb` 标定依据（样本量/分位数/来源）、`enabled`、`rule_version`。约束：标定为真必有时间 |
| **mapping_set**〔v0.2 新增〕 | **语义映射集（M7 语义中枢）**：`mapping_id`+`version`（UNIQUE）、`source_kind`（表头/桩号/单位/编码，解决哪一类「不对应」）、`source_sample` 样表来源、`source_hash` 源结构指纹（同结构可复用映射，不必再问人）、`target_table`、`entries jsonb` 映射条目 `[{raw,normalized,target_field,unit,transform}]`、`status`（候选/已确认/已废弃）、`confirmed_by/at`、`proposed_by`（rule/llm/human）、`confidence`。约束：**status=已确认 必带确认人与时间** |

---

## I. 设计控制参数（.CTR）

> 依据纬地教程 v5.88 **§13.10《设计参数控制数据文件（*.ctr）》**：18 类格式 / 29 个关键字。
> 本工程 `.CTR` 实测 **36 个关键字**：**19 个有数据、17 个为空**；另有 `ZDMDG.DAT`
> **教程全文搜不到**（20 行数据，值域 42~74，经比对既不等于 `.DMX` 地面线 46.393~124.188、
> 也不等于 `.ZDM` 设计标高 54.064~83.814，含义无法确定 → **经确认跳过，只登记**）。
> 下面 9 张表覆盖**有数据的全部**；17 个空关键字与 `ZDMDG` 只在
> `contracts/design-import/README.md` 里登记，**不建表**。
>
> ★ **本节的统一约定**（与 A16/A17 一致）：`section_id` + `station_km` 锚定，**键是桩号**；
> **不挂 `station_id` 外键**（.CTR 的分段桩号不是 .STA 桩号序列的子集）；
> 侧别用 `left`/`right`（源文件用 `Z`=左 / `Y`=右 的前缀，解析时归一）；
> 坡度一律是**简写 m 值**（教程：坡度 1:m 只写 m），有正负，`0` = 从中央水平向外
> （碎落台/护坡道，此时「坡高」实为**宽度**），`9999` / `-9999` = 垂直向上 / 向下
> —— **9999 是哨兵不是数值**，按 NULL 存。
>
> ⚠ **与 A16/A17 的一处本质差别**：`I7 land_use_width` 是**变化点**不是分段常量 ——
> 教程明写「如果分段桩号间的用地宽度不等时，则每个桩号的用地宽度按照**线性渐变**进行计算」。

### I1. slope_segment 路基边坡分段
`section_id` + `side`（left/right）+ `slope_kind`（fill 填 / cut 挖）+ `station_km`（分段终点桩号）+ `group_seq`（第几级边坡，从 1 起）+ `slope_ratio` + `control_height_m` + `max_height_m` + `protection`（0 不砌护 / 1 砌护）。

> 来自 `.CTR` 的 `ZTFBP` / `YTFBP` / `ZWFBP` / `YWFBP`（Z=左、Y=右；T=填、W=挖）。
> 教程格式：「分段终点桩号，本段变化组数，坡度，控制坡高，最大坡高，砌护控制，……」
> 一组 = **一级边坡**。分级放坡：第一级放到最大坡高仍不交地面线就接第二级。
> 本工程填方 5 级、挖方 6 级（都在桩号 5805.421，即整条路一个边坡方案）。
> ⚠ `slope_ratio = 0` 时，`max_height_m` 表示**碎落台/护坡道的宽度**，不是高度。
> 唯一键 `(section_id, side, slope_kind, station_km, group_seq)` —— **`group_seq` 必须在键里**：
> 同一个桩号下天然有多级边坡，漏掉它等于对每一级都喊一次"重复"。

### I2. ditch_segment 边沟 / 排水沟断面分段
`section_id` + `side` + `ditch_kind`（side_ditch 边沟 / drainage_ditch 排水沟）+ `station_km` + `group_seq`（第几个折点）+ `slope_ratio` + `height_m` + `protection`。

> 来自 `.CTR` 的 `ZBGXS`/`YBGXS`（边沟）与 `ZPSGXS`/`YPSGXS`（排水沟）。
> 教程格式：「分段终点桩号，本段变化组数，坡度，坡高，砌护控制，……」
> ★ 组数 `0` 表示**该段不设沟** —— 本工程排水沟就是 0 组（左侧、右侧各一行 0）。
> 本工程边沟每侧 3 个折点。

### I3. standard_cross_section 路基标准断面分段
`section_id` + `side` + `station_km` + 9 个数值：`median_half_width_m` / `median_crossfall_pct` / `median_height_m` / `lane_width_m` / `lane_crossfall_pct` / `hard_shoulder_width_m` / `hard_shoulder_crossfall_pct` / `earth_shoulder_width_m` / `earth_shoulder_crossfall_pct`。

> 来自 `.CTR` 的 `ZBZDM` / `YBZDM`。教程格式（一个分段一行，桩号后 9 个数值）：
> 「分段终点桩号，半幅中分带的宽度，中分带的坡度，中分带的高度，行车道的宽度，
> 行车道的坡度，硬路肩的宽度，硬路肩的坡度，土路肩的宽度，土路肩的坡度。」
> ★★ **这张表是 A17 `roadbed_width` 的独立校验源** —— 本工程两边完全自洽：
> 行车道 3.500、硬路肩 0.750、土路肩 0.750，横坡 2.0 / 2.0 / 3.0。
> 两个**来源完全不同**的文件（`.WID` 与 `.CTR`）在同一组数上对上，说明两条解析链都没错。

### I4. roadbed_trench 路槽分段
`section_id` + `side` + `station_km` + `median_trench_depth_m` / `lane_trench_depth_m` / `hard_shoulder_trench_depth_m` / `earth_shoulder_trench_depth_m`。

> 来自 `.CTR` 的 `ZLCSD` / `YLCSD`。教程格式：「分段终点桩号，中分带路槽深度，
> 行车道路槽深度，硬路肩路槽深度，土路肩路槽深度（单位为米）」。
> 用途：土方计算时按此**扣除路槽面积**。

### I5. structure_control 桥涵构造物控制
`section_id` + `structure_kind`（bridge 桥 / culvert 涵洞 / tunnel 隧道）+ `anchor_station_km` + `name` + `start_station_km` / `end_station_km` / `center_station_km` + `angle_deg` + `span_text` + `structure_form` + `control_elev_m` + `elev_control_type` + `deck_type` + `trailing_flag`。

> 来自 `.CTR` 的 `QHSJ`（桥）/ `HDSJ`（涵洞）/ `SUIDAO`（隧道）。用途：纵断面图/总体图标注，
> 且土方计算时**扣除大中桥与隧道土方**。
> ★ `anchor_station_km` 是「用于唯一标识的那一个桩号」：**涵洞取中心桩号，桥/隧道取起点桩号** ——
> 三种构造物靠它共用同一个唯一键。
> ⚠ 教程明写 `QHSJ`/`HDSJ` 的桩号**可以不按递增排列**，故不设递增约束。
> ⚠⚠ **教程自己前后不一致**：正文对 `QHSJ` 只列了 **10 项**，但**教程自己的示例**
> （`4100 4280 4190 京广铁路分离式立交桥 5*35m 预应力混凝土箱梁 90.00 135.80 0 1 0`）
> 与**实测文件**（`273.000 333.000 300.000 桥 30+30 混凝土空心板梁桥 90.0000 0.0000 0 1 0`）
> 都是 **11 列** —— 尾部有 3 个整数而正文只解释了前 2 个。
> 第 3 个存进 `trailing_flag`，**原样存下待考**（不猜、不丢）。
> `span_text` 是 **text** 不是数值：教程明确跨径格式含 `+` 与 `×`（`30+30`、`1-1.500×2.000`、`3x12m`）。

### I6. earthwork_composition 挖方土石成份分段
`section_id` + `station_km` + `pct_1` … `pct_6`。

> 来自 `.CTR` 的 `TFFD`。教程格式：「分段桩号，第一类至第六类土（石）所占挖方数量中的百分比」。
> 用途：土石方数量计算时按此把挖方量**分成六类**。
> 本工程：桩号 5722.468，20 / 60 / 20 / 0 / 0 / 0 %（和 = 100）。
> ★ 六项之和应为 100，但**不设 CHECK 约束**：设计中间稿的百分比可以先不配平。

### I7. land_use_width 附加用地宽度分段
`section_id` + `side` + `station_km` + `fill_land_width_m` + `cut_land_width_m`。

> 来自 `.CTR` 的 `ZYDK` / `YYDK`。教程格式：「起点桩号，填方用地宽度，挖方用地宽度；…；
> 终点桩号，填方用地宽度，挖方用地宽度。」
> 用途：横断面图绘**用地界碑**；土方文件记录用地宽度以计算用地表、绘用地图。
> ⚠⚠ **本表是变化点，不是分段常量**（与 A16/A17 不同）：教程明写「如果分段桩号间的用地宽度
> 不等时，则每个桩号的用地宽度按照**线性渐变**进行计算」。取某桩号的宽度要**插值**，不是查上一行。

### I8. extra_fill 超填 / 清除表土分段
`section_id` + `side`（可为 NULL）+ `fill_kind`（side_overfill 超宽填筑 / top_overfill 顶面超填 / topsoil_clear 清除表土）+ `station_km` + `width_m` + `thickness_m`。

> 来自 `.CTR` 的 `ZCHT`/`YCHT`（左右侧超宽填筑）、`DCHT`（顶面超填）、`QCHBT`（清除表土）。
> 教程格式：超宽填筑「分段终点桩号，一侧超填水平宽度」；顶面超填「分段终点桩号，超填厚度」；
> 清除表土「分段终点桩号，左右侧坡脚外增加宽度，清除表土厚度」。
> ⚠ **本工程这四个关键字全为空** —— 表建好但 **0 行**（保留结构以便将来接别的项目）。
> `side` 可为 NULL：清除表土是左右一起处理；且唯一键里 side 为 NULL 时 PostgreSQL
> 视作互不相等，这是**有意**的（一行覆盖左右两侧）。

### I9. design_control_text 地质概况 / 水准点
`section_id` + `text_kind`（geology 地质概况 / benchmark 水准点）+ `station_km` + `name` + `elev_m` + `content`。

> 来自 `.CTR` 的 `DZGK`（地质概况）、`SHUIZHUNDIAN`（水准点）。用途：纵断面绘图标注。
> 教程格式：地质概况「分段终点桩号，地质概况文字说明」（**不超过 128 个汉字字节数**，
> 汉字之间可以空格但**不能换行**）；水准点「位置桩号，名称，高程，说明」。
> ⚠ **本工程这两个关键字全为空** —— 表建好但 **0 行**。

### I 节**未建表**的 17 个空关键字 + 1 个无定义关键字（登记备查）

| 关键字 | 教程 §13.10 定义 | 本工程 |
|---|---|---|
| `ZFJBK` / `YFJBK` | 第 8 类：左右侧附加路面板块 | 空 |
| `ZJSG` / `YJSG` | 第 14 类：左右侧截水沟 | 空 |
| `ZFYHP` / `YFYHP` | 第 18 类：左右侧反压护坡 | 空 |
| `GONGDIAN` / `BGKZ` / `PZSTDG` / `RAILWAY_SHJG` | **教程未定义** | 空 |
| `ZDMDG` | **教程全文搜不到** | **20 行数据**（含义无法确定 → 经确认跳过） |
| `XXXX` | 文件结束标记 | — |

> 为什么"登记不解析"而不是"顺手建表"：这些关键字**本工程没有数据**，
> 没有实测样本可校验解析器写得对不对。写一个"猜"的解析器，会让下游把猜出来的东西
> 当设计输入用；登记下来则人一眼能看到"这个文件里还有这些没接"。

---

## J. 逐桩土方与路基设计断面（.tf / .lj）

> 依据纬地教程 v5.88 **§13.9《土方数据文件（*.tf）》**、**§13.6《路基设计中间数据文件（*.lj）》**。
>
> ★★ **本节的建表原则：照数据文件的样式，好追溯。** 因此：
> ① 文件里有的列**全建** —— `.tf` 74 列里有 **44 列本工程全 0**（清表/超填/沟面积/
>    路槽 B·C/垫层/路床/培土/包边土/截沟/台阶/挖 1–6 类/填 1–6 类/弃 1–6 类），照样建；
> ② 列名用**文件自带的表头**逐条译成英文 —— `.tf` 是**唯一**一个自带完整表头的纬地文件，
>    所以 74 个列名**没有一个是我猜的**；
> ③ 与 `.DMX`/`.ZDM` 重复的量（地面标高/设计标高）**照样存** —— 可追溯优先，
>    重复的风险由**一致性对账**兜住，而不是靠"不存"。
>
> ★ **这两张是逐桩表**（332 行 = `.STA` 桩号序列的 332 个），故用 `station_id` 外键锚定
> `station_sequence` —— 与 I 节不同：`.CTR` 的分段桩号**不是** `.STA` 桩号序列的子集，
> 所以那边只能存 `station_km`。`station_km` 在这里是**照文件原样存下**的（可追溯），
> 主锚定是 `station_id`。

### J1. earthwork_section 逐桩土方断面（.tf，74 列）
`section_id` + `station_id` + `station_km` + **文件自带表头的 74 列** + `remark`。

> 教程 §13.9：记录桩号、填挖方断面面积、左右侧坡口坡脚至中桩的距离等。
> 用途：土石方计算与调配、公路用地图、路线总体图生成的基础数据。
> 实测：332 行 × 74 列（文件第 75 列是空串 `""`，是程序产物，**不建列**）。
> 值域实测：挖方面积 0~1014.901、填方面积 0~669.644、中桩填挖 −58.406~+17.305、
> 基缘高 54.437~80.889、坡脚高 41.163~124.758。`计排水沟` 是 **0/1 标志**。
>
> ⚠⚠ **说明书正文把第 2/3 列写反了**：正文写「`[桩号] [填方面积] [挖方面积]`」，
> 而**文件自带表头**写「`[桩号] [挖方面积] [填方面积]`」。用 `.lj` 的
> **设计标高 vs 地面标高**独立判填挖，再比全部 332 行谁大：
> **按文件表头 → 326/332 吻合；按说明书 → 6/332**。（6 处不符全是
> 「设计标高 = 地面标高」的零填零挖边界，那时谁大谁小本就没有意义。）
> **结论：文件表头是对的，说明书正文不可信。** 本表按文件表头建列。

### J2. roadbed_design_point 逐桩路基设计断面（.lj，24 列）
`section_id` + `station_id` + `station_km` + 地面标高 + 设计标高 + 10 个宽度 +
11 个高差 + `remark`。

> 教程 §13.6：由"路基设计计算"自动生成，保存**所有指定桩号（`.dmx` 里的所有桩号）**
> 断面的路幅宽度、设计标高、地面标高及超高情况。路基设计表与横断面戴帽子都从它取数。
> 「此文件在特殊情况时，用户可做少量修改」—— 故它是**可编辑的设计输入**，不是纯派生量。
>
> 实测：332 行 × **24 列**（说明书说"每一行共 20 项数据" —— **对不上**）。
> 前 13 列**恒为常数、与桩号无关**（说明它们就是**标准断面**）：
>
> | 列 | 值 | 含义 | 列 | 值 | 含义 |
> |---|---|---|---|---|---|
> | 4 | 0.750 | 左土路肩宽 | 8 | 0.000 | 右中分带宽 |
> | 5 | 0.750 | 左硬路肩宽 | 9 | 0.000 | ★**待考** |
> | 6 | 0.000 | 左中分带宽 | 10 | 3.500 | 右路面宽 |
> | 7 | 3.500 | 左路面宽 | 11 | 0.000 | ★**待考** |
> |  |  |  | 12 / 13 | 0.750 | 右硬路肩 / 右土路肩宽 |
>
> 第 14–24 列是 **11 个高差**（第 14 列实测 −0.0375~+0.4725，**可为负**）。
>
> ⚠⚠ **说明书与实测三处对不上**（逐列比出来的，不是猜的）：
> 1. 说明书说 **20 项**，实测 **24 列**；
> 2. 说明书给的列序是「左土路肩 左硬路肩 **左路面 左中分带**」，
>    实测第 6 列 = 0.000（中分带）、第 7 列 = 3.500（路面）—— **中分带与路面写反了**；
> 3. 第 **9、11** 列在说明书里**没有对应项**，本工程恒为 0.000。按对称读法右侧应是
>    「中分带 0.000 / 路面 3.500 / 硬路肩 0.750 / 土路肩 0.750」4 项，实测第 8–13 列
>    却是「0.000 0.000 3.500 0.000 0.750 0.750」—— **多插了两个 0.000**。
>    故第 9、11 列**不猜**，按位置命名（`extra_width_09_m` / `extra_width_11_m`）并标**待考**。
>
> ★ **11 个高差是什么**：说明书只说「各路幅宽度位置相对于路面设计标高位置
> （超高旋转位置）的设计高差」，没说几个、怎么排。我用 `.CTR` 的横坡
> （土路肩 3% / 硬路肩 2% / 行车道 2% / 中分带 0%）按宽度**累计**算，
> 与 K0+000 的 0.0225 / 0.0375 / 0.1075 **完全吻合**；但拿同一组常数横坡套全部
> 332 行**只对 204 行**（其余 128 行最大差 **0.4725**）—— **差的那 128 行正是超高段**。
> 即：这 11 列编码的是**逐桩真实横坡**，因此它们**能反过来校验 `.SUP`**。
> （这一条留给契约测试，尚未做。）

---

## K. 逐桩横断面地面线（.HDM）

> 依据纬地教程 v5.88 **§13.8《横断面地面线数据文件（*.hdm）》**。
> 首句原文：「此文件记录**外业横断面测量**的成果数据。」
>
> 格式：首行版本串（本工程 `HINTCAD5.83_HDM_SHUJU`），其后**每三行**一个桩号断面 ——
> 第 1 行 = 中桩号，第 2 行 = 左侧，第 3 行 = 右侧；每行开头是该侧**总点数**，
> 随后是各测点「相对于前一测点的平距、高差」。
>
> ★★ **定性：这是外业测量的地面线，不是设计面。** 三份独立证据一致：
> ① §13.8 首句「外业横断面测量的成果数据」；
> ② 本工程文件名「052201341刘其立道路毕设**横断面地面线文件**.HDM」；
> ③ 文件内容实测（333 断面 / 2215 测点）高差 **−30.994 ~ +22.976 m**、中位 0.075 m、
>    39.6% 的测点 |高差| > 2 m —— 这是**地形起伏**的量级，路基设计面不会如此。
>    （横断面**设计**成果在 `.tf`，已由 J1 `earthwork_section` 承接。）
>
> ★ **与 `profile_ground_point`（← `.DMX` 纵断面地面线）同构**，一纵一横。
> 表名照内容走，故叫 `cross_section_ground_point`。
>
> ★ 两处**故意不照抄** `profile_ground_point`：
> ① **不用 `station_id` 外键** —— 实测 `.HDM` 有 333 个断面，`station_sequence`
>    只有 332 个（`.HDM` 多一个 `5701.461`），**不是子集**。故照 I/J 节里
>    `.CTR`/`.SUP`/`.WID` 的规矩直接带 `station_km`。
>    （J 节能用 `station_id`，因为 `.tf`/`.lj` 正好 332 行。）
> ② **不建父表** —— 一个桩号有左右两侧、每侧若干测点，看似三层，但
>    `(section_id, station_km, side, seq_no)` 一个四元组就能唯一定位一个测点；
>    父表能提供的信息（每侧几个点）是 `COUNT(*)` **派生量**，不落列。
>
> ★ **只存增量**：文件给的是「相对前一测点的平距/高差」，原样存。
> 累积平距、累积高差、各测点绝对高程都是**派生量**，不落列。
>
> ⚠ **变更史**：v0.5 早期版本在 DDL 文件头写着「横断面地面线（.HDM）经确认**不做**」。
> 2026-09 用户改判为**要做** —— 理由与定性无关：M2 的 `.HDM` 解析器**能**解析出
> `cross_section` 段，而平台**没有对应的表**，解析出来的数据没有地方存，**解析完就丢**。
> 「段能解析、表不存在」= 静默丢数据，比明说"不做"更糟。
> 迁移脚本：`89_migrate_v05_hdm.sql`。

### K1. cross_section_ground_point 逐桩横断面地面线测点（.HDM）

`section_id` + `station_km` + `side` + `seq_no` + `offset_m` + `elev_diff_m` + `remark`。

| 列 | 类型 | 含义 |
|---|---|---|
| `section_id` | bigint | → `road_section(id)` |
| `station_km` | numeric(12,6) | 断面中桩号，单位 km。**精度标准 1 mm**，与全库其余 32 处 `station*_km` 一致 |
| `side` | char(1) | `L` 左（每三行第 2 行）／`R` 右（第 3 行） |
| `seq_no` | smallint | 该侧第几个测点，从 1 起，顺序同文件 |
| `offset_m` | numeric(10,4) | 相对**前一测点**的平距 m（**非累积**）。实测 0.019 ~ 50.000，中位 12.389 |
| `elev_diff_m` | numeric(10,4) | 相对**前一测点**的高差 m（**非累积**，上正下负）。实测 −30.994 ~ +22.976 |

唯一约束 `(section_id, station_km, side, seq_no)`。
实测规模：333 个断面 × 左右两侧 = **2215 行**（本工程 section_id=6）。

### M1. earthwork_transfer 土石方调配过程（.tsf）

`section_id` + 一次调配的起讫桩号 + 六类用量与运距。**★一行 = 一次调配**，不是一次桩号。

| 列 | 类型 | 含义 |
|---|---|---|
| `section_id` | bigint | → `road_section(id)`。**★按内容走**：桩号 0~5805.421 正好覆盖一个路段全长 |
| `transfer_no` | integer | 纬地的调配序号（.tsf 列「GCID」，实测 1–27，无重复） |
| `section_seq` | smallint | 纬地的「分段编号」（本工程全 = 1）。⚠ 不是本库的 `road_section.id` |
| `cut_start_km` / `cut_end_km` | numeric(12,6) | 取土段起讫（「取土段S/E」）。⚠ `source_kind=1` 时两者**相等** |
| `fill_start_km` / `fill_end_km` | numeric(12,6) | 弃土段（即填方段）起讫 |
| `used_soil_m3` / `used_rock_m3` | numeric(14,4) | 用土/用石（松方）。实测 107.93 ~ 113086.35；**石全为 0** |
| `used_soil_compacted_m3` / `used_rock_compacted_m3` | numeric(14,4) | 压实后 |
| `used_class_1..6_m3` | numeric(14,4) | 松土/普通土/硬土/软石/次坚石/坚石 |
| `used_class_1..6_compacted_m3` | numeric(14,4) | 同上，压实后 |
| `haul_soil_m` / `haul_rock_m` | numeric(12,4) | 运距。⚠ **不是两段中心的直线距离**（实测差最多 128 m） |
| `haul_class_1..6_m` | numeric(12,4) | 分类运距 |
| `source_kind` | smallint | **0=路段内调运**、**1=从取土坑取土**。★不是「坑的类型」 |

唯一约束 `(section_id, transfer_no)`。

**★★ 桩号不是 `.STA` 的子集** —— 实测 68 个里 3 个不在（273/333/930 m，与最近 `.STA` 差 7/7/10 m）。
它们是**调配算法算出来的分段边界**，不是测量桩号，故本表**直接带 `station_km`**，不锚 `station_sequence`
（与 I 节 `.CTR` 同一条规矩）。

**★★ 六分类不是猜的** —— 源列名 `用土1/2/3` + `用石4/5/6`，数字 **1–6 连续**，
正好对上 `earthwork_composition.pct_1..6`。

**★★ 恒等式（实测验证）**：`用土1+用土2+用土3 == 用土`（相对偏差 ~1e-9 = 双精度舍入）；
`用土k(压实) == 用土k ÷ 系数k`（系数见 `earthwork_factor`）。

### L1. earthwork_factor 土石方压实系数（.tsf）

`design_project_id` + 六类土石的压实系数 + `remark`。

| 列 | 类型 | 含义 |
|---|---|---|
| `design_project_id` | bigint | → `design_project(id)`。**★不是 `section_id`**：全工程一组系数 |
| `factor_soil_1` | numeric(8,4) | 松土的压实系数（.tsf 列「土方1」）。本工程 **1.23** |
| `factor_soil_2` | numeric(8,4) | 普通土（「土方2」）。本工程 **1.16** |
| `factor_soil_3` | numeric(8,4) | 硬土（「土方3」）。本工程 **1.09** |
| `factor_rock_1` | numeric(8,4) | 软石（「石方1」）。本工程 **0.92** |
| `factor_rock_2` | numeric(8,4) | 次坚石（「石方2」）。本工程 **0.92** |
| `factor_rock_3` | numeric(8,4) | 坚石（「石方3」）。本工程 **0.92** |

唯一约束 `(design_project_id)`。

**★★ 用法：压实方 = 松方 ÷ 系数**（系数是「松方 ÷ 压实方」的倍数，**不是乘数**）。

**★★ 这个语义不是猜的**，是实测反推 + 算术验证：
`过程`表 GCID=1 里 `用土 819.1131000000418` → `用土(压实) 707.1642932578042`，比值 1.1583。
本表 土方1/2/3 = 1.23/1.16/1.09 对应 松土/普通土/硬土，按 `earthwork_composition`（.CTR 的 TFFD）
实测的 20%/60%/20% 加权：`0.2/1.23 + 0.6/1.16 + 0.2/1.09 = 0.8633`，
`819.1131 × 0.8633 = 707.13` ✓ 与实测 707.1643 对得上。

**★ 六分类与 `earthwork_composition.pct_1..6` 同一套**（松土/普通土/硬土/软石/次坚石/坚石）。

**★★ 为什么没有桩号** —— 三种情况要分清：

| 表 | 锚 | 为什么 |
|---|---|---|
| `.tf` / `.lj` / `.HDM` 逐桩表 | `station_id` | 行数 = `.STA` 桩号序列 |
| I 节（`.CTR`） | `station_km` 直接存 | **有桩号但不同源**：分段桩号不是 `.STA` 的子集 |
| **本表** | `design_project_id` | **根本没有桩号**：全工程一组系数 |

**源文件**：纬地 **HintTF 土石方调配系统**的 `.tsf`，**Microsoft Access / Jet 4 数据库**，
2,695,168 B，**20 张表**。
⚠ 此前它被归进 `_BLOCKED_SUFFIX`（理由「需 ODBC/Jet 引擎」），**那条理由是错的** ——
实测 `uv run --with access-parser` 纯 Python 就解出 20 张表，表名/列名全中文。
已从 `blocked` 移到 `pending`。

## 时序库建模要点（IoTDB 优先评估）

```
存储组 root.road.<line_code>         例 root.road.g228
序列    root.road.g228.<station>.<install_id>.<quantity>
        root.road.g228.k4635710.s1001.strain
        root.road.g228.k4635710.s1001.accel_x
特性    1. 原始层 TTL 滚动（保留 1~2 周）+ 特征值层永久（min/max/mean/rms/std/p95，对齐 dict_stat_metric）
       2. 高频（应变/振动）与分钟级（气象）分存储组，避免压缩率互拖
       3. 写入幂等去重（时间戳+通道唯一）
       4. 批次号贯穿两侧对账（data_import_batch.id）
```
TimescaleDB 备选（同生态）：`ts_sample(channel_id, ts, value, quality)` + hypertable 按日 chunk；128kHz 单通道日 8.6 亿点需原始+稀释双层。

---

## 待办（下阶段）

- [ ] 数据字典字段逐项过 T/CECS 规程附录（静态/动态/运行三表）核对名称与格式（n..4,1 等 ISO 类型标注）
- [ ] 时序库 POC：IoTDB vs TimescaleDB（写入吞吐/压缩率/查询延迟）
- [ ] dict 种子数据脚本 + 分区自动建表函数（monthly_partition_ddl()）
- [ ] 传感器台账字段对接厂家出厂参数（±5000με/1k-128k/精度等级）
- [ ] 与二期 PHM（MySQL+MongoDB）现网数据迁移/共存的映射评估
