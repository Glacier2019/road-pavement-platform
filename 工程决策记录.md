# 工程决策记录（History of Decisions）

> 此文件由 2026-09-15 历史清理前自动导出，保留全部 26 条提交的完整信息，
> 避免清理历史后「当初为什么这么做、踩了什么坑」丢失。
> **提交哈希为清理前哈希**，仅作检索标识；正文才是价值所在。

---

## 69124f2 chore: 建立版本控制与 .gitignore

chore: 建立版本控制与 .gitignore

此前整个项目（38.5 人周工作量、十余份交付物）没有任何版本控制，
2026-09-14 的会话里用 rm 删掉了 16 个文件，全部不可恢复。

忽略项：.docxvenv 虚拟环境、__pycache__、cailiao 原始资料（128 MB 输入）、
bands 中间裁剪图（可由 cailiao + crop_bands.py 再生）。

---

## d979ec8 交付物与生成脚本：报告 v1.2、研究生任务计划、DDL 与数据字典

交付物与生成脚本：报告 v1.2、研究生任务计划、DDL 与数据字典

- 路面性能数智化平台总体设计与模块搭建方案（7域主体-12域全景·v1.2）docx/md/pdf 三格式
- 研究生任务分配与个人实施计划（路面性能7域·平台主线）docx：
  本次修订补入第五章「论文课题：创新点核查、几何主线与文献起点」
  （四课题评级 / 几何主线 / 差异化声明 / 4 章骨架 / 10 条已核实文献 / 检索局限），
  并把章节标题从手工加粗的 Normal 改为内置 Heading 1/2 样式，恢复 Word 导航与自动目录能力
- 路面性能数据库-DDL-v0.1.sql、数据字典 v0.1、结构化大纲
- 各生成/校验脚本（build_docx_report_v3_std.py、gen_student_plan_docx.py 等）
- extracted/ 标准与设计文件的文本抽取结果

---

## d6c2234 骨架栈 scaffold：契约先行的 M1/M2/M6 竖切与三项契约测试

骨架栈 scaffold：契约先行的 M1/M2/M6 竖切与三项契约测试

- 修复 M6 出口服务的路由遮蔽缺陷：/v1/objects/{object_type} 原先注册在
  /v1/objects/wim_axle 之前，把静态路由静默吃掉，导致该端点永远返回 404
  （契约文件与接入规约都把它当作标准路径）。现调整注册顺序并加注释约束。
- 新增 tests/contract/test_api_routes.py：路由自洽 + 契约双向对齐 + TestClient 真实请求
  三组断言。已实测：对修复前的顺序能报出 3 项失败，对修复后全绿。
- README 验收判据第 7 条由"一项契约测试"扩为三项；导读同步更新验证状态。

---

## b14de79 会话审计：session-20260907 问题分析与证据文件

会话审计：session-20260907 问题分析与证据文件

- 会话审计-20260907_094951.md：325 条消息的分级问题清单、已核对无问题项、修复顺序
- session_timeline.txt / session_narrative.txt：导出会话的可读时间线与叙事摘要

---

## bcea257 test: 路由契约测试支持零依赖运行

test: 路由契约测试支持零依赖运行

psycopg 缺失时自动用最小桩顶上（该测试本就不触库），并打印告警而非静默；
docstring 补充"本机直跑"方式。现在无需网络、无需容器、无需建环境即可执行。

---

## 48b4bad 架构图与模块说明：6 服务落地范围、M1-M10 分工、7 域数据流动

架构图与模块说明：6 服务落地范围、M1-M10 分工、7 域数据流动

---

## 8212ebd 架构图改为出版级三图（A/B/C）：5pt 字形硬下限 + 官方审计全绿

架构图改为出版级三图（A/B/C）：5pt 字形硬下限 + 官方审计全绿

按 nature-figure 技能契约重建图件，替换原来字号仅 2.6pt 的合图。

- 新增 gen_arch_figures.py：matplotlib 定尺出图（170mm 宽 = 报告最窄排版宽度），
  字号以 pt 为单位，使 5pt 下限成为结构保证而非事后验算；
  自检覆盖真实文本包围盒、出画布、重叠、字号下限、缺字形。
- 新出架构图A（分层架构与模块接口）/ B（七域数据流动）/ C（落地范围与模块分工），
  各出 PDF+SVG+PNG@600dpi+TIFF@600dpi(LZW)，SVG 文本可编辑。
- 官方审计：validate_figure 20 pass/1 warn/0 fail；
  audit_pdf_text 三图 minimum found 5pt、below minimum 0；
  audit_figure_collisions --strict 三图 0 fail/0 warn。
- 官方审计抓出并修复 3 个自检漏掉的缺陷：B 图列坐标累加错误致最右列出画布(44 FAIL)、
  A 图虚线契约框压表头、C 图文字压方块下边框。自检已补画布边界检查。
- 字体与 emoji：matplotlib 链无 Noto Sans CJK SC（仅 JP 面）会静默回退，
  改为显式首选 WenQuanYi Zen Hei 并核验零缺字形；U+26A0 在该链缺字形，改矢量警告三角。
- 新增 qa_render.py（最终尺寸栅格 + 视觉抽查裁切）与
  output/架构图-图件契约与QA记录.md（含诚实标注：哪些已目视、哪些仅程序化验证）。
- 改名避开报告已占用的图号（原 图3 与 图3-板块全景与数据依赖 撞号）。
- TIFF 加 LZW，单图由 43-46MB 降至 1.1-2.1MB。

---

## 8232691 补齐架构图视觉 QA，并更正「视觉通道余额耗尽」的错误结论

补齐架构图视觉 QA，并更正「视觉通道余额耗尽」的错误结论

用户核查后指出通道余额正常，经复测确认：那次 402 是瞬时错误，通道当时可用。
真正导致多张裁切块「读出胡话」的是裁切方法，共三个原因：

- 坐标系搞反：PIL 原点在左上、图件在左下，按图件坐标裁切取到了另一端；
- 把文字横切一半：手工估 y 落在文字中间，模型只拿到半个字高便自信编造
  （实测编成医学文本、MBTI 职业对照表）——它编造时不报错，语气与读对时一样确定；
- 关键词子串匹配抓错段落："红虚线" 在副标题里也有，并集包围盒横跨整幅图被压缩。

修正方法（已固化进 qa_render.py）：
- 裁切框改由 PDF 文本层真实包围盒驱动，结构上不可能切断文字；
- 关键字用完整段落文字匹配；不靠降采样凑像素预算，而是收小裁切框；
- 密集区块按 y 分带逐行取（94.5k 像素塞 12 段会编造，单行 5 段约 7 万像素才可靠）。

视觉 QA 结果（22 处抽查全部逐字读对，6 处模型主动报「无不确定字符」）：
- 架构图A：版面配色、图例间距、契约栏全部三块；
- 架构图B：表头行、色带正文、红/绿双闭环标注（并确认虚线在文字上方未压线）、底部注记；
- 架构图C：服务行四组、端口列对齐、契约块①②③④、分工表 M1-M3/M6/M10 行、M7 注记。
未逐个目视的仅剩 C 图分工表 M4/M5/M7/M8/M9 行与四个端口值（同路径同字号，
已通过官方碰撞审计 0 fail/0 warn），已在文档中逐条标注为「未目视」。

同时更正 QA 记录第四节：删去「余额耗尽」的误判，如实记录已目视/未目视清单、
踩坑原因与视觉通道真实能力边界（瓶颈是文本段数与输出长度，不只是像素）。

---

## fabfe5e 导出件改为字节可复现：固定 SVG 元素 id 与 PDF 创建时间

导出件改为字节可复现：固定 SVG 元素 id 与 PDF 创建时间

排查「提交里出现不该有的 SVG 改动」时发现：qa_render.py 本身无副作用
（已用运行前后指纹比对实测确认），真正原因是导出件字节不可复现——
每次重跑都产生假 diff。两处来源与修法：

- SVG：matplotlib 默认给 clip-path 等元素生成随机 id，并写入 dc:date
  → svg.hashsalt 固定 id；导出传 metadata={'Date': None} 去掉时间戳；
- PDF：后端默认写入 /CreationDate 当前时间
  → 在 import matplotlib 之前 setdefault SOURCE_DATE_EPOCH（内建在脚本里，
  不依赖调用者环境变量，否则别人跑仍不一致）。

修完连续两次运行的全部 PDF/SVG 的 md5 完全一致；官方审计无回归
（validate 20 pass/1 warn/0 fail，三图 text 与 collisions 均 PASS）。
QA 记录补记该问题与修法。

---

## fdbae52 回源核对图件内容，修正 7 处错误口径并同步 5 个交付件

回源核对图件内容，修正 7 处错误口径并同步 5 个交付件

此前只验了「印出来清不清楚」，没验「图上写的话对不对」。本次从生成脚本抽出
图上全部 185 条文本，逐条回到报告/DDL/数据字典/scaffold/学生计划核对。

查出 7 处错误（同一类错误的多个投影）：
- 把报告第五章「25 表核心逻辑视图」误当物理 DDL v0.1 的表数（实为 30 表）：
  数据字典第 13 行、报告第五章说明、DDL 文件 30 个 CREATE TABLE 三处互证。
  图A 层间契约②、图A 契约栏②、图C 契约块② 共 3 处受影响。
- 图A 声明「契约测试三项：DDL／报文 Schema／OpenAPI」——scaffold/tests/contract/
  实为 Schema↔Pydantic／造数器／HTTP 路由三项，**没有任何 DDL 测试**。这是声明了
  一项并不存在的测试覆盖，最严重。（scaffold/README.md L136 一直是对的，
  是图件转写时写错。）
- 图C 阶段：M4 P1·P2→P1、M10 P5→P4（依 §6.2「P1=M1/M2/M3/M4…P4=M7（+M10 评估）」）；
  M9 报告未单列，改为显式标注推断来源而非编造。
- 「零依赖可运行」不成立（三个测试都用 uv run --with 拉依赖）。

核对为正确的关键论断（未改）：16 条容器化条目、6 服务映射、M1–M10 名称、
关键路径、A/B/C/D 分工与 M7 未指派（学生计划第三章）、7 域感知源与双库归属、
待办①②、动作层 501、MCP 出处 §10.4、契约①③④ 真源路径。

同类错误扩散到 4 个交付件，已一并修正并重新生成产物：
架构与模块说明（含 15→16 条容器化口径）、MODULE-ONBOARDING、
学生计划生成脚本＋Word、旧挂图脚本＋SVG/PNG。

QA 记录新增第七节「内容事实核对」，含错误表、正确论断证据表、扩散表与教训。
官方审计无回归（20 pass/1 warn/0 fail，三图 text 与 collisions 均 PASS），
导出件仍字节可复现。

---

## 39612d5 新增图 D：一页总图（模块作用分色 + 7 域数据流）

新增图 D：一页总图（模块作用分色 + 7 域数据流）

---

## d2adfed 实跑验收：修 3 个缺陷（API 500 / minio latest 失效 / 缺 healthcheck）+ 图D 已完成代码绿框标注

实跑验收：修 3 个缺陷（API 500 / minio latest 失效 / 缺 healthcheck）+ 图D 已完成代码绿框标注

---

## 49d0923 M3（对象域 DAO）落地 + Grafana 界面中文化

M3（对象域 DAO）落地 + Grafana 界面中文化

一、M3：把"应用不直连存储"从约定变成结构上做不到
  骨架期 M6（services/api/app.py）自己 import psycopg、自己持有 ConnectionPool、
  自己内联 SQL —— 报告 6.1 写着"应用不直连存储"，代码上却只能这么写。
  新增 packages/rpdao/（catalog / repo / pool / errors + 契约③ 真源 README），
  M6 改为只经 DAO 取数，拿不到 cursor、越域取数抛 ContractViolation。
  边界刻意划清：M3 是"数据出口"层；M2 作为"接入/落库"这个动作本身仍直接写库，
  写入收口是后续议题（已写进 README 欠账）。

  新增 tests/contract/test_dao_contract.py，把四条架构约束变成可执行断言：
  域目录自洽（21 域内 + 9 跨域 = 30）、目录↔DDL 双向对齐（30 对 30 零差异）、
  越域/越界/注入防护、M6 不直连存储（AST 判定）。

  写这个测试时测试自己先错了两处，都已修并记录：
   - 误报：正则把文档字符串里"不再 import psycopg"判成违规 → 改用 AST；
   - 漏报（更危险）：查"可选筛选项必须带 cast"的正则没考虑 cast 夹在中间
     （%(x)s::timestamptz IS NULL），一处都扫不到却仍打印通过。修正后
     **用修复前的 SQL 反向验证该断言确实会报错**，避免出现永远为真的假测试。

  复验：四项契约测试全绿；接口 passages=194/overloaded=38 与库内完全一致；
  6/6 容器 healthy；GET /v1/catalog 返回 7 域 / 21 种对象类型 / 9 张跨域表。

二、Grafana 中文化：设置一行不够，版本决定覆盖面
  加 GF_USERS_DEFAULT_LANGUAGE=zh-Hans 即可（自带语言包，无需插件），启动日志
  确认已采纳。但 11.3 下首页与主界面大半仍是英文——实测两版语言包：
    11.3.0 →  96 命名空间 /  1,676 条
    13.2.1 → 215 命名空间 / 10,935 条（6.5 倍）
  故升到 13.2.1（钉死版本，不用浮动 tag）。provisioning 完好：数据源 rp-pg
  仍在、4 个面板仍绑定、查询实测 194/38；6/6 容器仍 healthy。
  代价：镜像 485 MB → 1.39 GB（在意可退回一行，代价是半中文）。

  排查中撞到一个真实陷阱并写入 compose 注释：GF_SECURITY_ADMIN_PASSWORD
  **只在首次初始化建库时生效**，持久卷里已有口令时改 env 无效；用
  grafana cli admin reset-admin-password 可修，但**必须重启容器**——运行中的
  进程缓存着旧口令哈希（对比三次 grafana.db 的 user.updated 证实哈希已变、
  接口仍 401，restart 后立刻 200）。另：排查时别用 curl 反复重试，会触发
  5 分钟连续失败锁定，让人误判成"重置没生效"。

三、图 D 与文档同步
  双库存储层 chip 由绿虚框转为绿实框（M1 存储 + M3 DAO 均已落地），
  服务层虚框注明"查询路由已实跑验证／动作层仍 501"；审计仍 PASS（0 fail/0 warn）。
  README 补四份契约（原写"三份"，缺契约③ DAO）、目录树补 packages/rpdao、
  §7 模块接入顺序标出已完成 M1/M2/M3/M6 与下一步；
  验收报告补第八、九节（M3 复验 + Grafana 两个陷阱）。

四、仓库卫生
  .dockerhome/（DOCKER_CONFIG 指向的本机 Docker 配置，含 buildx 状态）此前被
  跟踪，已加入 .gitignore 并停止跟踪；补 .uvpython/。已确认该目录无凭据文件。

---

## 14650d8 课题乙文档：表格内粗体标记 **X** 转义为「X」，并同步重生成 docx 与路线图

课题乙文档：表格内粗体标记 **X** 转义为「X」，并同步重生成 docx 与路线图

---

## 5355323 整理仓库：文档流水线收进 docpipe/，根目录只留开发代码/交付物/流水线三类

整理仓库：文档流水线收进 docpipe/，根目录只留开发代码/交付物/流水线三类

根目录原本散着 28 个 .py 脚本 + input/ extracted/ bands/ cailiao/ 四个素材目录
+ 两个会话审计 txt + 一个 md，与开发代码 scaffold/ 混在同一层，看不出主次。

先确认了一件事：**开发代码不需要新建文件夹——它已经在 scaffold/ 里了**。
该收拢的是文档与图件流水线（非平台代码）。

整理后根目录只剩四项：
  README.md      仓库导航（新增：哪个目录是什么、开发时动哪个）
  scaffold/      平台开发代码（未改动）
  output/        交付物 + 表结构 DDL 真源
  docpipe/       文档与图件流水线（新增）

docpipe/ 结构：
  scripts/     28 个生成脚本
  materials/   原始资料（原 cailiao/，149 MB，不入库）
  extracted/   素材 PDF 抽取文本
  bands/       制图中间裁切图（17 MB，不入库）
  input/       原始笔记与待确认项
  audit/       会话审计产物（含原先散在根目录的两个 txt 与一个 md）

为什么 output/ 必须留在根目录、没有一并收进 docpipe/：
  docker-compose.skeleton.yml 直接挂载 ../output/路面性能数据库-DDL-v0.1.sql
  给 PostgreSQL 初始化——它是契约②的唯一真源，挪窝就要改挂载与 20 多处引用。

搬家安全性是查证过的，不是想当然：
  - 26/28 个脚本用绝对路径 /data/cy/shujuku/... 定位输入输出；**没有任何脚本用
    Path(__file__) 或 os.path.dirname(__file__)**，故脚本换位置后写入目标不变；
  - 已跟踪文件用 git mv，git 识别为 56 个重命名（--find-renames 确认），历史保留；
  - 有 4 个脚本的素材路径需要同步改（build_docx_report_v3_std.py / extract_pdfs.py /
    vl_bands.py / crop_bands.py）。其中 crop_bands.py 是**残留复查时才发现的**——
    我第一遍 grep 的模式写作 `bands/`（带斜杠），而它是 `bands"`，漏了；改用
    扫描旧根目录全部路径的复查才发现。改完已逐个确认目标目录真实存在。

执行副作用与修复：mkdir 先建了目标目录，导致 mv 把素材又套了一层
（docpipe/materials/cailiao），已拍平；拍平后逐个核对文件数，与搬家前记录一致
（materials 246/149M、extracted 23/1.9M、bands 6/17M、input 2）。

验证（改完实跑，不是只看文件在不在）：
  - 从新位置执行 gen_arch_figures.py：正常出图 132 段文本、0 问题，
    产出仍落在 output/，两项图件审计 PASS；
  - docker compose config 通过，DDL 挂载未受影响，6/6 容器仍 healthy；
  - .gitignore 同步为 docpipe/materials/ 与 docpipe/bands/。

注：output/ 下 4 个「课题乙」文件在本次整理期间（14:24–14:26）被另一路工作改动，
不属于本次整理，已保持未暂存、未提交。

---

## 1e18983 图 D 补开发视图、新增图 E 开发路线图，并出下一阶段任务分配与真实数据校准评估

图 D 补开发视图、新增图 E 开发路线图，并出下一阶段任务分配与真实数据校准评估

用户要求：在图 D 上画出其他模块、体现可分模块分小组开发、独立可拔插、先后顺序，
并分配下一阶段任务；同时问真实数据进来后各模块的测试校准工作量。

一、先纠正一个前提：模块不用新画，图 D 上本来就有 M1–M10
  缺的是「谁做、什么阶段、插哪个契约、哪些还没落地」这层信息。所以本次是**加开发视图**，
  不是补模块。

二、图 D（增补）
   - 每行标注「阶段 · 主责组 · 插槽」：P1 · B组 · 插槽：②  …
   - 未落地模块（M4/M5/M7/M8/M9/M10）改用**灰虚框**，与已落地的绿框形成一眼可辨的对比
   - 图例写明口径：阶段取报告 §6.4（P1=M1/M2/M3/M4 ｜ P2=M5/M6 ｜ P3=M8 ｜ P4=M7(+M10)）；
     主责取图 C 分工表（A=数据底座 B=感知接入 C=融合诊断 D=应用决策）；
     插槽 ①②③④＝四份契约
   - 不引入第三种配色：图件契约写明「行色＝模块作用，列色＝对象域」，加组色会破坏这套纪律，
     故分组用文字表达、状态用既有绿/灰框
   - 修掉图例里一处我上次编辑留下的重复句

三、图 E（新增）｜分模块 · 分组 · 先后：可拔插式开发路线图
   「先后」是时间维度，硬塞进图 D 会同时毁掉两张图的可读性，故单独成图：
   横轴＝P1..P5 阶段，纵轴＝6 个主责组下的 M1–M10，状态分已交付/下一步/待开发，
   并标出关键路径 M1→M3→M4→M5→M6→M8 与可并行单元 M2‖M3、M7‖M8（报告 §6.4 原话）。
   报告的阶段日期**没有**照排到图上：原计划 P1＝2027.3-5，而实际 2026-09 就已 3/4 完成，
   照排会误导；改在图例交代计划节点与实际进度的差异。

四、图 C（修正 3 处，跨交付物一致性）
   - M3「规划中」→「已落地」：DAO 已实现并实跑验证
   - M4「部分落地」→「规划中」：这是本次查出的**实质错误**。data_quality_log 里的 26 条
     是 M2 按自身职责写的契约级拒收记录，ingest 明确声明「不承担治理（M4）职责」；
     M4 的核心动作「真值晋升」从未发生（data_import_batch.truth_flag 全为 false）⇒ M4 无实现代码。
     原「部分落地」与验收报告「尚无实现代码」矛盾，核实后采信后者。
   - 契约③ 从「M3 对外接口（待定稿）」改为指向真实真源 scaffold/packages/rpdao/README.md

五、下一阶段任务分配与真实数据校准评估（新增文档）
   结论：下一阶段＝P1 收尾补 M4 真数据门——P1 的验收条件写的是「真数据门上线」，门没开 P1 就没结束；
   它是 P1 四模块里唯一没动的，也是关键路径上唯一缺口。M4（A组）与 M7（待指派）互不依赖，可并行。

   关于「真实数据放进来校准工作量大吗」，先纠正提问前提：
   「现在测试版比较快」不是效率高，是**覆盖面窄**。实测：30 张逻辑表 15 张有行、15 张零行，
   有行里只有 2 张承载业务数据（且是合成的），7 个域只有 LO 跑过数据，1 条报文契约、1 个造数器，
   真值晋升 0 次，calibration_log / alarm_rule 均 0 行。
   结论分三层：
   (1) 工作量在各模块间**高度不均匀**——M3 几乎不动，M1 是容量/性能问题，M2/M6 中等，
       **M4/M5 最重**（阈值标定与反演标定，本质就是标定工作）。
       校准集中在语义与质量层，不摊在每个模块上——这正是「只认契约」的红利。
   (2) **真正的成本驱动不是校准，是契约漂移**：契约一改，所有依赖它的组全部返工。
       故成本 ≈ 可控的校准工时 + 契约变更数 × 每组返工（随组数放大）。
       控制手段是各组开工前先做「真实数据摸底」：拿真实报文/表格先跑契约校验看违约率与类型分布。
   (3) 前置阻断项：**IoTDB 不在栈里**（compose 中 0 次出现），RE/WE 两域没有时序落点，
       真实数据现在放进来做不了——不是工作量大，是没有容器可放。
   诚实边界：拿到真实数据前不给工时数字，只给结构与判据。

   另附本次查出的 4 处既有文档不一致（阶段时间表、契约份数、IoTDB 时点、M4 状态），建议一并统一。

验证（全部实跑，非目测）：
   - 五张图两项独立审计全数双 PASS（字号下限 ≥5pt、零碰撞）
   - 图件字节可复现：同一份代码连跑两次，全部产物哈希一致
   - 四项契约测试全绿；6 服务 healthy；端到端 passages=194/overloaded=38 未受影响
   - 文档引用的报告原话逐条 grep 核实在原文中存在

注：output/ 下 4 个「课题乙」文件属另一路正在进行的工作，未暂存、未提交。

---

## 5b957b5 模块骨架：M4/M5/M7/M8/M9/M10 机械层落地，业务层留空待各组实现

模块骨架：M4/M5/M7/M8/M9/M10 机械层落地，业务层留空待各组实现

按用户选定范围执行：写 6 个模块的骨架 + 契约测试 + 容器 + 各组开发模板。
不写业务逻辑——M4 阈值、M5 标定系数、M7 映射规则都必须用真实数据定，
提前编数字再拿合成数据"验证通过"只是自我印证（造数器 axle_num 分支
自我抵消即为此类实例）。

机械层（接入五件套全齐，每模块一份）：
  容器定义   Dockerfile + compose 服务段（各带独立 profile + healthcheck）
  健康与指标 /healthz 逐项报依赖、/metrics 出 Prometheus 文本
  配置外置   config/*.yaml，代码零硬编码
  契约文件   contracts/{governance,fusion,semantic,apps,console,agent}/
  契约测试   tests/contract/ 离线可跑，覆盖应通过/应拒绝两侧 + 反向确认

新增契约 6 份（草案 v0.1，待各组定稿）：
  M4 quality_rule / promotion   M5 diagnosis   M7 mapping_set
  M8 maintenance_plan           M9 module_registry   M10 action_ticket

反空转设计（防"永远不会失败的检查"）：
  - 规则集未标定 → 校验/晋升返回 503，而非"通过"
  - 无 truth_flag=true 批次 → 融合辨析返回 503，而非对未晋升数据出诊断
  - 未实现接口 → 501 并标注 owner，而非空数组

实测：
  - 9 个 app.py 全部编译通过
  - 6 个契约测试全绿；run_contract_tests.sh 一键跑，反向验证确认会报失败
  - M4 实跑 /healthz 200（PG 通、11 条规则加载），门禁正确关闭
  - M9 实跑探测 10 个登记模块，可达 6/不可达 4 如实区分（新增 TCP 探测，
    避免用 HTTP 去测 PG 得到误导性错误）
  - compose 默认仍原 6 服务，--profile mX 才多起对应模块
  - 原 6 服务基线未受影响，端到端 passages=194/overloaded=38 两侧一致

文档统一：
  - 三份契约 → 四份（补契约③ DAO），编号与报告 §6.4 对齐
  - 新增 scaffold/SKELETON-GUIDE.md（各组开工前先读）
  - README 目录树补 6 个模块服务

未做（用户未选）：修造数器 axle_num 自我抵消 bug；图件契约 QA 记录更新

---

## 757bc2f 修造数器隐蔽 bug：违约被「以错误的理由」拒收 + 违约日志无法区分类型

修造数器隐蔽 bug：违约被「以错误的理由」拒收 + 违约日志无法区分类型

一、造数器 axle_num 分支自我抵消（原 bug 比记录的更严重）

原写法把 axle_num 加一后又 append 了一个 5000kg 的轴，两个改动互相抵消：
 ① `len(axles) != axle_num` 这条校验分支**从未真正触发过**；
 ② 那条多余的轴不在总重里，报文被「总重偏差」规则拦下——**被拒了，但拒因是错的**。

所以原测试「50/50 被拒收 → 通过 ✓」照样全绿，覆盖的却是另一条分支。
库内实据：26 条违约里，被记为总重偏差的 9 条其实是轴数不符
（差值恰为 5000.0 即指纹）——拿日志排障的人会去调总重校验，方向全错。

修复：
  - simulator: 只改 axle_num、不动 axles；新增 _assert_injected() 自检，
    确认「想注入的违约确实注入了，且只注入这一种」
  - test_simulator_contract.py: 断言从「被拒」下沉到「拒因正确」，
    逐类核对 300 条样本的拒因分布（旧断言下 bug 可通过，新断言下报
    「axle_num 违约从未产生」「只产生 2/3 类违约」）
  - 反向验证：把 bug 塞回去 → 新断言正确报失败；恢复 → 全绿

二、违约日志无法区分类型（3 类违约共用一个 issue_code）

接入服务原先把所有违约记成 issue_code='contract_violation'，26 条违约在日志里
完全分不出成因。新增 services/ingest/violations.py 做精确分类：
  - 命名空间与 M4 规则集共用；两条跨字段规则 cross.axle_num_vs_axles /
    cross.gross_vs_axle_sum 在 M2（接入时点）与 M4（批次时点）**必须同名**
  - 覆盖 13 种违约形态，无一落入兜底码；兜底码 contract_violation 刻意保留，
    用于暴露「新增校验忘了分类」
  - 分类时补了一课：`device_code=None` 是类型错误而非字段缺失，
    首版分类器漏了整类形态（被自己的测试抓出）

三、实测
  - 端到端 1:1 吻合：发 5 超速/6 总重/4 轴数 → 库里精确落成
    5 range.speed_kmh / 6 cross.gross_vs_axle_sum / 4 cross.axle_num_vs_axles
  - 新增契约测试 test_violation_codes.py（含 M2↔M4 跨层命名一致性检查）
  - 契约测试 7 个全绿；ingest 重建后 /healthz 200、MQTT 订阅正常
  - Dockerfile 同步 COPY violations.py（否则容器内 ImportError）

文档：
  - SKELETON-GUIDE.md 新增 §6 案例（含原始代码、指纹证据、三处修复、反向验证）
  - 任务文档把该 bug 从「已知未修」改为「已修 + 案例」

---

## d85952b 文档口径统一：契约份数、时间表冲突显式标注、图 E 登记进 QA 记录

文档口径统一：契约份数、时间表冲突显式标注、图 E 登记进 QA 记录

一、契约份数（原「三份」）统一为四份

MODULE-ONBOARDING 写「三份契约」（无 DAO），scaffold/README 已更新为四份，
导读文档亦为三份——三处不一致。统一为四份，并把编号与报告 §6.4 对齐
（① 报文格式 ② 表结构 ③ 数据出口 DAO ④ 服务接口）。
工单模板里的旧编号（#1 表结构/#2 报文格式/#3 服务接口）一并修正。

二、阶段时间表冲突：显式标注，不擅自改

MODULE-ONBOARDING §4 与总设计报告 §6.4/§七 有三处不一致：
  - P1 时间：2026.11–2027.1  vs  2027.3-5
  - P2 内容：M5+IoTDB+WE      vs  M5+M6
  - IoTDB 时点：P2 随 RE 接入   vs  P1 第一优先补齐项
这三处都影响**各组的工作截止时间**，属管理判断而非文档口径，故在
MODULE-ONBOARDING §4 用警示框如实并列两说 + 判据 + 「待导师定夺」，
不删任何一方。阶段划分（P1=M1/M2/M3/M4｜P2=M5/M6｜P3=M8｜P4=M7(+M10)）
以报告 §6.4 为准，此点无争议，已对齐。

三、图件 QA 记录新增 §10（图 E + 骨架后的状态口径）

  - 图 E 的定位：图 D 答结构问题、图 E 答时间与归属问题；刻意不给图 D
    再加颜色维度
  - 图 E 两处踩坑：单模块组带 12.2pt 装不下 13pt 标签（差 0.3pt，目视不可见，
    只有碰撞审计抓得到）；合并注记 160 字=630pt 超出 481.9pt 栏宽
  - §10.3 如实记录：图 D §9.1「无框：M4–M10」在骨架落地后**已过时**，
    但**本次未改图**——「骨架算不算落地」是管理判断，需导师先定口径
  - §9.1 原表述已加过时标注并指向 §10.3

四、任务文档的不一致表更新

第 1、3 条标注为「已在文档中就位、待导师定夺」；新增第 5 条记录图 D 状态过时。

---

## a3b8021 图件新增第四级状态「骨架已就位」+ 修掉图上两处与事实相反的过时陈述

图件新增第四级状态「骨架已就位」+ 修掉图上两处与事实相反的过时陈述

一、新增第四级状态（导师定口径后）

§9.1 原有三级装不下骨架态：塞进「绿虚·部分落地」说不清哪部分落了地（读者会高估），
塞进「无框·无实现代码」与事实相反（容器起得来、/healthz 实测 200）。骨架是机械层完整、
业务层全空，与「部分落地」是**性质差别**而非程度差别，故新增一级，四态并列：
  done=绿实线 ｜ partial=绿虚线 ｜ skeleton=灰蓝点线 ｜ plan=灰实线

新常量 ACCENT["skeleton"]="#5A6B7A"（偏蓝的灰）。不用纯灰是因为灰度打印时它与 plan 的
#8A8A8A 几乎同色，必须靠「点线 vs 实线」二次编码才分得开。

落地范围（三张图，全部只改数据字典、不碰布局代码）：
  - 图 A：M3 plan→done；治理层 M4+M7 part→skeleton；融合辨析层 M5、应用层 M8/M9/M10 →skeleton
  - 图 C：M4/M5/M7/M8/M9/M10 规划中→骨架
  - 图 D：DEV 三条带 + DEV_X 两条横切带 →skeleton
  - 图 E：M5/M8/M9/M7 →skeleton；M4 保持 next（"下一步启动"更具行动价值）

二、顺带修掉两处**与事实相反**的过时陈述（比状态口径更严重）

  1. 图 A · M3 层原写「api 服务直接持有 psycopg 连接池查 PG，DAO 尚未抽出」——
     实际 DAO 已抽出为 rpdao 包（pool/repo/catalog/errors），api/app.py 改为
     from rpdao import、不再持有连接池、不再内联 SQL，且有 test_dao_contract.py。
  2. 图 C 注记原写「M4 无实现代码」——实际 M4 骨架已就位并实测（/healthz 200、
     11 条规则加载、门禁关闭返 503），只有真值晋升未实现。

  状态标签错了是分类问题（读者会存疑），陈述句错了是事实问题（读者会直接采信）。
  图上凡以陈述句断言"某物不存在"，都必须在事实变化时同步核验，否则会静默变成假话。

三、图 E 阶段日期同步为已定口径

ROADMAP_PHASES 原为报告 §七 的设计阶段口径（2027.3-5 起），改为
MODULE-ONBOARDING §4 的口径（2026.11–2027.1 起），并在脚本注释里写明依据。

四、踩坑三处（全部由自检/审计拦下）

  1. KeyError 'skeleton'：ACCENT 键名是 skel、状态值是 skeleton，两处不一致
  2. 越界宽 360.5/375.9 > 327.9：图 A 带内文字不放折行，新说明过长
     → 改用与脚本相同的估宽公式（全角=5.0/半角=3.4）**改前先算**
  3. 重叠 153.5×2.2pt：收短后多加了第 4 行，带高只容 3 行
     → 宽×高是两个约束，改文字时要同时算（坑 2、3 是同一处改两次才过）

五、审计（五图 × 两项，全 PASS）

  A/B/C/D/E：audit_pdf_text --min-pt 5 PASS（0 below）；
             audit_figure_collisions --strict PASS（0 fail / 0 warn）；
             内建自检合计 0 问题、缺字形 0 类；两次运行 PDF md5 一致（字节可复现）。

文档：QA 记录新增 §11（实施范围、四态表、过时陈述对照表、三处踩坑、审计结果）。

---

## 6f0bf87 修图 E 的图内自相矛盾：阶段条已改口径，底部注记还在复述旧口径

修图 E 的图内自相矛盾：阶段条已改口径，底部注记还在复述旧口径

改完 ROADMAP_PHASES 的日期后，图 E 底部注记仍写「阶段节点沿用报告 §七原计划
（P1 2027.3-5 ｜ …）」——同一张图里阶段条与注记给了两套日期。

这是本次最值得记的一次失败：阶段条改对了、注记没跟着改，而**自检与碰撞审计都抓不到**
——两者各自都是合法文本，不重叠、不越界、字号合规。只有把渲染出来的文本读一遍才发现。

教训：格式化审计只能证明"没说错形状"，不能证明"没说错话"。改一处口径，必须把同一份
产物里所有提到该口径的地方一起找出来核验——**包括以自然语言复述、不以变量引用的那些**。

修法：注记改为「阶段节点按 MODULE-ONBOARDING §4（导师确认）：P1 2026.11–2027.1 ｜
P2 2027.2–3 ｜ P3 2027.4–6 ｜ P4 2027.6–9 ｜ P5 二期——较报告 §七的设计阶段口径整体前移，
因 M1/M2/M3/M6 已于 2026-09 完成实跑验证。」

核验：get_text() 全文扫 2027.3-5 / 沿用报告 → 残留 0 行。
五图审计复跑：字号全 PASS、碰撞全 0 fail / 0 warn。
QA 记录补 §11.6。

---

## fdf0a7c 契约变更工单 #1（契约②扩展＋契约③写入收口）＋ 分组分模块任务书

契约变更工单 #1（契约②扩展＋契约③写入收口）＋ 分组分模块任务书

一、契约变更工单 #1：两张新表 + 写入收口（待导师确认字段设计）

现状核查（改之前的事实）：
  - 存量 30 张表，**无** quality_rule、**无** mapping_set —— 两张都是新表，走工单
  - mapping_set 的字段口径已由既有 JSON Schema 定下（其中已写明"需工单批准后方可落库"）
  - **读已收口、写完全没收口**：api 走 rpdao；但 ingest 用 4 条裸 SQL 直连写库，
    而 rpdao 一个写方法都没有 →「写入只经 M2」这条硬线**没有被代码强制**

字段设计要点：
  - quality_rule：code UNIQUE + layer，并加约束 `code LIKE layer || '.%'`
    —— 让"同一条规则在 M2/M4 用了不同的码"这种漂移**在数据库层就写不进去**，
    直接钉住上一轮那个真实故障（26 条违约全记成同一个码，3 类分不出来）
  - mapping_set：字段严格对齐既有 JSON Schema（mapping_id/source_kind/entries/
    status/confirmed_by），并把 schema 里的 allOf/if-then 同时落成库约束
  - 9 个待确认问题（Q1–Q9）在文档里逐条列出，附建议与备选

设计已实测验证（临时 schema 内，全部回滚未留痕）：
  quality_rule 5 条路径：正常插入✓、码前缀与 layer 不符被拒✓、
    标定态为真但无时间被拒✓、带时间通过✓、格式非法被拒✓
  mapping_set 7 条路径：候选态通过✓、已确认缺时间被拒✓、带确认人+时间通过✓、
    source_kind 越枚举被拒✓、entries 空数组被拒✓、proposed_by 越枚举被拒✓、
    (mapping_id,version) 重复被拒✓ 且不同 version 允许（映射可迭代）✓

二、分组分模块任务书（P1 起）

  - §1 四个组各自在 P1 的动作（A 做 M4、B 做写入收口、C 只做摸底不许启动、
    D 做 M9 看板与 M8 输入契约；M7 仍无人）
  - §2 十个模块逐一列"下一步第一件事"＋为什么 M4 是唯一关键缺口
    （truth_flag 全 false、11 条规则只有 2 条结构性规则能免标定）
  - §3 每个模块的"六件事"开工模板（现有 4 个跑通模块即范本）
  - §4 门禁表：谁能动、谁必须等
  - §5 开工前必做：真实数据摸底 6 项（A+C 共同）
  - §6 三条纪律（含造数器 bug 案例的引用）
  - §7 本轮交付清单 7 项，含 M7 指派与 IoTDB 决策

---

## 2affb85 契约②扩展 v0.2（quality_rule/mapping_set）＋ 契约③写入收口（M2 不再绕过契约）

契约②扩展 v0.2（quality_rule/mapping_set）＋ 契约③写入收口（M2 不再绕过契约）

按契约变更工单 #1 实施。这是 M4 真数据门与 M7 语义中枢从机械层进入业务层的前置条件。

一、契约② 扩展：DDL v0.1 → v0.2（30 → 32 表）

新增两张表，存量 30 表未改一列（纯新增、无破坏性变更）：
  - G5 quality_rule  M4 质量规则库：规则定义 + **标定状态可审计**
    核心约束 ck_qr_code_layer：`code LIKE layer || '.%'` —— 规则码前缀必须等于 layer，
    使「同一条规则在 M2 与 M4 用了不同的码」这种漂移**在数据库层就写不进去**。
    直接钉住上一轮的真实故障：26 条违约全记成同一个 issue_code，3 类分不出来。
    另有 ck_qr_calibrated：标定态为真必有时间（「标定过没有、谁标的」不再只是 YAML 布尔值）。
  - H1 mapping_set   M7 映射集：字段严格对齐既有 JSON Schema，并把 schema 里的
    allOf/if-then（「已确认」必带确认人）**同时落成库约束** —— 因为 M7 的写入
    可能来自导入脚本而不经 API，schema 未必跑得到，库约束才是兜底。

版本文件重命名 v0.1 → v0.2，13 个文件 15 处引用同步更新。

二、契约③ 写入收口：读已收口，写原先完全没有

收口前的事实：api 走 rpdao 取数；但 ingest **自带连接池 + 4 条裸 SQL 直连写库**，
而 rpdao **一个写方法都没有** —— 「写入只经 M2」这条硬线只写在文档里，
任何模块 import psycopg 就能绕过契约写库且不报错。

新增 packages/rpdao/write.py：
  - assert_writer 表级写权守卫，三条性质各配契约测试：
    **默认拒绝**（未登记写权的表一律拒写，避免"每加一张表就多一个无人看守的写入口"）、
    **越权报错**（M5 写 wim_axle_record 直接抛 WriteGuardError，这是"模块可拔插"的前提）、
    **空写入报错**（0 行不得当作成功）
  - WriteDao.write_txn / TxnWriter：多表写入保持**同一事务**
  - TABLE_OWNER：21 张表有唯一写权、11 张只读

ingest 重构为走契约③：4 条裸 SQL + 自带连接池全部移除。
Dockerfile/compose 构建上下文抬到 scaffold 根（依赖 packages/rpdao，与 api 同理）。

三、实施中抓到并修掉三个自己埋的错（都不会被"编译通过+单测绿"发现）

  1. DEDUPE_REQUIRED 给 wim_axle_record 填了 ("device_code","pass_time")：
     device_code **根本不是该表的列**（设备信息在 data_source 里），且该表唯一索引
     只有 (id,pass_time)（id 自增，不可能当业务去重键）。生成的 ON CONFLICT 会让 PG
     报 no unique or exclusion constraint matching —— **运行时才炸**。
     已加测试第 6b 组：解析 DDL 断言每个去重键都有唯一索引背书。
  2. 收口若把「主记录＋轴组明细」改成两次 dao.insert，两次各取一条池连接，
     **原子性悄悄消失**，进程中间挂掉会留下没有轴组明细的过车记录，而下游看它完全合法。
     已加 write_txn，并加静态断言锁死。
  3. 批次计数原语义是 `raw_count = raw_count + EXCLUDED.raw_count`（累加），
     通用 upsert 是**替换**，套用会覆盖上一轮累计值。改走 execute_write（仍过守卫）。

  共同点：风险不在"新代码写错"，而在**旧语义没搬全**——收口/迁移时每一步都要问
  "原来的行为里，有哪些是接口签名看不出来的？"（事务边界、累加语义、真实约束……）

四、验证（全部实跑，非推断）

  DDL 应用实库：无 ERROR 且**重复执行无 ERROR（幂等）**；pg_constraint 查到 14 条新约束
  约束行为（临时 schema 内、全部回滚未留痕）：quality_rule 5/5、mapping_set 7/7 路径如设计
  三处表数：DDL 32 ＝ 字典 32 ＝ catalog 32，**逐表集合相同**
  **端到端**（造数器 60 条）：received 60 → inserted 45 / rejected 15 / duplicated 0 / db_errors 0
    wim_axle_record +45、wim_axle_detail +167、data_quality_log +15
  **事务原子性**：孤立主记录（无轴组明细）**0 条**
  违约分类：新增 15 = axle_num_vs_axles 6 + speed_kmh 5 + gross_vs_axle_sum 4，**0 条兜底码**

五、图件与文档同步

  图 A/C/D/E：契约② `DDL v0.1 / 物理 30 表` → `v0.2 / 32 表`；
              契约③ `待定稿` → `已定稿 / 7 域读接口＋表级写权守卫`
  五图自检 0 问题、缺字形 0 类；两项审计（--min-pt 5 / --strict）全 PASS
  注：全文扫描 v0.1 时图 A/C 有命中，但那是 contracts/openapi/m6-gateway.v0.1.yaml
      —— **契约④自己的版本号**，非残留。扫旧口径要扫带上下文的串，不能扫裸版本号。

新增契约测试 2 个（已登记进 run_contract_tests.sh，全量 **通过 9 ｜ 失败 0 ｜ 跳过 0**）：
  - test_ddl_dict_catalog.py 26 断言：三处真源表数一致（逐表比对，数量相同≠集合相同）
  - test_write_guard.py 50 断言：写权守卫应通过/应拒绝两侧 + 去重键↔索引 + M2 不绕契约

---

## 82dbff1 文档：写入收口的使用规矩 + 收口/迁移里最隐蔽的一类坑（旧语义没搬全）

文档：写入收口的使用规矩 + 收口/迁移里最隐蔽的一类坑（旧语义没搬全）

一、SKELETON-GUIDE 新增 §8 §9

§8 「现在写也走 DAO 了」：收口前的问题（ingest 自带连接池+4 条裸 SQL、rpdao 一个写方法都没有，
   硬线只写在文档里）、代码用法、写权对照表位置、三条纪律。

§9 「收口/迁移里最隐蔽的一类坑：旧语义没搬全」——本次抓到三个错，
   **没有一个是"新代码写错了"，全是"原来有、搬的时候丢了"**：
     1. 丢事务边界 → 留下没有轴组明细的过车记录，而下游看它完全合法、不报错
     2. 丢累加语义 → 通用 upsert 是替换，会覆盖批次上一轮的累计计数
     3. 丢真实约束 → 声明的去重键没有唯一索引背书，ON CONFLICT 运行时才炸

   三个都能通过语法编译、都能让单元测试全绿，只在真跑或数据出错时暴露
   （第 1 个甚至不报错，只是数据慢慢变脏）。故做这类工作时每一步都要问：
   **"原来的行为里，有哪些是接口签名看不出来的？"**——改之前必须逐条读完原代码。

§9.1 反向验证：怎么写"不会永远通过"的检查（应通过/应拒绝双向 + 元测试）。

二、MODULE-ONBOARDING 新增 §4.5

新模块若要写库：必须走契约③，不许自带连接池、不许写裸 SQL。
三条规矩 + 「写入只经 M2」的口径说明（业务数据 vs 平台元数据，故按表级写权设计）。

三、分组分模块任务书更新

  - M3 的"补写方法与写权守卫"、M2 的"契约③写入收口"、交付清单第 2/3 项 → 标记 ✅ 已完成
  - 新增 §9「已落地的基础设施（各组直接用，不必自己造）」：6 项能力各写明在哪、怎么用，
    并指向 SKELETON-GUIDE §8–§9 的三个坑

---

## a973065 gitignore：补 pgdata/miniodata/grafanadata 命名卷与 .DS_Store

gitignore：补 pgdata/miniodata/grafanadata 命名卷与 .DS_Store

这三个是 docker-compose 的命名卷目录，本机跑起来后会在仓库根出现。
原先未忽略——一旦有人在根目录直接 docker compose up，数据库文件就会被 git add 进来。
属"防患于未然"：现在它们不存在，但规则必须先立好。

---

## 79b4951 学生上手手册（1363 行）＋ README 新人入口 ＋ 全仓库陈旧口径清查

学生上手手册（1363 行）＋ README 新人入口 ＋ 全仓库陈旧口径清查

一、新增 output/学生上手指南-分模块开发手册.md

面向"刚拿到仓库、要开始写代码"的同学，目标是读完就能独立开工。九个章节：

  §0 三种阅读路径（新手/会 Python/做过后端/组长/只看代码，各自读哪几节）
  §1 全局：先看哪张图、四组十模块、三条数据硬线
  §2 已完成的代码 —— **六个必须读懂的参考实现**，逐个说明"你要模仿什么"
  §3 环境搭建：从零到跑起来，含六个验收检查点、常见错误表、无 Docker 怎么办
  §4 规矩：四份契约、接入五件套、门禁（M5 为何锁着）、三条纪律、反模式清单
  §5 **分模块开工手册**：A/B/C/D 四组 + M7，每个模块都是
     「现状 → 你的任务 → 具体怎么做（分步）→ 怎么测 → 交付物 → 卡住看哪」
  §6 测试体系：九个契约测试各测什么、红了怎么三步定位、
     **怎么写"不会永远通过"的测试**（两侧 + 元测试）
  §7 实验与造数：造数器全部参数、三个必做实验、真实数据摸底六项、校准记录五项
  §8 交付与验收：DoD 八条、提交规范、本轮交付清单
  §9 卡住了怎么办：求助顺序、正确问法、FAQ、术语表
  附录：端口容器对照、文件路径速查、提交信息模板

二、README 加"新人从这里开始"

原来打开仓库第一眼是目录说明，学生不知道该干什么。现在最前面是三条最快路径
（开始写模块 / 先看代码 / 不熟规矩）+ 一分钟离线自检 + 当前重心提示。

三、全仓库陈旧口径清查（改 14 处）

契约②升 v0.2／物理 32 表后，多处说明还写着旧口径。清查原则：

  ✅ 改"学生读到的现行指令/口径"：scaffold/ 下的代码与配置、契约对照表、图件脚本文本
  ❌ 不改"描述当时状态的历史报告"：build_docx_report*.py、docpipe/audit/、各交付报告
     —— 它们记录的是写那份报告时的事实，改写等于篡改历史记录

改动的 9 个文件 14 处包括：
  - M4 的 app.py 与 quality_rules.yaml：不只说表数，更要说清**实质变化**——
    `quality_rule` 表**已经建好了**，口径改为"YAML 是种子、表是权威"
  - M7 的 semantic/app.py：原写"该表尚不在冻结的 DDL 里"，现在**已经落进 v0.2**，措辞已失效
  - rpdao/README、MODULE-ONBOARDING 的表数；图件脚本与契约对照表的口径

四、任务书 §2 的 M2 行

仍写着"下一步：契约③写入收口"，但收口已完成（上一轮提交）。已更新为
"✅ 已完成，转做真实数据摸底接入侧准备 + 设备自管逻辑"。

五、写手册时抓到我自己埋的四处硬错（同一类错误犯了四次）

手册里写"用 localhost:8001/v1/modules/status 看模块状态"——**错了**：
那是 M9(console) 的接口、端口 8024，而 M6(api) 提供的是对象查询/指标/目录/动作提交。
且默认 profile 下 console 根本没起，学生照着敲必然失败。

还把造数器默认端口写成 1883——**实际默认值就是 18883**（骨架栈为避让本机
已占用的 1883 统一改成 18883，造数器默认值已同步）。

复核方法：把手册里所有 `localhost:<端口>/v1/...` 引用抽出来，与各模块 app.py 的
真实路由逐条对照。修完 8 处引用全部对得上。

> 教训：**写文档时凭印象填接口名/端口/默认值，是最高频的出错点**，
> 而且这类错会被学生一行行照抄执行。凡"具体值"必须回代码里核，不能凭记忆。

---

## a0dcd32 仓库瘦身：只留代码与契约（37 MB → 0.47 MB），并修好链路里一处坏挂载

仓库瘦身：只留代码与契约（37 MB → 0.47 MB），并修好链路里一处坏挂载

一、按决定移出 output/ 与 docpipe/

  仓库从 37.1 MB / 219 文件 → **0.47 MB / 83 文件**。
  output/（报告、图件、课题计划文档）与 docpipe/（文档图件流水线、标准规范素材）
  本机文件保留，仅从版本控制移除，并加入 .gitignore 防止再被提交回来。

  理由：它们体积大、含未定稿材料（课题乙）与受版权保护的素材，
  且日常开发根本用不到；而代码本身只有 0.3 MB。

二、但有三个文件**必须**随代码走，已搬进 scaffold/

  判断标准不是"它长得像文档还是代码"，而是**"代码是否依赖它"**：

  | 文件 | 新位置 | 为什么必须留 |
  |---|---|---|
  | DDL 真源 | `scaffold/sql/10_ddl_v0.2.sql` | compose 挂载它建库；两个契约测试读它比对 |
  | 数据字典 | `scaffold/sql/数据字典-v0.2.md` | `test_ddl_dict_catalog.py` 要三方比对 |
  | 学生手册 | 仓库根 `学生上手指南-分模块开发手册.md` | 学生入口 |
  | 任务书 | 仓库根 `分组分模块任务书-P1起.md` | 学生入口 |

  DDL 与数据字典看着像文档，其实是**契约②的真源**——所以它们进 scaffold/。

三、顺手修掉一个一直坏着的挂载（本轮最有价值的发现）

  compose 第 48 行挂的是 `../output/路面性能数据库-DDL-v0.1.sql`，
  而该文件早已改名为 v0.2 —— **这个挂载指向不存在的文件**。
  即：任何人在干净环境 `up -d`，PostgreSQL 都**不会建表**，只会静悄悄跳过。
  搬家后改为 `./sql/10_ddl_v0.2.sql`（与 20_/90_ 编号一致），既修好又本地化。
  已用 `docker compose config` 验证挂载解析到真实路径。

四、路径引用同步（10 个文件）

  scaffold/tests/contract/{test_write_guard,test_ddl_dict_catalog}.py、
  scaffold/{README,SKELETON-GUIDE,MODULE-ONBOARDING}.md、
  scaffold/packages/rpdao/catalog.py、scaffold/docker-compose.skeleton.yml、
  学生手册、scaffold/README 的验收报告引用。
  手册里"图件/设计说明/工单"等仍指向 output/ 的，改为注明"不随代码仓库分发，
  向导师索取"——**不假装它们还在**。

五、验证

  - 契约测试 **9/9 全绿**（搬运中 `m3-写权` 曾因 DDL_PATH 未同步而失败，
    正是靠跑全套测试才发现——见下）
  - `docker compose config` 解析通过，挂载指向真实文件
  - YAML 合法（搬运中我曾把缩进写成 8 空格而实际应为 6，导致 YAML 解析失败；
    用 repr 逐行核对缩进后修正，未凭肉眼判断）
  - 仓库内已无指向 output/ 的活引用

> 教训：**「搬文件」真正的风险不是文件丢了，是引用没跟上。**
> 两个契约测试都硬编码了 output/ 下的路径，其中一个在单独跑时才会暴露。
> 改路径后必须跑**全量**测试，不能只跑改动的那一个。

---

## 0603c9d 模块目录重构：services/ + packages/ → modules/M<n>-<name>/（每模块一目录）

模块目录重构：services/ + packages/ → modules/M<n>-<name>/（每模块一目录）

背景：学生要求"不同模块各占一个文件夹，各自扩展、最后整合"。
原结构目录散在两处（services/ 8 个 + packages/ 1 个），且不带 M 编号，
学生一眼看不出"哪个文件夹是我的"。

新结构（scaffold/modules/，9 个模块目录，M 编号命名）：

  M2-ingest/    M3-rpdao/rpdao/    M4-governance/    M5-fusion/
  M6-api/       M7-semantic/       M8-apps/          M9-console/
  M10-agent/

（M1 是编排层：docker-compose.skeleton.yml + sql/ + ops/ 留在 scaffold 根）

关键决策：
1. rpdao 包嵌入 modules/M3-rpdao/rpdao/ —— 不能直接叫 M3-rpdao：
   Python 按目录名 import，带连字符的目录无法 `import rpdao`，
   所以模块目录内再嵌一层 rpdao/，import 名不变，各服务 `from rpdao...` 无需改。
2. compose build 上下文/路径、两个 Dockerfile 的 COPY、9 个契约测试的
   sys.path 与文件路径、全部文档引用同步更新。
3. 契约内容零改动——纯路径搬移。

验证：
- 契约测试 9/9 全绿（重构中曾因 sys.path 指向旧 packages/、多段路径
  `ROOT / "services" / "ingest" / ...` 未被单段正则命中而失败，逐一修复）
- docker compose config 解析通过，9 个 build 上下文目录全部在位
- import rpdao 在新位置可导入

文档同步：
- 新增 scaffold/MODULES.md：M 编号 ↔ 目录 ↔ 组 ↔ 状态 ↔ 门禁 权威索引
- scaffold/README.md、学生手册附录 B 的目录树重写为 modules/ 结构
- 全部 services/<模块>/ 模板引用改为 modules/<模块>/

---

