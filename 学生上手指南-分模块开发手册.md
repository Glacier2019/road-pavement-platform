# 学生上手指南 · 分模块开发手册

> **读者**：刚拿到这个仓库、要开始写代码的同学。
> **目标**：读完这一份，你能独立把环境跑起来、看懂已有代码、知道自己的模块要做什么、
> 并且知道"做对了"长什么样。
> **配套文档**（本手册不重复它们的内容，只告诉你什么时候去翻）：
> - `scaffold/MODULE-ONBOARDING.md` —— 接入规矩（四份契约、五件套、门禁、反模式）
> - `scaffold/SKELETON-GUIDE.md` —— 骨架栈怎么用、踩过的坑
> - `分组分模块任务书-P1起.md` —— 任务分配总表（谁做什么）
> - `scaffold/sql/数据字典-v0.2.md` —— 32 张表每个字段什么意思
> - 🔍 **架构图 D（总体架构与七域数据流）不在代码仓库里**——图件属交付物，
>   已随 `output/` 移出。向导师索取 `架构图D-总体架构与七域数据流.pdf`，
>   或在项目共享目录里找。**它值得先看**，但这本手册不依赖它也能读完。

---

## 0. 先读这三段

### 0.1 这个项目在做什么

给一段真实公路（**G228 福州滨海大道试验段**）建一套"路面性能数智化平台"：
把路上各种传感器（称重、应变、加速度、温湿度……）的数据收进来，
经过质量治理与融合分析，最终回答工程问题——**这条路现在什么状态、什么时候该修、怎么修**。

代码分成 **10 个模块（M1–M10）**，由 **4 个组**认领，每个模块是**独立可拔插的服务**。

### 0.2 你现在的起点在哪

**别从零开始，也别自己发明结构。** 已经有 4 个模块是完整可跑的（M1/M2/M3/M6），
还有 6 个模块的**骨架已就位**——意思是"机械层全齐（容器、健康检查、配置、契约文件、
契约测试），业务层是空的"。你的工作绝大多数是**在骨架里填业务逻辑**，
而不是搭架子。

> 这条非常重要：**骨架的六个位置都是刻意留好的**。你只要照着已有模块的样子填，
> 就不会出错。手册第 2 节给你 6 个"照着抄"的参考实现。

### 0.3 五种阅读路径（按你的情况选）

| 你的情况 | 读哪几节 | 预计耗时 |
|---|---|---|
| **完全新手**，没碰过 Docker/K8s | §1 → §2 → §3 → §4 → §5（你的模块）→ §6 → §7 | 2–3 天 |
| **会 Python**，没做过服务化 | §1 → §2.2 → §3 → §4 → §5（你的模块）→ §6 | 1 天 |
| **做过后端**，只想快点开工 | §2.2 → §3.4 → §4 → §5（你的模块） | 3–4 小时 |
| **组长**，要排期和卡门禁 | §1 → §4.3（门禁）→ §5（全组）→ §7.3 → §8 | 2 小时 |
| **只想先看代码** | §2.2 六个参考实现，直接点开读 | 1 小时 |

---

## 1. 全局：一张图 + 四组 + 十模块

### 1.1 先看图

向导师索取 `架构图D-总体架构与七域数据流.pdf`，看三样东西：

1. **七域数据流**（左边到右边那条带箭头的链）：GE 几何 → SU 结构 → RE 检测/材料 →
   LO 荷载 → WE 环境 → TE 交通 → DE 病害。数据按这个顺序流动。
2. **四份契约**（图上的 ▲ 标）：①报文格式 ②表结构（DDL）③数据访问层（DAO）④服务接口（OpenAPI）。
   这是模块之间**唯一**的沟通方式。
3. **图例的四级状态**：实心绿＝已落地，虚绿＝部分完成，**灰蓝点线＝骨架已就位**，
   灰实线＝规划中。**你就是去把灰蓝点线变成实心绿的**。

### 1.2 四个组与十个模块

| 组 | 名称 | 主责模块 | 你这轮做什么 |
|---|---|---|---|
| **A** | 数据底座 | **M1** 基础设施、**M3** 数据访问层、**M4** 真数据门 | M1/M3 已落地；**集中做 M4**（全项目唯一的关键缺口） |
| **B** | 感知接入 | **M2** 数据接入与设备自管 | 写入收口已完成；转做**真实数据摸底**的接入侧准备 |
| **C** | 融合诊断 | **M5** 融合辨析引擎 | ⛔ **不得启动**（门禁未开）。先做真实数据摸底 |
| **D** | 应用决策 | **M6** API、**M8** 应用层、**M9** 管理台、**M10**（二期） | 为 M8/M9 准备假数据 demo 的输入契约；协助 A 组做 M4 对外出口 |
| **待指派** | — | **M7** 语义中枢 | ⚠️ 还没人。**有意愿的同学主动申请**，这是块好料 |

**关键路径**（决定项目能不能按时交付的链条）：

```
M1 → M3 → M4 → M5 → M6 → M8
```

**可并行**（不互相挡路）：`M2 ‖ M3`、`M7 ‖ M8`

### 1.3 三条数据硬线（违反了要返工）

1. **写入只经 M2** —— 七域业务数据只有 M2 能写。这不是口头约定，**代码里强制**（见 §4.5）。
2. **读只经 M3（rpdao）** —— 谁都不许自己连数据库写 SQL 取数。
3. **跨模块只认契约** —— 模块之间不互相 import 代码，只通过契约（报文/表/DAO/接口）交互。
   这条是"模块能拔插"的前提。

---

## 2. 已经完成的代码：你的起点与范本

### 2.1 现状总览

| 模块 | 状态 | 已有 | 缺什么 |
|---|---|---|---|
| **M1** 基础设施与编排 | ✅ 已落地 | Docker 编排 6 服务、三库（PG/MQTT/MinIO）、Grafana | **IoTDB 未接入** |
| **M2** 数据接入与设备自管 | ✅ 已落地 | MQTT 订阅→契约校验→落库/拒收 完整竖切；写入走契约③ | 真实数据的接入侧准备 |
| **M3** 对象域数据访问层 | ✅ 已落地 | `rpdao` 包：7 域读接口、写权守卫、单元自查 | —（本模块已完成） |
| **M4** 数据治理·真数据门 | 🟡 骨架已就位 | 容器、健康检查、11 条规则 YAML（仅 2 条已标定） | ★ **阈值标定 + 真值晋升真正跑通** |
| **M5** 融合辨析引擎 | 🟡 骨架已就位 | 容器、诊断契约 schema | ⛔ 业务逻辑（**等 M4**） |
| **M6** API 服务 | ✅ 已落地 | 7 域读路由、`/openapi.json`、模块状态看板 | 动作层（501 占位）、质量门状态出口 |
| **M7** 语义中枢 | 🟡 骨架已就位 | 容器、`mapping_set` schema + 数据表 | 全部业务逻辑（**待指派**） |
| **M8** 应用层 | 🟡 骨架已就位 | 容器、养护方案契约 | 假数据 demo |
| **M9** 平台管理台 | 🟡 骨架已就位 | 容器、模块注册表契约；**自己的** `/v1/modules`、`/v1/modules/status` 已通 | 看板界面 |
| **M10** Agent 执行引擎 | 🟡 骨架已就位 | 容器、工单契约 | 二期，暂不动 |

### 2.2 六个必须读懂的参考实现 ★

**这是本手册最重要的一节。** 下面 6 个文件是"写对了长什么样"的活样本。
不要只扫一遍——**打开读，边读边对照后面的"你要模仿什么"**。

#### 参考 1：`scaffold/modules/M3-rpdao/catalog.py` —— 表清单与写权

**它做什么**：平台 32 张表的**唯一登记处**。`ALL_TABLES` 是全部表名，
`TABLE_OWNER` 记录每张表**唯一允许哪个模块写**，`selfcheck()` 会自查表数与实际库是否一致。

**你要模仿什么**：
- **"默认拒绝"的思想**：`TABLE_OWNER` 里没有的表＝只读表，谁都写不进去。
  这比"默认允许、出事再说"安全得多。
- **拒绝"硬编码数字"**：`EXPECTED_PHYSICAL_TABLES = 32` 不是随便写的常量——
  契约测试会拿它和 DDL 文件、数据字典三个地方对着比，任何一处单独改动都会失败。

#### 参考 2：`scaffold/modules/M3-rpdao/pool.py` + `repo.py` —— 读接口

**它做什么**：连接池管理 + 7 个数据域的读方法（`GE`/`SU`/`RE`/`LO`/`WE`/`TE`/`DE`）。

**你要模仿什么**：
- **模块不自己连数据库**。M6（api）用了 `rpdao` 之后，**自己一行连接代码都没有**。
  你的模块也一样：要读数据就 `from rpdao import Dao`。
- **跨域拦截**：`repo.py` 里每个域只能读自己域的表。这是"模块可拔插"的具体实现。

#### 参考 3：`scaffold/modules/M3-rpdao/write.py` —— 写权守卫 ★★★

**它做什么**：把"写入只经 M2"从**文档约定**变成**代码强制**。

**为什么必读**：这个文件里有三个真实踩过的坑，注释写得很详细：

| 坑 | 教训 |
|---|---|
| 给 `wim_axle_record` 声明了不存在的去重键 `(device_code, pass_time)` | 该表**根本没有这两个列的组合唯一索引**，`ON CONFLICT` 会让 PostgreSQL **运行时才报错** |
| 多表写入若不用 `write_txn` | 两次 `insert` 各取一条池连接，**原子性悄悄消失**——会留下没有明细的过车记录 |
| 批次计数套用通用 `upsert` | 原语义是**累加**，通用接口是**替换**，会覆盖历史累计值 |

**你要模仿什么**：
- **报错要指出"该修哪一处"**。看 `assert_writer` 的三层判断顺序
  （先判表是否存在 → 再判是否只读 → 最后判越权），每层的报错信息都不一样。
- **注释要写"为什么"，不是"是什么"**。函数名已经说了做什么，注释的价值在于
  记录"为什么不能那样写"。

#### 参考 4：`scaffold/modules/M2-ingest/app.py` —— 一个完整竖切 ★★★

**它做什么**：M2 的全流程。MQTT 收到报文 → Pydantic 校验 → 合格落库 / 不合格进拒收区。

**你要模仿什么**：
- **一条竖切比十个空架子值钱**。这个模块做的事不多（就三件：收、验、落），
  但从 MQTT 到数据库全通了。**你的模块也应该先打通一条最窄的竖切**，再扩展。
- **健康检查要逐项报依赖**：看 `/healthz` 的返回——它分别报 postgres 和 mqtt 的状态，
  而不是笼统返回一个"ok"。
- **拒收路径和正常路径一样重要**：不合格的数据不是丢掉，而是写进 `data_quality_log`
  并带上**精确的规则码**。

#### 参考 5：`scaffold/modules/M2-ingest/violations.py` —— 错误分类 ★★

**它做什么**：把校验失败分类成**精确的规则码**（如 `range.speed_kmh`、`cross.axle_num_vs_axles`）。

**为什么必读**：这里修过一个很典型的 bug——原先所有违约都记成同一个码
`contract_violation`，导致 **26 条违约里有 9 条其实是别的类型、根本分不出来**。

**你要模仿什么**：
> **不要把不同的错误归成一类。** 一个"什么都记"的错误码，等于没有错误码。
> 你的模块报错时，要问自己："看到这个码，我能知道该去改什么吗？"

#### 参考 6：`scaffold/modules/M6-api/app.py` —— 只读出口 + 抗空虚设计

**它做什么**：M6 对外提供 7 域读接口。动作类接口（还没实现的）返回 **501 + 责任人**。

**你要模仿什么**：
- **未实现的东西要显式报错，不能返回空**。看它怎么处理"数据还没来"的情况：
  不是返回空列表让调用方以为"这条路没病害"，而是返回 503/501 说明原因。
- 这条原则的原话是：**「一个永远不会失败的检查，比没有检查更糟。」**
  同理：一个永远返回空结果的接口，比报错的接口更糟——因为调用方会拿它当结论。

### 2.3 每个骨架服务都有的通用长相

打开任意一个 `scaffold/modules/<模块>/app.py`，你都会看到这四个端点：

| 端点 | 作用 | 为什么必须有 |
|---|---|---|
| `GET /healthz` | **逐项**报依赖状态 | 容器编排靠它判断健康；笼统的 ok 掩盖问题 |
| `GET /metrics` | Prometheus 文本指标 | Grafana 看板的数据源 |
| `GET /docs` | 自动生成的交互式 API 文档 | 你调自己接口的工具 |
| `GET /openapi.json` | 机器可读接口契约 | 上游模块/前端据此对接 |

**加上**：`Dockerfile`（含 `HEALTHCHECK`）、`config/`（配置全外置）、
`tests/contract/test_<模块>.py`（离线契约测试）、`contracts/`（契约文件）。

这四样＋一样 ＝ **接入五件套**，见 §4.2。

### 2.4 契约与测试资产

**契约文件**（`scaffold/contracts/`）：

```
topics.yaml                              # 契约① MQTT 主题与报文格式总表
messages/wim_axle.v1.schema.json         # 契约① 称重报文 schema
governance/quality_rule.v0.1.schema.json # M4 质量规则
governance/promotion.v0.1.schema.json    # M4 真值晋升
fusion/diagnosis.v0.1.schema.json        # M5 诊断结论
semantic/mapping_set.v0.1.schema.json    # M7 语义映射集
apps/maintenance_plan.v0.1.schema.json   # M8 养护方案
console/module_registry.v0.1.schema.json # M9 模块注册表
agent/action_ticket.v0.1.schema.json     # M10 工单
openapi/m6-gateway.v0.1.yaml             # 契约④ M6 服务接口
```

**表结构契约**：`scaffold/sql/10_ddl_v0.2.sql`（32 表，契约②真源）
**字段说明**：`scaffold/sql/数据字典-v0.2.md`

**9 个契约测试**（`scaffold/tests/contract/`）——每个测什么见 §6.1。

---

## 3. 环境搭建：从零到跑起来

### 3.1 你要装什么

| 工具 | 版本 | 怎么装 | 验证 |
|---|---|---|---|
| **Docker** | 24+ | 官网装 Docker Desktop（Win/Mac）或 `apt install docker.io docker-compose-v2` | `docker --version` |
| **Docker Compose** | v2 | 随 Docker Desktop 一起；Linux 单独装 compose 插件 | `docker compose version` |
| **Python** | 3.11+ | 建议用 `uv` 管理（见下） | `python3 --version` |
| **uv** | 最新 | `curl -LsSf https://astral.sh/uv/install.sh \| sh` | `uv --version` |
| **git** | 任意 | 系统自带或官网 | `git --version` |

> **为什么要 uv**：仓库里所有 Python 脚本都用 `uv run --with xxx script.py` 的方式跑，
> 它会自动下载所需依赖到一个临时环境，**不污染你的系统 Python**。你也可以用 pip/venv，
> 但那样要自己维护依赖，容易和同学的环境不一致。

### 3.2 拉代码

```bash
git clone https://github.com/Glacier2019/road-pavement-platform.git
cd road-pavement-platform
```

> 国内网络慢的话，可以走镜像：
> `git clone https://ghfast.top/https://github.com/Glacier2019/road-pavement-platform.git`

**先确认你的身份**（提交记录要认得出是谁写的，这是成绩依据之一）：

```bash
git config user.name  "你的姓名"
git config user.email "你的邮箱"
```

### 3.3 起骨架栈

```bash
cd scaffold
cp .env.example .env          # 生成配置（.env 不入库，每人一份）
docker compose -f docker-compose.skeleton.yml up -d
```

**第一条命令在做什么**：`.env` 里是各服务的端口和口令。本机 5432/1883/3000/8000
这些常用端口可能已被占用，所以仓库统一避让成了 **55432/18883/3001/8001**。
`.env` 被 git 忽略，所以你可以随便改而不影响别人。

**等 1–2 分钟**（第一次要拉镜像），然后看状态：

```bash
docker compose -f docker-compose.skeleton.yml ps
```

### 3.4 验收：六个检查点（全部通过才算起对了）

默认 `up -d` **只起 6 个基线服务**（pg / mqtt / minio / grafana / ingest / api）。
其余 6 个模块在 **profile** 后面（m4/m5/m7/m8/m9/m10），要显式指定才会起。
这是刻意的：**门禁没开的模块不该跑起来**（见 §4.3）。

```bash
# ① 六个容器都应是 healthy（不是 Up）
docker compose -f docker-compose.skeleton.yml ps
#    预期：rp-pg, rp-mqtt, rp-minio, rp-grafana, rp-ingest, rp-api 全部 (healthy)
#    ⚠ 如果显示 Up 而不是 healthy，说明 healthcheck 没通过，去看日志

# ② 数据库 32 张表已建好
docker exec rp-pg psql -U rp -d road_pavement -c "\dt" | wc -l
#    预期：34 左右（32 张业务表 + 表头行 + 分隔行）

# ③ M2 接入服务的健康检查（会逐项报依赖）
curl -s localhost:8010/healthz | python3 -m json.tool
#    预期：status=ok，deps 里 postgres/mqtt 都 ok，devices 显示已加载设备数

# ④ M6 API 服务的接口契约
curl -s localhost:8001/openapi.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(len(d['paths']), '个路径')"
#    预期：打印出路径数量（>0）

# ⑤ M6 的对象查询与目录（这才是 M6 真实拥有的接口）
curl -s localhost:8001/v1/catalog | python3 -m json.tool | head -20
curl -s "localhost:8001/v1/objects/wim_axle?limit=3" | python3 -m json.tool | head -20
#    ⚠ 注意：模块状态看板 /v1/modules/status 属于 M9(console)，端口 8024，
#      不在默认 profile 里。要看它得先 --profile m9 up -d console。

# ⑥ MQTT 控制台能打开
#    浏览器访问 http://localhost:18083   账号 admin / 密码 public
```

### 3.5 常见错误与解法

| 现象 | 原因 | 解法 |
|---|---|---|
| 容器是 `Up` 不是 `healthy` | healthcheck 未通过 | `docker logs rp-<名字> --tail 30` 看报错 |
| `port is already allocated` | 端口被占 | 改 `.env` 里对应端口，重新 `up -d` |
| `rp-ingest` 起来就退出 | 连不上 PG 或 MQTT | 看日志；确认 `depends_on` 的两个服务已 healthy |
| 数据库里表不全 | DDL 没跑 | 见 §5 的 M1 模块，`docker exec -i rp-pg psql ... < DDL` |
| 拉镜像卡住 | 网络 | 配 Docker 镜像加速器 |
| `curl: connection refused` | 服务还没起完 | 等 30 秒再试；`ps` 确认状态 |

### 3.6 没有 Docker 怎么办（重要）

**契约测试不需要 Docker、不需要数据库、不需要网络**，可以离线跑：

```bash
cd scaffold
./run_contract_tests.sh            # 跑全部
./run_contract_tests.sh m4         # 只跑 M4 相关的
./run_contract_tests.sh --list     # 列出所有可跑的契约测试
```

所以你**在只有 Python 的机器上也能干活**——写代码、跑契约测试、提交。
只是不能做端到端联调（那需要 Docker）。

> **这条对你们最重要**：交作业前的自检就靠这个脚本，它绿了才提交。

---

## 4. 规矩：不读懂会返工

### 4.1 四份契约，各自的唯一真源

模块之间只通过契约交互。**每份契约只有一个真源文件，改别处一律无效**：

| # | 契约 | 真源文件 | 承诺方 | 谁在用 |
|---|---|---|---|---|
| **①** | 报文格式 | `scaffold/contracts/topics.yaml` + `messages/*.schema.json` | **B 组** | M2 收发报文 |
| **②** | 表结构 | `scaffold/sql/10_ddl_v0.2.sql` | **A 组** | 所有模块 |
| **③** | 数据出口（DAO） | `scaffold/modules/M3-rpdao/` | **A 组** | 所有模块读写数据 |
| **④** | 服务接口 | `scaffold/modules/*/openapi.json`（自动生成） | **D 组** | 前端/上游模块 |

**表结构还有一份人读版**：`scaffold/sql/数据字典-v0.2.md`。
DDL 和字典**必须同时改**——有契约测试盯着（见 §6.1 第 8 项）。

### 4.2 接入五件套（新模块必须有这五样）

你的模块要能被别人用，必须齐这五样。**骨架里都已经留好位置了，照着填即可**：

| # | 名称 | 位置 | 验收标准 |
|---|---|---|---|
| 1 | **容器定义** | `modules/<模块>/Dockerfile` | 含 `HEALTHCHECK` 指令 |
| 2 | **健康与指标** | `app.py` 里的 `/healthz`、`/metrics` | `/healthz` **逐项**报依赖，不是笼统 ok |
| 3 | **配置全外置** | `modules/<模块>/config/` | 代码里不出现硬编码的地址/口令/阈值 |
| 4 | **契约文件** | `contracts/<模块>/*.schema.json` | 有 schema，且被契约测试校验 |
| 5 | **契约测试** | `tests/contract/test_<模块>.py` | **离线**可跑，覆盖**应通过**和**应拒绝**两侧 |

> 第 5 条的"两侧"最容易漏：只测"合格的能过"是不够的，
> **必须也测"不合格的会被拒"**——否则你无法证明拦截逻辑真的存在。见 §6.4。

### 4.3 门禁：谁必须等谁

**规则**：*下游不启动，直到上游的两个契约测试都绿。*

```
M1 ──→ M3 ──→ M4 ──→ M5 ──→ M6 ──→ M8
                    ⛔ C 组现在绝对不能动 M5
```

| 模块 | 门禁条件 | 现在的状态 |
|---|---|---|
| M3 | M1 的表建好 | ✅ 已解锁 |
| M4 | M3 的读接口稳 | ✅ 已解锁 |
| **M5** | **M4 的质量门绿** | ⛔ **未解锁——C 组请勿写业务逻辑** |
| M6 | M4 的质量门状态可读 | 🟡 部分解锁（读接口已通，质量门出口待接） |
| M8 | M6 的接口契约稳定 | 🟡 可以做假数据 demo |

**为什么 M5 必须等**：M5 是"融合辨析"，它的输入是 M4 质量门**筛过的可信数据**。
如果 M4 没上线，M5 就是拿脏数据算结论——**算出来的东西没法验证，等于白做**。
C 组这段时间的任务是真实数据摸底（见 §7.3），不是写 M5 的代码。

### 4.4 三条纪律（违反会返工）

#### 纪律 1：未标定的东西不许出结论

代码里所有的阈值、系数、权重，**没有用真实数据标定过之前，不许用来下结论**。

配置里用 `calibrated: true/false` 标记。当前 11 条质量规则里**只有 2 条**标了 true
（两条是结构性规则，不需要真实数据）。其余 9 条是统计阈值，**必须等真实数据**。

> 这不是形式主义。一个未标定的阈值配上"看起来合理"的输出，
> 会让整条链路上所有人都以为结论可信——**这比直接报错危险得多**。

#### 纪律 2：不要拿合成数据当验证依据

仓库里有造数器（`scaffold/simulator/wim_simulator.py`），它生成的过车数据是**合成**的。
它可以用来：验证链路通不通、验证拒收路径、验证接口对不对。
它**不能**用来：标定阈值、证明模型准、写进论文当实验结果。

> 一句话：**合成数据能验证"管道不漏"，不能验证"算法对"。**

#### 纪律 3：契约先行，改契约走工单

**学生不参与设计决策**，只做"实现、验证、造数、写文档"。
如果你写着发现契约有问题（字段不够、类型不对、少一张表），**不要自己改**，
走工单（模板见 `scaffold/MODULE-ONBOARDING.md` §5），由导师把关。

> 这不是限制你们，是保护你们：契约一改，下游所有组都要返工。
> 一个组私自改契约，代价是四个组重做。

### 4.5 写库规矩（契约③ v0.2 起）

**要写数据？必须走 DAO，不许自己连数据库、不许写裸 SQL。**

```python
from rpdao.write import WriteDao

dao = WriteDao(PG_DSN, app_name="rp-<你的模块>")
dao.open()

# 单表写：必须声明你是谁
dao.insert("你的表", [{...}], writer="M<你的模块号>")

# 多表写：必须用 write_txn（否则原子性会丢）
with dao.write_txn(writer="M<模块号>") as tx:
    rid = tx.insert_returning("主表", {...})
    tx.insert("明细表", [...], on_conflict=("主表id", "序号"))
```

三条规矩：

1. **默认拒绝**：`catalog.TABLE_OWNER` 里没登记写权的表，**任何人都写不进去**。
   你的表不在你名下 → **开工单**，不要绕过。
2. **越权报错**：写别人的表会抛 `WriteGuardError`，**不是静默成功**。
3. **多表写必须 `write_txn`**：否则两次 `insert` 各取一条池连接，原子性就没了。

> 详细用法与三个踩过的坑：`scaffold/SKELETON-GUIDE.md` §8–§9。**动手写库前必读。**

### 4.6 反模式清单（出现即打回）

| 反模式 | 为什么不行 | 正确做法 |
|---|---|---|
| 自己 `import psycopg` 连库 | 绕过契约③，写权守卫失效 | 用 `rpdao` |
| 写裸 SQL 改数据 | 同上 | 用 `dao.insert` / `execute_write` |
| 模块之间互相 `import` | 破坏"可拔插"，改一个坏一片 | 通过契约交互 |
| 硬编码地址/口令/阈值 | 换环境就炸，阈值改动无法审计 | 放 `config/` |
| `/healthz` 只返回 `{"ok":true}` | 掩盖真实依赖状态 | **逐项**报依赖 |
| 未实现返回空列表/空对象 | 调用方会把它当结论（"没病害"） | 返回 501/503 + 原因 |
| 所有错误记同一个码 | 无法定位问题（曾致 9/26 条误记） | 分类到精确规则码 |
| 只测"正确输入能通过" | 无法证明拦截逻辑存在 | 加"错误输入必须被拒" |
| 拿合成数据当实验结果 | 结论不可信 | 合成数据只验证管道 |

---

## 5. 分模块开工手册

**怎么用这一节**：找到你的组、你的模块，照着做。每个模块都是同一个结构：

> **现状** → **你的任务** → **具体怎么做（分步）** → **怎么测** → **交付物** → **卡住看哪**

---

## A 组 · 数据底座（M1 / M3 / M4）

你们的模块是**所有人的地基**。M1/M3 已经完成，**M4 是整个项目唯一的关键缺口**。

### A-1 · M1 平台基础设施与编排

#### 现状

✅ **已落地**。`scaffold/docker-compose.skeleton.yml` 编排了全部 10 个模块 + 3 个中间件
（PostgreSQL / EMQX MQTT / MinIO 对象存储）+ Grafana 监控。数据库 32 张表已建好。

#### 你的任务

**唯一缺口：IoTDB 未接入。**

设计里是"三库分工"：PostgreSQL 存结构化业务数据、**IoTDB 存时序传感器数据**、
MinIO 存文件（视频/图片/点云）。现在 IoTDB **完全不在 compose 里**。

#### 具体怎么做

1. **先搞清楚为什么需要它**
   - 三库分工的设计说明**不在代码仓库里**（属交付文档），向导师索取
     `架构与模块说明（6服务·模块·7域数据流）.md`，找到三库分工那一节
   - 问自己：`wim_axle_record` 是结构化数据（每车一条、字段固定），放 PG 合理；
     但加速度、应变这类**高频时序**数据（每秒几十到几千个点）放 PG 会怎样？
   - 把答案写进你的笔记——这是你理解"为什么"的证明

2. **在 compose 里加 IoTDB 服务**
   - 参考现有 `pg` / `mqtt` 服务的写法（`docker-compose.skeleton.yml` 第 30–106 行）
   - 必须包含：`image`、`container_name`（命名规范 `rp-<角色>`）、端口映射（避让常用端口）、
     `healthcheck`、`volumes`（数据持久化）、`logging: *default-logging`（复用日志配置）
   - 端口建议 **6667**（IoTDB 默认），本机映射到 **56667** 之类避让

3. **写一个最小验证脚本**
   - `uv run --with apache-iotdb ...` 连上去，建一个时间序列，写一个点，读回来
   - 把它放进 `scaffold/ops/` 目录（参考 `ops/grafana/` 的组织方式）

#### 怎么测

```bash
docker compose -f docker-compose.skeleton.yml up -d
docker compose -f docker-compose.skeleton.yml ps | grep iotdb      # 应 healthy
docker exec rp-iotdb ... # 你的验证命令
```

同时**必须保证原有 6 个服务仍然 healthy**——加服务不能弄坏已有的。

#### 交付物

- [ ] compose 里的 IoTDB 服务定义（含 healthcheck）
- [ ] 连接验证脚本 + 它的运行结果
- [ ] `scaffold/ops/` 下的一份简短说明（怎么连、端口多少、数据存哪）

#### 卡住看哪

`scaffold/SKELETON-GUIDE.md` §5「要新建一个模块怎么办」；现有 pg 服务的写法。

---

### A-2 · M3 对象域数据访问层（`rpdao`）

#### 现状

✅ **已完成**（包括 2026-09-15 的写入收口）。这个模块**不需要你写新功能**，
但它是**你最该读懂的参考实现**。

#### 你的任务

**不是写代码，是当"守门人"**：

1. 当别人的表需要加写权时，你负责改 `catalog.py` 的 `TABLE_OWNER` 并补契约测试
2. 当 DDL 有变更时，你负责同步 `catalog.py` 并确保三处表数一致
3. 你是**契约③的承诺方**——别人用 `rpdao` 遇到问题先找你

#### 具体怎么做

**先完整读一遍这三个文件**（这是你的职责范围）：

| 文件 | 读什么 |
|---|---|
| `modules/M3-rpdao/catalog.py` | `ALL_TABLES` 怎么来的、`TABLE_OWNER` 怎么定的、`selfcheck()` 查什么 |
| `modules/M3-rpdao/repo.py` | 7 个域的读方法、跨域拦截怎么实现的 |
| `modules/M3-rpdao/write.py` | 写权守卫的三层判断、`write_txn` 为什么必须存在 |

**然后做一次"验证性复述"**：用自己的话写一份 200 字的说明，回答
"如果 M5 想写 `wim_axle_record`，会发生什么？在哪一行报错？报什么错？"
交给导师——这证明你真的读懂了，而不是扫过。

#### 怎么测

```bash
cd scaffold
python3 tests/contract/test_write_guard.py          # 50 断言，应全过
python3 tests/contract/test_ddl_dict_catalog.py     # 26 断言，应全过
python3 tests/contract/test_dao_contract.py         # 7 域读接口
```

#### 交付物

- [ ] 一份 200 字的验证性复述（上面那个问题）
- [ ] 后续的写权变更（如果有）

---

### A-3 · M4 数据治理·真数据门 ★★★ 全项目关键路径

#### 现状

🟡 **骨架已就位**，而且骨架质量很高：

| 已有 | 位置 |
|---|---|
| 4 个真端点 | `modules/M4-governance/app.py`：`/v1/rules`（列规则）、`/v1/batches/{batch_no}/validate`（校验批次）、`/v1/promotions`（真值晋升）、`/v1/batches/{batch_no}/report`（批次报告） |
| 11 条质量规则 | `modules/M4-governance/config/quality_rules.yaml`（分"缺失/越界/离群/跨字段"四类） |
| 2 个契约 schema | `contracts/governance/quality_rule.v0.1.schema.json`、`promotion.v0.1.schema.json` |
| 2 张数据表 | DDL v0.2 的 `quality_rule`（规则库）、沿用 `data_quality_log` |

**关键现实**（必须先接受这个事实再动手）：

- `data_import_batch.truth_flag` **全为 false** → **真值晋升从未发生过**
- 11 条规则里**只有 2 条**标了 `calibrated: true`（两条结构性规则，不需要真实数据）
- 其余 9 条是统计阈值，**必须用真实数据标定**
- 门禁关闭时接口**返回 503**（刻意设计——宁可报错也不给假通过）

#### 你的任务

**P1 的验收条件就是「真数据门上线」。这是全项目最重要的一件事。**

分两阶段，**第一阶段不写代码**：

**阶段一：拿到真实数据，做标定**（见 §7.3）
**阶段二：把标定结果落库，让 `truth_flag` 真正晋升**

#### 具体怎么做

**阶段一：标定（不写代码）**

1. **拿到真实数据**——这是 A 组的第一个动作，不是写代码。
   找导师确认数据来源（真实 WIM 过车数据 / 检测数据）。§7.3 有完整的摸底清单。

2. **对每条规则统计触发率**。用真实数据跑一遍，对 11 条规则逐条回答：
   - 触发率是多少？
   - **触发率 0% 的规则**：是阈值定错了，还是这批数据根本没覆盖这种情况？**必须查清是哪种**——
     这两种情况的处理方式完全不同（前者改阈值，后者要么等数据、要么承认规则未验证）。

3. **逐条标定并写清依据**。在 `quality_rules.yaml` 里把规则改成 `calibrated: true`，
   并在 `remark` 里写明**依据**（用了多少条数据、什么时间段的、触发率多少、
   为什么定这个阈值）。**没有依据的 true 等于 false。**

**阶段二：落库与晋升**

4. **把标定结果同步进 `quality_rule` 表**（DDL v0.2 G5）。
   口径是：**YAML 是种子，表是权威**——即先有 YAML 定义，标定后把状态写进表；
   运行时以表为准（因为表可以审计"谁在什么时候标的"）。

5. **跑通真值晋升**：`POST /v1/promotions` 让 `data_import_batch.truth_flag`
   真正从 false 变 true。**这是 M4 上线的标志。**

6. **把质量门状态接到 M6**：让 `/openapi.json` 能反映
   "质量门是开着还是关着"——下游要靠这个决定能不能算结论。

#### 怎么测

```bash
cd scaffold
./run_contract_tests.sh m4                    # M4 的契约测试

# 起 M4 服务（注意 profile）
docker compose -f docker-compose.skeleton.yml --profile m4 up -d governance
curl -s localhost:8020/healthz | python3 -m json.tool
curl -s localhost:8020/v1/rules | python3 -m json.tool | head -20

# 标定前：校验接口应返回 503（这是对的，不是 bug）
curl -s -o /dev/null -w "%{http_code}\n" -X POST localhost:8020/v1/batches/<批次号>/validate
# 标定后：应返回 200 + 真实结果
```

**三条必做的实验**：

| 实验 | 做什么 | 验证什么 |
|---|---|---|
| ① 门禁关闭时 | 标定前调 validate | 返回 **503**，不是"通过" |
| ② 注入已知坏数据 | 用手工构造的越界数据跑校验 | 能被检出，且规则码精确 |
| ③ 晋升全链路 | 跑 promotions 后查库 | `truth_flag` 真的变了 |

#### 交付物

- [ ] 11 条规则的**标定报告**（每条：触发率、依据、最终阈值、是否 calibrated）
- [ ] 更新后的 `quality_rules.yaml`（含 `calibrated: true` 和依据）
- [ ] `quality_rule` 表里的标定记录（`calibrated_by`/`calibrated_at`/`evidence` 都有值）
- [ ] 真值晋升跑通的证据（晋升前后的 `truth_flag` 查询结果）
- [ ] M6 能读到质量门状态的证据

#### 卡住看哪

`modules/M4-governance/config/quality_rules.yaml` 顶部的注释（写得很详细，是给你看的）；
`modules/M4-governance/README.md`；`scaffold/sql/10_ddl_v0.2.sql` 的 G5 段
（`quality_rule` 表设计，含 `ck_qr_code_layer` 等约束）。

---

## B 组 · 感知接入（M2）

### 现状

✅ **已落地**。M2 是**目前唯一打通了完整竖切**的模块，也是你写自己模块时最该抄的样本。

已有：

| 资产 | 位置 | 说明 |
|---|---|---|
| 完整竖切 | `modules/M2-ingest/app.py` | MQTT 订阅 → Pydantic 校验 → 落库 / 拒收 |
| 报文模型 | `modules/M2-ingest/models.py` | 与 schema 双实现（`test_wim_contract.py` 校验两者一致） |
| 违约分类器 | `modules/M2-ingest/violations.py` | 精确规则码（修过"全都记成一个码"的 bug） |
| 造数器 | `simulator/wim_simulator.py` | 真设备到场前驱动整条链路 |
| 写入收口 | 已完成 | 4 条裸 SQL + 自带连接池已移除，走契约③ |

### 你的任务（P1 收尾）

1. **真实数据的接入侧准备**——配合 A 组摸底，准备好接真实设备/真实数据文件
2. **设备自管**：`sensor_install` / `sensor_channel` 两张表你已有写权，
   补齐"设备注册 → 通道配置 → 上线状态"的管理逻辑
3. **盯住契约①**：你是契约①的承诺方，报文格式有问题先找你

### 具体怎么做

1. **先读懂现有竖切**（参考实现 4 和 5，§2.2）。重点看：
   - 报文校验失败后**不是丢弃**，而是写 `data_quality_log` 并带**精确规则码**
   - 设备未注册时**立即重载一次设备映射**（新设备接入场景）——这是实战细节
   - `/healthz` 分别报 postgres 和 mqtt

2. **真实数据接入准备**。问清楚三件事：
   - 真实数据从哪来？（MQTT 推送 / 文件导入 / 数据库对接）
   - 如果是文件：格式是什么？多久一批？历史数据要不要回灌？
   - 真实设备的 `device_code` 命名规则是什么？（现在造数器用的是 `WIM01`）

3. **把接入侧的未知项列成清单**交给导师。**不要猜**——猜错格式的代价是整条链路返工。

### 怎么测

```bash
cd scaffold

# 契约测试（离线）
./run_contract_tests.sh m2

# 干跑造数器：只看报文不发（不需要 broker，最快验证）
uv run --with paho-mqtt simulator/wim_simulator.py --dry-run --count 3

# 端到端：发 60 条，25% 故意违约
uv run --with paho-mqtt --with pydantic simulator/wim_simulator.py \
    --count 60 --invalid-rate 0.25

# 看结果：应"收 60 → 落 45 / 拒 15"
curl -s localhost:8010/stats | python3 -m json.tool

# 查库确认（把 <N> 换成残差）
docker exec rp-pg psql -U rp -d road_pavement -c \
  "SELECT issue_code, count(*) FROM data_quality_log GROUP BY issue_code ORDER BY 2 DESC;"
#   预期：出现精确的规则码（如 range.speed_kmh / cross.axle_num_vs_axles），
#        不应有兜底码 contract_violation
```

### 交付物

- [ ] 接入侧未知项清单（真实数据来源/格式/频率/历史回灌需求）
- [ ] 设备自管逻辑（注册、通道配置、状态）
- [ ] 契约①如有变更，走工单

### 卡住看哪

`scaffold/SKELETON-GUIDE.md` §6（一个"以错误理由被拒"的隐蔽 bug 案例，值得当案例读）；
`modules/M2-ingest/violations.py` 的注释。

---

## C 组 · 融合诊断（M5）

### ⛔ 先说最重要的一句：**P1 阶段不得启动 M5**

M5 的门禁是 **M4 的质量门绿**。M4 还没上线，所以 M5 现在是**锁着的**。

**为什么**：M5 是"融合辨析"——它的输入是 M4 质量门**筛过的可信数据**。
如果 M4 没上线，M5 就是拿脏数据算结论，**算出来的东西无法验证，等于白做**。

**这不意味着 C 组闲着。** 你们这段时间做**真实数据摸底**——这件事只有一次机会
（真实数据第一次到手时最值得仔细看，之后大家就习惯了）。见 §7.3。

### 现状

🟡 骨架已就位：`modules/M5-fusion/app.py`（容器、健康检查、`/metrics`）、
`contracts/fusion/diagnosis.v0.1.schema.json`（诊断结论契约）、`config/`。

### 你的任务（P1 收尾）

**做真实数据摸底，产出《真实数据摸底报告》。不要写 M5 的业务逻辑。**

### 具体怎么做

按 §7.3 的 6 项清单逐项做。你的报告要回答的是**"这批数据能支撑什么样的诊断结论"**，
而不是"数据长什么样"。

**特别注意这三件事**（它们决定 M5 能不能做）：

1. **数据覆盖度**：7 个域里，真实数据实际覆盖了几个？
   （现状是：7 个域里只有 LO 域曾经跑通过数据流——这意味着 M5 想做"融合"，
   可能只有 1 个域的输入。**这个发现本身比写代码重要。**）

2. **时间对齐**：不同域的采集频率一样吗？M5 要"融合"，就必须把不同频率的数据
   对齐到同一时间轴上。**先搞清楚能不能对齐、误差多大。**

3. **标定依赖**：M5 的诊断模型需要哪些系数？这些系数能不能从这批数据标定出来？
   标不出来的要列清楚——**列出来不丢人，假装能标定才是问题。**

### 交付物

- [ ] 《真实数据摸底报告》（按 §7.3 的 6 项）
- [ ] **不要**提交 M5 的业务代码
- [ ] 一份"数据能支撑什么/不能支撑什么"的诚实结论

---

## D 组 · 应用决策（M6 / M8 / M9 / M10）

### D-1 · M6 API 服务与指标语义层

#### 现状

✅ **已落地**。M6 真实拥有的接口（`grep '@app.get' modules/M6-api/app.py` 可核对）：

| 接口 | 作用 |
|---|---|
| `/v1/catalog` | 对象目录（有哪些对象类型、各多少条） |
| `/v1/objects/wim_axle`、`/v1/objects/wim_axle/{record_id}`、`/v1/objects/{object_type}` | 对象查询 |
| `/v1/metrics/wim_hourly` | 小时级指标 |
| `/v1/actions/{action_name}` | 动作提交（**501 占位，待你实现**） |
| `/openapi.json` | 契约④的真源（自动生成） |

> ⚠️ **模块状态看板 `/v1/modules/status` 不是 M6 的**，那是 M9(console) 的，
> 端口 8024。别在自己模块里找别人的接口——这是"跨模块只认契约"的基本功。

#### 你的任务

1. **动作层仍是 501 占位** → 逐步实现
2. **配合 A 组把质量门状态暴露出来**（下游要靠它决定能不能算结论）
3. **你是契约④的承诺方**：接口一改，前端和上游都要返工，改之前想清楚

#### 具体怎么做

1. 读 `modules/M6-api/app.py`（参考实现 6，§2.2）。重点看：
   - 它**怎么处理"数据还没来"**——不返回空，而是报错说明原因
   - 501 响应里**带责任人**（让调用方知道该找谁）
2. 挑一个 501 占位接口，实现它。**第一条竖切要窄**——完整跑通一个，
   比十个半成品有价值。
3. 加接口时**同步更新契约**：`/openapi.json` 是自动生成的，
   但 `contracts/openapi/m6-gateway.v0.1.yaml` 是手写的真源，要一起改。

#### 怎么测

```bash
cd scaffold
./run_contract_tests.sh m6
curl -s localhost:8001/openapi.json | python3 -m json.tool | head -40
curl -s localhost:8001/v1/catalog | python3 -m json.tool
curl -s -o /dev/null -w "%{http_code}\n" -X POST localhost:8001/v1/actions/some_action
# 已实现的接口：200 + 真数据
# 未实现的接口：501 + 责任人（不是空列表！）
```

#### 交付物

- [ ] 至少 1 个 501 占位接口变成真实现
- [ ] 质量门状态能在接口里读到
- [ ] `m6-gateway.v0.1.yaml` 同步更新

---

### D-2 · M8 应用层

#### 现状

🟡 骨架已就位：4 个端点（`/v1/fem/run` 有限元、`/v1/capacity/assess` 承载力、
`/v1/maintenance/plan` 养护方案、`/v1/twin/view` 数字孪生视图）、
`contracts/apps/maintenance_plan.v0.1.schema.json`。

#### 你的任务

**做假数据 demo**。等 M4/M6 的出口稳定后再接真数据。

**为什么先做假数据 demo 是对的**：应用层的价值在于"把数据变成工程决策"，
这个转化逻辑**不依赖真实数据就能验证**。假数据 demo 能让你和导师先对齐
"输出长什么样才算有用"，等真数据来了直接换数据源。

#### 具体怎么做

1. 从 `/v1/maintenance/plan` 开始——它最靠近项目的最终目标（"什么时候修、怎么修"）
2. 输出必须符合 `contracts/apps/maintenance_plan.v0.1.schema.json`
3. **假数据要标注清楚**：在响应里带 `"data_source": "synthetic"` 之类的字段，
   **绝不能让 demo 看起来像真结论**（违反纪律 2）

#### 怎么测

```bash
cd scaffold
./run_contract_tests.sh m5-m10        # 覆盖 M5–M10 的产出契约
docker compose -f docker-compose.skeleton.yml --profile m8 up -d apps
curl -s -X POST localhost:8023/v1/maintenance/plan -H 'Content-Type: application/json' -d '{}' | python3 -m json.tool
```

#### 交付物

- [ ] 至少 1 条端到端 demo（输入 → 输出 → 符合 schema）
- [ ] 输出里明确标注是合成数据

---

### D-3 · M9 平台管理台 ★ 现在就能做

#### 现状

🟡 骨架已就位：**你自己的服务**已经有 `/v1/modules`（列出模块）和
`/v1/modules/status`（状态看板）两个接口，**数据源在你自己的 `config/modules.yaml` 里**
（`modules/M9-console/config/modules.yaml`），10 个模块都已登记。

> ⚠️ 别去 M6 找这个数据——`localhost:8001` 是 M6(api)，它提供的是
> 对象查询（`/v1/objects/*`）、指标（`/v1/metrics/*`）、目录（`/v1/catalog`）和
> 动作提交（`/v1/actions/*`）。模块状态是**你 M9 自己**的事。

#### 你的任务

**做一个真正的看板界面**。这是 D 组当前**最独立、最快能出成果**的任务——
因为数据源（M6 的接口）已经通了，不依赖任何未完成的模块。

#### 具体怎么做

1. **先看数据**：起你自己的服务，然后
   `curl -s localhost:8024/v1/modules/status | python3 -m json.tool`
   把返回的字段列出来——这就是你要展示的东西
2. **做一个最小可用界面**：一个 HTML 页面（可以是单文件），调这个接口，展示：
   - 10 个模块各自的：名称、状态、健康、责任人
   - **门禁状态**（哪些模块被上游卡住）——这是本项目特有的、很有价值的展示
3. 放在 `modules/M9-console/` 下，由它自己的服务托管

**不用上大框架**。先做一个能跑的静态页 + fetch，比引入 React 全家桶有价值。

#### 怎么测

```bash
docker compose -f docker-compose.skeleton.yml --profile m9 up -d console
curl -s localhost:8024/healthz
# 浏览器打开 http://localhost:8024/ 看界面
```

#### 交付物

- [ ] 一个能打开的看板页面
- [ ] 展示 10 个模块状态 + 门禁关系
- [ ] 截图 + 访问方式写进 README

---

### D-4 · M10 Agent 执行引擎

**二期，本阶段不排期。** 想提前了解的话读
`contracts/agent/action_ticket.v0.1.schema.json`（工单契约）。

---

## M7 语义中枢 ⚠️ **责任人待指派**

### 为什么特别说这个模块

M7 是**全项目最有意思、也最独立**的一块，但现在**还没有人认领**。
不指派的话，P4 会变成无人负责的模块。

### 它要解决什么问题

现实中的路面数据来自不同单位、不同年代、不同设备，**同一件事有不同的叫法**：

| 问题 | 例子 |
|---|---|
| 表头不一致 | 一份 Excel 叫"桩号"，另一份叫"起点桩号"、`K+`、`STA` |
| 单位不一致 | 重量有的用 kg、有的用 t、有的用磅 |
| 编码不一致 | 病害类型有的写"裂缝"、有的写 `crack`、有的写 `CR-01` |

M7 的任务就是**把外来的数据映射到平台的标准字段上**，并且**每一步映射都要可追溯、可人工确认**。

### 现状

🟡 骨架已就位，而且**数据表已经建好了**：

| 资产 | 位置 |
|---|---|
| 3 个端点 | `modules/M7-semantic/app.py`：`/v1/mappings/probe`（探查）、`/v1/mappings`（列出映射）、`/v1/mappings/{mapping_id}/confirm`（人工确认） |
| 配置 | `modules/M7-semantic/config/mapping.yaml` |
| 契约 | `contracts/semantic/mapping_set.v0.1.schema.json` |
| **数据表** | **`mapping_set`（DDL v0.2 H1）——已经建好了** |

### 你的任务（指派后）

**结构探查 → 规则 → LLM 候选 → 人工确认 → `mapping_set` 落库**

### 具体怎么做

1. **先读表设计**：`scaffold/sql/数据字典-v0.2.md` 里 `mapping_set` 那一节。
   注意几个库约束（**它们是设计意图的直接体现**）：
   - `status` 只能是 `候选`/`已确认`/`已废弃`
   - **`status = '已确认'` 时必须有 `confirmed_by` 和 `confirmed_at`**
     —— 即"确认"这件事必须留痕，不能是匿名按钮
   - `entries` 必须是非空 JSON 数组
   - `(mapping_id, version)` 唯一 —— 映射**有版本**，改映射是新增版本而不是覆盖

2. **实现探查**（`/v1/mappings/probe`）：
   给它一份外来表头，它返回**候选映射**（`status='候选'`, `proposed_by='rule'` 或 `'llm'`）。

3. **人工确认**（`/v1/mappings/{id}/confirm`）：
   把候选变成 `已确认`，**必须记录确认人和时间**。

4. **接 LLM 的地方要格外小心**：
   LLM 的输出**只能作为候选**（`proposed_by='llm'`），**绝不能直接写 `已确认`**。
   这是本模块最重要的设计约束——库约束已经在强制它了，你的代码也要遵守。

### 怎么测

```bash
cd scaffold
docker compose -f docker-compose.skeleton.yml --profile m7 up -d semantic
curl -s localhost:8022/healthz
curl -s -X POST localhost:8022/v1/mappings/probe -H 'Content-Type: application/json' \
     -d '{"headers": ["桩号","总重(kg)","轴数"]}' | python3 -m json.tool
```

**必做的实验**：验证库约束真的在拦——
试着直接往 `mapping_set` 插一条 `status='已确认'` 但没有 `confirmed_by` 的记录，
**必须被数据库拒绝**。这个实验证明你的设计防线是真的。

### 交付物

- [ ] 探查接口能返回候选映射
- [ ] 确认接口留痕（确认人 + 时间）
- [ ] LLM 候选与人工确认**分离**的证据
- [ ] 版本机制可用（改映射 = 新增 version）

---

## 6. 测试体系

### 6.1 九个契约测试，各测什么

跑法：`cd scaffold && ./run_contract_tests.sh`（**离线，不需要 Docker**）

| 名称 | 负责模块 | 它保证什么 | 你什么时候会被它挡 |
|---|---|---|---|
| **m2-报文** | M2 | 报文 schema 与 Pydantic 模型**两个实现完全一致** | 改了 schema 没改模型（或反之） |
| **m2-造数器** | M2 | 造数器产出的报文**必须过得去契约** | 改了报文格式忘了改造数器 |
| **m3-dao** | M3 | 7 域读接口、跨域拦截有效 | 越域读数据 |
| **m6-路由** | M6 | HTTP 路由契约稳定 | 改了接口路径/返回结构 |
| **m2-违约分类** | M2 | 违约码精确，且 M2/M4 跨层命名一致 | 新增违约类型没给码，或与 M4 规则码不一致 |
| **m4-治理** | M4 | 质量规则契约 + 真值晋升契约 + **配置↔契约一致** | 改了 `quality_rules.yaml` 但没同步 schema |
| **m5-m10** | M5–M10 | 五个模块的产出契约（诊断/映射/养护/登记/工单） | 你模块的产出不符合自己的 schema |
| **m3-写权** | M2/M3 | **写权守卫应通过/应拒绝两侧** + 去重键↔DDL 索引 + M2 不绕契约 | 见 §4.5 三条规矩 |
| **m1-表数** | M1/M3 | **DDL ↔ 数据字典 ↔ catalog 三处表数一致**（逐表比对） | 你改了 DDL/字典/catalog 任一处但没改齐 |

### 6.2 三个"你一定会撞上"的测试

#### ① `m1-表数`：三处表数一致

**为什么会有这个测试**：DDL 文件、数据字典、`catalog.py` 是三个独立的地方，
**它们天生有互相打脸的风险**（改一处忘另一处）。人工评审靠不住，
所以钉成断言：**任何一处单独改动都会失败**。

**撞上了怎么办**：看报错，它告诉你哪张表在哪个文件里缺失。把三处改齐。

#### ② `m3-写权`：写权守卫

**撞上了怎么办**：看报错信息，它会告诉你三种情况里的哪一种：

| 报错 | 意思 | 怎么办 |
|---|---|---|
| `UnknownTable` | 表名不在 `ALL_TABLES` | 表名写错，或新表没登记进 catalog |
| `WriteGuardError`（只读表） | 这张表没登记写权 | 走工单登记唯一写入方 |
| `WriteGuardError`（越权） | 表属于别的模块 | **别绕**，去找那个模块 |

#### ③ `m4-治理`：配置 ↔ 契约一致

**撞上了怎么办**：你改了 `quality_rules.yaml` 但没同步 schema（或反之）。
两边必须一起改。

### 6.3 测试红了：三步定位法

```
第 1 步：看它说哪个文件、哪一行、哪个断言
        ——不要先猜，先读报错（报错信息是写来给人看的，本项目尤其如此）

第 2 步：问"我最近改了什么"
        git diff            # 看未提交改动
        git log -1 -p       # 看最近一次提交

第 3 步：判断是"我错了"还是"测试错了"
        —— 90% 是你错了。剩下 10% 是真的发现了测试的 bug，
           这时候**不要自己改测试**，报给导师（测试是契约的一部分）
```

### 6.4 怎么写"不会永远通过"的测试 ★

**这是本项目最核心的工程原则，希望你们学会并带走。**

> 「**一个永远不会失败的检查，比没有检查更糟。**」

因为它给你**虚假的安全感**——你以为守住了，其实什么都没守住。

**具体怎么写**：每条规则都要测**两侧**。

| 只测一侧（❌ 无用） | 测两侧（✅ 有用） |
|---|---|
| 合格的能写进去 | 合格的能写进去 **＋ 不合格的必须被拒** |
| 正常的能通过校验 | 正常的能通过 **＋ 越界的必须报错** |
| 未实现返回 200 | 未实现返回 **501 + 原因** |

**再加一层：元测试**（确认断言不是恒真的空断言）。

看 `tests/contract/test_write_guard.py` 的第 8 组——它专门验证：
"如果守卫被改成永远放行，第 2 组必须失败；被改成永远拒绝，第 1 组必须失败"。

**没有这一层，你无法排除"这组测试其实什么都没测"的可能。**

---

## 7. 实验与造数

### 7.1 造数器用法（`scaffold/simulator/wim_simulator.py`）

真设备到场之前，用它驱动整条链路。

```bash
cd scaffold

# ① 干跑：只看报文不发（不需要 broker，最快的自检）
uv run --with paho-mqtt simulator/wim_simulator.py --dry-run --count 3

# ② 正常发：20 条，每秒 1 条
uv run --with paho-mqtt simulator/wim_simulator.py \
    --host localhost --port 18883 --count 20 --interval 1.0

# ③ 带违约：60 条，25% 故意违约（验证质量门与拒收路径）
uv run --with paho-mqtt --with pydantic simulator/wim_simulator.py \
    --count 60 --invalid-rate 0.25

# ④ 固定随机种子（结果可复现——写实验报告必须用它）
uv run --with paho-mqtt simulator/wim_simulator.py --count 60 --seed 42
```

**全部参数**：

| 参数 | 默认 | 说明 |
|---|---|---|
| `--host` / `--port` | localhost / **18883** | broker 地址。**注意默认值就是 18883**（不是 1883）——骨架栈为避让本机已占用的 1883 统一改成了 18883，造数器的默认值已同步 |
| `--topic` | `g228/lo/wim01/axle` | 发布主题 |
| `--count` | 10 | 条数（0 = 不停） |
| `--interval` | 1.0 | 秒/条 |
| `--invalid-rate` | 0.1 | 违约报文比例 |
| `--seed` | 无 | 随机种子（**写报告时必填**） |
| `--dry-run` | 关 | 只打印不发送 |

### 7.2 三个必做的实验

| # | 实验 | 怎么做 | 验证什么 | 预期结果 |
|---|---|---|---|---|
| **1** | **骨架栈验收** | `up -d` 后 `ps` | 6 个容器健康 | 全部 `(healthy)`，不是 `Up` |
| **2** | **拒收路径** | `--invalid-rate 0.25 --count 60` | 质量门真的在拦 | 收 60 → 落 45 / 拒 15；`data_quality_log` 出现**精确规则码**，无兜底码 |
| **3** | **端到端贯通** | 发数后查库 | 数据真的落进去了 | 主记录 + 明细行数增加；**孤立主记录 0 条**（无"半条数据"） |

**实验 3 的"孤立主记录"怎么查**（这条最能体现工程严谨性）：

```bash
docker exec rp-pg psql -U rp -d road_pavement -tAc \
  "SELECT count(*) FROM wim_axle_record r
   WHERE NOT EXISTS (SELECT 1 FROM wim_axle_detail d WHERE d.record_id = r.id);"
#   预期 0。若不为 0，说明多表写入的原子性破了——去查是不是没用 write_txn
```

### 7.3 真实数据摸底：开工前必做（A 组与 C 组共同）

**六项清单**，逐项出结论：

| # | 摸底项 | 要回答的具体问题 |
|---|---|---|
| 1 | **数据来源** | 谁提供？什么设备？哪个时间段？覆盖哪几个断面？ |
| 2 | **覆盖度** | 7 个域里，真实数据实际覆盖了几个？（**现状只有 1 个域曾跑通**） |
| 3 | **数据量与频率** | 多少条？采样频率多少？不同域频率是否一致？ |
| 4 | **质量现状** | 缺失率多少？有没有明显离群？与合成数据的差异在哪？ |
| 5 | **时间对齐** | 不同域的数据能不能对齐到同一时间轴？误差多大？ |
| 6 | **标定可行性** | M4 的 9 条统计阈值、M5 的诊断系数，各自能不能从这批数据标定出来？标不出来的列清楚 |

> **第 2 项和第 6 项最重要**，因为它们决定**项目能做到什么程度**。
> 现在的诚实结论是：拿到真实数据前**不给工时数字**——猜出来的数字只会误导排期。

### 7.4 校准记录该写什么

标定任何阈值/系数，记录必须包含这五项（缺一项就不算标定）：

1. **数据来源**：哪个文件/哪张表，什么时间段，多少条
2. **统计量**：触发率/分布/分位数（给数字，不给"看起来合理"）
3. **最终取值**：定成了多少
4. **依据**：为什么定这个值（可以引用工程规范、可以引用统计分布，但要写出来）
5. **标定人 + 时间**：谁定的、什么时候

对应到 `quality_rule` 表的字段：`calibrated` / `calibrated_at` / `calibrated_by` / `evidence`。

> **库约束在强制这件事**：`calibrated = true` 但没写 `calibrated_at` 的记录**插不进去**。
> 这是刻意的——"标定过"不能只是一个布尔值，必须能追到人和时间。

---

## 8. 交付与验收

### 8.1 每个模块的 DoD（完成定义）

逐条打勾，**缺一条不算完成**：

- [ ] **接入五件套齐**：容器定义（含 healthcheck）、`/healthz` + `/metrics`、
      配置全外置、契约文件、**离线可跑的契约测试**
- [ ] **契约测试绿**：`./run_contract_tests.sh <你的模块>` 全过
- [ ] **契约测试覆盖两侧**：既有"应通过"，也有"应拒绝"
- [ ] **一条真跑的竖切**：不是"代码写完了"，是"端到端跑通了一次"
- [ ] **未实现的显式报错**：返回 501/503 + 原因，**不返回空**
- [ ] **README 更新**：怎么起、怎么调、怎么测
- [ ] **无硬编码**：地址/口令/阈值都在 `config/`
- [ ] **不绕契约**：没有裸 SQL、没有自建连接池、没有跨模块 import

### 8.2 提交规范

**分支**：一人一分支，命名 `feat/M4-calibration` 或 `fix/M2-reject-code`

```bash
git checkout -b feat/M4-calibration
# ... 干活 ...
git add <明确写文件路径>        # ⚠ 不要用 git add -A，会把不该提交的带进去
git commit -m "M4: 标定 9 条统计阈值，触发率与依据见 calibration-report.md"
git push -u origin feat/M4-calibration
# 然后在 GitHub 上开 Pull Request
```

**commit message 要求**（三条）：

1. 第一行：`<模块>: <做了什么>`（不超过 50 字）
2. 空一行
3. 正文：**为什么这么做**、怎么验证的、有什么已知限制

> 正文比标题重要。看 `git log` 里已有的提交——每一条都写了"为什么"，
> 包括踩过的坑。**这是这个仓库的传统，请延续。**

### 8.3 本轮（P1 收尾）交付清单

| # | 交付物 | 负责 | 验收标准 |
|---|---|---|---|
| 1 | **M4 真数据门上线** ★ | A | `truth_flag` 真正晋升；9 条阈值有标定依据 |
| 2 | 真实数据摸底报告 | A + C | 六项清单逐项有结论 |
| 3 | IoTDB 接入 | A | compose 里有服务且 healthy |
| 4 | M9 模块状态看板 | D | 能打开、显示 10 模块状态与门禁 |
| 5 | M8 假数据 demo | D | 至少 1 条端到端，标注合成数据 |
| 6 | M2 接入侧准备 | B | 未知项清单交导师 |
| 7 | **M7 责任人指派** | 导师 | 有人认领 |
| 8 | M5 | C | ⛔ **不交付代码**，只交摸底报告 |

---

## 9. 卡住了怎么办

### 9.1 求助顺序（按这个顺序，别跳步）

```
1. 先读报错信息
   —— 本项目的报错信息是特意写清楚的（指出该修哪一处），先读完再问

2. 查这三份文档
   scaffold/MODULE-ONBOARDING.md     接入规矩
   scaffold/SKELETON-GUIDE.md        骨架用法 + 踩过的坑
   scaffold/sql/数据字典-v0.2.md          字段含义

3. `git log` 看看有没有人踩过同样的坑
   git log --oneline --all | head -30

4. 问同组同学

5. 问导师（带上：你做了什么、报错全文、你已经试过什么）
```

**第 5 步的正确问法**：

> ❌ "我的服务起不来，怎么办？"
> ✅ "`rp-governance` 起来后 healthcheck 一直失败。日志显示 `connection refused` 连 PG。
>    我确认 `rp-pg` 是 healthy 的，`.env` 里端口是 55432。试过重启服务，没用。
>    这是我 `docker logs` 的输出：<粘贴>"

### 9.2 常见问题 FAQ

**Q：契约测试过了，但服务起不来？**
A：契约测试是**离线**的（不碰 Docker/数据库）。它只验证契约，不验证运行环境。
服务起不来去看 `docker logs`。

**Q：我改了 DDL，`m1-表数` 测试红了？**
A：对，这是设计如此。三处要一起改：DDL 文件、数据字典、`catalog.py`。
**另外提醒**：改 DDL 是契约变更，**要走工单**，不能自己改。

**Q：我的模块要写一张表，但 `assert_writer` 报"只读表"？**
A：那张表没登记写权。走工单申请，**不要绕过守卫**。

**Q：为什么 M4 的门禁关着时返回 503，而不是返回"全部合格"？**
A：因为返回"全部合格"是**假通过**——下游会拿它当结论。
这一条的原则是：「宁可报错，也不给假通过」。

**Q：可以用 AI 帮我写代码吗？**
A：可以，但**你必须能解释每一行**。提交前问自己：
"如果导师问我这里为什么这么写，我答得上来吗？"
本仓库的注释传统是写"为什么"——AI 通常只会写"是什么"，这部分要你自己补。

**Q：我想改一下契约（加个字段），行不行？**
A：走工单（`scaffold/MODULE-ONBOARDING.md` §5 有模板）。
**学生不参与设计决策**，但可以提工单。契约一改下游全组返工，所以必须导师把关。

### 9.3 术语表

| 术语 | 意思 |
|---|---|
| **契约** | 模块之间的接口约定。四份：报文/表结构/DAO/服务接口 |
| **真源** | 某个东西唯一的权威定义处（改了别处无效） |
| **接入五件套** | 新模块必须齐的五样：容器/健康指标/配置外置/契约文件/契约测试 |
| **门禁** | 上游不绿，下游不许启动 |
| **标定** | 用真实数据确定阈值/系数，并记录依据 |
| **真值晋升** | 把批次数据标记为"可信"，从 `truth_flag=false` 变 `true` |
| **竖切** | 一条从入口到出口的完整通路（哪怕很窄），比十个半成品有价值 |
| **拔插** | 模块能独立启停、替换，不影响别人 |
| **反模式** | 已知的错误做法，出现即打回 |
| **造数器** | 合成数据的生成器，用于验证链路（**不能**验证算法） |

---

## 附录

### A. 端口与容器对照

| 服务 | 容器名 | 本机端口 | Profile | 状态 |
|---|---|---|---|---|
| PostgreSQL | `rp-pg` | 55432 | 默认 | ✅ 已落地 |
| EMQX (MQTT) | `rp-mqtt` | 18883 / 18083(控制台) | 默认 | ✅ 已落地 |
| MinIO | `rp-minio` | 9002 / 9003(控制台) | 默认 | ✅ 已落地 |
| Grafana | `rp-grafana` | 3001 | 默认 | ✅ 已落地 |
| **M2 接入** | `rp-ingest` | 8010 | 默认 | ✅ 已落地 |
| **M6 API** | `rp-api` | 8001 | 默认 | ✅ 已落地 |
| **M4 治理** | `rp-governance` | 8020 | `m4` | 🟡 骨架 |
| **M5 融合** | `rp-fusion` | 8021 | `m5` | 🟡 骨架（⛔ 锁定） |
| **M7 语义** | `rp-semantic` | 8022 | `m7` | 🟡 骨架（待指派） |
| **M8 应用** | `rp-apps` | 8023 | `m8` | 🟡 骨架 |
| **M9 管理台** | `rp-console` | 8024 | `m9` | 🟡 骨架 |
| **M10 Agent** | `rp-agent` | 8025 | `m10` | 🟡 骨架（二期） |

> 起非默认模块要显式指定 profile：
> `docker compose -f docker-compose.skeleton.yml --profile m4 up -d`

### B. 文件路径速查

```
scaffold/
├── MODULE-ONBOARDING.md          ← 接入规矩（必读）
├── SKELETON-GUIDE.md             ← 骨架用法 + 踩过的坑（必读）
├── run_contract_tests.sh         ← 契约测试总入口（离线可跑）
├── docker-compose.skeleton.yml   ← 全套编排
├── .env.example                  ← 配置模板（复制成 .env）
├── contracts/                    ← 四份契约的文件
├── modules/M3-rpdao/rpdao/          ← 契约③：数据访问层
│   ├── catalog.py                ←   表清单与写权（参考 1）
│   ├── repo.py                   ←   7 域读接口（参考 2）
│   └── write.py                  ←   写权守卫（参考 3 ★★★）
├── modules/                     ← ★ 每模块一目录（学生各自扩展、最后整合）
│   ├── M2-ingest/                ← M2（参考 4 ★★★，完整竖切）
│   │   ├── app.py
│   │   ├── models.py
│   │   └── violations.py         ←   错误分类（参考 5 ★★）
│   ├── M3-rpdao/rpdao/           ← M3 数据访问层（参考 1-3）
│   ├── M4-governance/            ← M4 ★ 关键路径
│   │   └── config/quality_rules.yaml  ← 11 条规则，等标定
│   ├── M5-fusion/                ← M5（⛔ 锁定）
│   ├── M6-api/                   ← M6（参考 6，只读出口）
│   ├── M7-semantic/              ← M7（待指派）
│   ├── M8-apps/                  ← M8
│   ├── M9-console/               ← M9
│   └── M10-agent/                ← M10（二期）
├── simulator/wim_simulator.py    ← 造数器
└── tests/contract/               ← 9 个契约测试

（代码仓库根目录）
├── README.md                            ← 入口
├── 学生上手指南-分模块开发手册.md        ← 你正在读的这份
└── 分组分模块任务书-P1起.md              ← 任务分配总表

scaffold/sql/
├── 10_ddl_v0.2.sql                      ← 契约② 真源（32 表），compose 挂载它建库
├── 数据字典-v0.2.md                      ← 字段说明（人读版）
├── 20_partitions.sql                    ← 按月分区
└── 90_seed_skeleton.sql                 ← 骨架种子数据
```

> **关于不在仓库里的东西**：本仓库**只装代码与契约**（约 0.5 MB）。
> 交付物性质的 `output/`（报告、图件、课题计划文档）与 `docpipe/`
> （文档图件生成流水线、标准规范素材）**有意不进来**——它们体积大、
> 含未定稿材料与受版权保护的素材，且开发时根本用不到。
> 需要时向导师索取。
>
> **唯一例外**是上面 `scaffold/sql/` 里的 DDL 与数据字典：它们看着像文档，
> 其实是**契约②的真源**——compose 要挂载它建库、契约测试要读它比对，
> 所以必须与代码同仓。**判断标准是"代码是否依赖它"，不是"它长得像什么"。**
```

### C. 提交信息模板

```
<模块号>: <一句话做了什么>

为什么：
  <为什么要做这个改动。是修 bug？补功能？响应什么需求？>

怎么验证的：
  <跑了什么测试/实验，结果是什么。给数字。>

已知限制：
  <还没做什么、有什么已知问题、什么情况下会失败。>

关联：
  工单 #<编号>（如果涉及契约变更）
```

---

**最后一句**：这个仓库里的每一处注释都在写"为什么"，每一处设计都在防"看起来对但其实错"。
你写代码时也请这样——**能说清"为什么不能那样写"的人，才是真的懂了**。
