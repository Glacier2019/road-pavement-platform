# 会话审计报告 · session-20260907.json

> 审计对象：`/home/zhanghe/下载/session-20260907.json`（session_id `20260907_094951_2da4ec`，标题「零基础学生开展计划与任务分配」）
> 审计方式：解析 325 条消息全文 + **直接核对工作目录 `/data/cy/shujuku` 的实际产出 + 实跑代码复现缺陷**
> 结论口径：标注「已实测复现」的为我在本机验证过；标注「已核对」的为逐条比对过原文/文件；其余为推断。

---

## 一、会话概况

| 项 | 值 |
|---|---|
| 消息总数 | 325（真实用户输入 **14** 条 / assistant 118 条 / 工具结果 185 条） |
| 时间跨度 | 2026-09-07 09:52 → 2026-09-14 13:31（中间 09-07 11:50 → 09-14 09:59 空档 ~7 天） |
| 工具调用 | write_file 50、read_file 39、terminal 38、search_files 16、patch 15、web_search 11、skill_view 5、web_extract 3、delegate_task 1、execute_code 1 |
| 上下文压缩 | **6 次**（全部发生在 09-14，其中 4 次挤在 12:44–13:29 的 45 分钟内） |
| 被审批拦截 | 4 次（1× execute_code，3× terminal） |
| 实际交付物 | 报告 v1.2（docx/pdf/md 三格式）、研究生任务 Word（+第八章）、`scaffold/` 骨架栈 23 个源码文件、导读 md |

主线任务链路：**研究生任务分配 Word** →（09-14）**读 6 份国标 → 报告补第九章** → **对标 Palantir → 报告补第十章** → **清理旧版本** → **写第一步的骨架栈代码**。

---

## 二、问题清单

### A. 交付物里的硬伤（已实测 / 已核对）

#### A1. 🔴 `GET /v1/objects/wim_axle` 返回 404 —— 路由被自己遮蔽（已实测复现）

`scaffold/services/api/app.py` 里 `/v1/objects/{object_type}`（第 106 行）注册在 `/v1/objects/wim_axle`（第 116 行）**之前**。Starlette/FastAPI 按注册顺序取第一个完全匹配，参数化路由会把静态路由吃掉。

我实跑该 app（用系统 fastapi 0.112.2 + 最新 starlette 1.3.1 双版本验证，结论一致）：

```
注册顺序 → ['/v1/objects/{object_type}', '/v1/objects/wim_axle', '/v1/objects/wim_axle/{record_id}', ...]
GET /v1/objects/wim_axle 实际命中 → /v1/objects/{object_type}  |  list_objects
  /v1/objects/wim_axle      -> 404 {'detail': '未登记的对象类型：wim_axle（白名单见 /openapi.json）'}
  /v1/objects/road_line     -> 正常路由（到达 DB 层）
```

影响面被放大三处：
- 它是**竖切链路里唯一的"对象查询"入口**，也是 `MODULE-ONBOARDING.md` 第 92 行给全校学生示范的接口路径 —— 文档指向一个必然 404 的地址；
- `contracts/openapi/m6-gateway.v0.1.yaml` 第 48 行**明确写了这个端点** → 契约与实现不一致，而契约测试只测报文，不测 HTTP 路由，测不出来；
- 容器从未启动过，所以"跑一遍就发现"的自愈路径也没走。

**修法**：把 `list_objects` 注册挪到所有具体路径之后；或把参数路由改成 `/v1/objects/{object_type}` 但显式排除 `wim_axle`。建议顺手补一个 `tests/contract/test_api_routes.py`（TestClient 断言 4 条路径的 status）。

#### A2. 🟠 交付物自相矛盾之一：规约说「取数只经 M6」，随附的 Grafana 面板直连 PG 裸 SQL（已核对）

- `MODULE-ONBOARDING.md` 三条硬线：「取数只经 M6、写入只经 M2」；`services/api/app.py` 开头也写「不允许直连库」。
- 但 `ops/grafana/provisioning/dashboards/wim_skeleton.json` 的 4 个面板**全部是 `rawSql` 直查 `wim_axle_record`**，完全绕过 M6。

给 0 基础学生的第一份参考实现，就在违反同一份文档教他的第一条铁律。要么把面板改成 `datasource: api`（调 M6），要么在规约里显式开一个"运维只读面板可直连"的例外。

#### A3. 🟠 交付物自相矛盾之二：api 服务没有 `/metrics`，不符合自己的"五件套"（已核对）

`MODULE-ONBOARDING.md` 第 35 行要求每个模块交 `/healthz` + `/metrics`，并以 `services/ingest/app.py` 为范例。实际：

| 服务 | /healthz | /metrics |
|---|---|---|
| ingest（范例） | ✅ | ✅ |
| api | ✅ | ❌（只有 /healthz） |

自己发的规约，自己的第二个服务就没过。

#### A4. 🟡 全链从未跑通过，但措辞读起来像已经跑通（已核对）

`README.md` 第 5 行：「一句话目标：**让一条 WIM 过车报文穿过 6 个容器跑通全链**」；导读同段：「竖切跑通，等于把三份契约当场验证了一遍」。

实际上：`docker compose up` 因审批超时被拦（消息 301），**6 个容器一次都没起过**。诚实披露是有的（README 与导读都写了"容器实跑 ⏸ 未执行"），但正文的完成式表述与验证状态表是打架的。三条已确认的隐含影响：

- Dockerfile 能否 build、initdb 顺序（10_ddl → 20_partitions → 90_seed）能否跑通、分区边界是否覆盖当前时间，**全部未验证**；
- `.env` 至今不存在（`scaffold/.env` 没有），照 README 第一步 `cp .env.example .env` 才能起 —— 这条路径没被走过一遍；
- 因此 A1 这类缺陷只能靠静态审查发现，而它没被发现。

#### A5. 🟡 自报数字未测量：`23 个文件 / 1694 行`（已核对）

实际（今天复测）：**23 个源码文件**（对的，另含 7 个 `.pyc`）/ **2316 行总计，排除 md 为 2011 行**。"1694" 在整个会话里没有任何一次统计命令支撑 —— 这是个凭印象写的数字，且写进了给用户的结论里。

#### A6. 🟡 学生任务 Word 没有标题层级（已核对）

`output/研究生任务分配与个人实施计划（路面性能7域·平台主线）.docx`：90 个段落**全部是 `Normal` 样式**，零个 Heading。对照报告 v1.2 是规范的 `Heading 1×12 / Heading 2×35 / Heading 3×8`。后果：Word 导航窗格空白、无法自动生成目录、无法交叉引用 —— 一份要发给学生和导师、还要反复改 12 个月的文件，这个缺陷会一直疼。

---

### B. 事实可靠性（本会话最值得警惕的一类）

#### B1. 🔴 报告初稿写入**未经核实的第三方数据**当事实，5 分钟后才自查更正

12:45 的 Palantir 回答里，以「**查了**，现在没有一个能用」的口气给出：

> `DioCrafts/OpenFoundry`（Go）｜**193 stars**，51 服务目录…｜**AGPL-3.0** 强传染
> `syzygyhack/open-foundry`｜**12 stars**、无 release、无社区｜被评价为"architecture design thesis in code form"
> 行业评估（**dataworkers.io**, 2026）｜开源组合覆盖 **70–80%**

这些数字在 12:55 被写进 `build_docx_report_v3_std.py` 并生成了 v1.2 报告。12:58 该会话自己起意"核实一下报告里写入的三个仓库数据"，逐仓拉取 GitHub 页面后发现**出入巨大**：DioCrafts 实为 **1 star、Rust+Svelte、未标注许可**；syzygyhack 实为 **77 stars / 23 forks / Apache-2.0**。13:01 全部更正后重建三格式。

**用户最终拿到的是更正版**，且会话主动披露了这次更正 —— 这点是加分的。但流程本身是**先写进交付物、后核实**，且拦截它的不是流程（没有"引用前必须核验"的规则），而是模型临时的自觉。同一模式在本次会话里出现过至少三次（B1 的仓库数据、B3 的违约率、09-07 的 CJCE 文献）。

#### B2. 🟠 未核实项仍留在正式报告里

- 「开源组合可覆盖 Palantir 本体能力约 **70–80%**」—— 报告里已自标"该比例系引用方估计，本章未独立核验"，措辞严谨，但**数字本身没有出处可查**（`dataworkers.io` 这个源在正文里被抹成"第三方技术博客"，等于承认源不可靠却保留比例）。
- **CJCE 2024《Integrating dynamic sensors and WIM systems…》**：在 S2/OpenAlex 均检索不到，会话自己标注"**存在性存疑，开题前需人工核验**"，但在同一段的正文里它已经被当作"已核实竞品"写进课题 B 的差异化论证。核验动作被推给了未来。

#### B3. 🟢 一个被自己抓到的**测量口径错误**（这次是对的，但值得记）

契约测试②最初报「违约报文 50/50 被接入层拒收，其中 **50** 条对 JSON Schema 仍合法 → 印证 Schema 表达力边界」。后来发现"50 条全合法"是因为造数器自己往报文里塞了 `payload["_injected"] = kind` 这个额外字段 —— 是测量假象。删掉后真实数字是 **34 条**（另 16 条是 Schema 本来就能拒的越界值）。

如果没发现，报告 10.4 节"接入层业务校验不可省略"的论据就架在一个虚高的数字上。**这是本会话里流程正确的一次自我纠错**，但同样依赖模型自觉。

---

### C. 用户明确要求但没落进交付物的内容

#### C1. 🔴「道路几何」升级 + 创新点核查结论**只存在于对话里**

09-07 用户连问两轮：*"思考搜索研究生论文的创新点够不够，内容是否充实饱满"*、*"你说的研究其实应该都未加入道路几何研究，再评估下创新点，关键是工作量饱满吗"*。会话给出了相当有价值的答复（四个课题逐一评级、几何分层带来的差异化升级、工作量排序 B≈D>A≈C、3 人版重排建议），并两次问用户「要不要把这份评估合并进任务 Word？」——用户没有正面回答，转向了别的任务，**这份结论就此没有落进任何文件**。

已核对 `output/研究生任务分配与个人实施计划（路面性能7域·平台主线）.docx` 全文（含 7 张表）：

```
几何时空基准   0 次
几何分层       0 次
线形           0 次
创新点         0 次
```

**用户本轮最核心的关切（论文创新点够不够、几何维度加没加进去）在最终交付物上是 0 命中。** 而且这份 Word 在 09-14 又被重新生成过一次（加第八章），依然没带上。这是本次会话最大的"需求落地缺口"。

#### C2. 🟠 深度调研（09-07 [41]）2/3 子代理超时，结果无文件沉淀

- 3 个并行子代理，**2 个在 600s 硬超时**（课题 A、课题 B/C）；根因是 `execute_code` 审批阻塞 + Semantic Scholar 无 key 被 429 限流。
- 最终结论（含 MSSP 2022 / DOI 10.1016/j.ymssp.2022.109930 / 被引 104）**只在对话里**，`output/` 下没有对应文件（目录里那两份 `深度调研报告-*.md` 是别的会话产出，本会话只是 `ls` 时列到了它们）。
- 这是"高价值产出零沉淀"：用户下次想引用时，只能翻聊天记录。

#### C3. 🟡 版本反复 + 前后矛盾的口径

| 时间 | 事件 |
|---|---|
| 12:55 | 写入未核实仓库数据 |
| 12:58 | 生成 v1.2（含错误数据） |
| 13:01 | 更正后**重建** v1.2（同一文件名，同一天内第 2 次生成） |
| 13:01 | 回复里写「三格式同步更新（**v1.1 保留**）」 |
| 13:05 | 回复里改成「v1.0、v1.1 及更早版本是否清理，只留 v1.2」 |
| 13:14 | 执行删除，v1.1 三件套 + media_v11 全没了 |

同一个文件名被重建两次、旧版"保留"与"清理"在 4 分钟内反转，且 v1.1 一旦删除**不可再精确复现**（`build_docx_report_v3_std.py` 现在已含第十章）。对外交付的版本谱系因此无法回溯。

---

### D. 不可逆操作与备份

#### D1. 🔴 16 个文件被 `rm` 删除，工作目录**不是 git 仓库**（已核对）

```
$ git rev-parse --is-inside-work-tree
致命错误：不是 Git 仓库
```

`/data/cy/shujuku` 无版本控制、无备份，删除走的是 `rm -v`（不是回收站）。被删的包括：v1.0/v1.1 报告三件套、`media_v11/`、研究生任务旧版、课题甲前两版 docx、课题甲技术路线图 png/svg 各 3 版、早期 ER 图 html。

会话**主动提示了风险**（"删除均不可逆（这批图没有 git 备份）——纯视觉版的路线图以后若要，得重跑生成脚本"），用户也批准了，所以这不是违规操作。但整个项目 12 个月、投入 38.5 人周、产出十几个交付物，**至今没有版本控制**，这是比任何单次删除都更大的结构性问题。

#### D2. 🟡 删除清单与实际不符（已核对）

13:14 的回复里列的待删 6 项与实际执行的 6 项一致；但 13:15 那次删除实际删了 **10 项**（回复里也列了 10 项）。核对当前 `output/`：会话声称保留的都在，声称删除的都不在 —— **结果是对的**，只是"同一批次里报 6 条、下一批次报 10 条"的表述容易让人误判家底。

---

### E. 过程/环境层

| # | 问题 | 证据 |
|---|---|---|
| E1 | 🔴 **4 次工具调用被审批超时拦截**，其中一次直接导致"全链未验证" | 消息 56（execute_code）、149、180、301（terminal）。301 就是起容器那条。会话的处理是**正确的**（BLOCKED 后停止、不重试、转告用户），但代价是真实验证被砍掉了 |
| E2 | 🟠 **上下文抖动**：6 次压缩，4 次在 45 分钟内，其中 [214] 与 [244] **逐字节相同**（25135 字符，md5 一致） | 压缩摘要本身要占上下文，短时间内重复注入同一份 25k 字符摘要，是典型的抖动信号 |
| E3 | 🟡 **导出文件本身可读性差**：6 处时间戳倒流；1 条 assistant 消息重复 | [51] 用户消息时间 09:52 却排在 11:49 的消息之后；[57] 与 [59] 内容完全相同。这是"用户消息按投递点插入、时间戳按输入时刻保留"的机制产物 —— 不算 agent 的错，但**审计时会被误导**，建议导出加显式序号 |
| E4 | 🟡 子代理超时被算作"已完成"的批处理回执 | `[ASYNC DELEGATION BATCH COMPLETE]` 里 3 个任务有 2 个是 `status=timeout`，标题却写"All ran in parallel and waited on each other" —— 回执格式会让人误以为全部成功 |

---

## 三、我核对过、**没问题**的部分（避免只报坏消息）

这些我逐条比对过，是站得住的，可以放心：

| 项 | 核对结果 |
|---|---|
| **ingest 的 SQL 与 DDL v0.1 的列对齐** | ✅ 逐列核对 `wim_axle_record`（14 列）、`wim_axle_detail`（7 列）、`data_quality_log`（10 列）、`data_import_batch`（12 列）—— 占位符数量、列名、`ON CONFLICT (batch_no)` 均有对应 UNIQUE 约束，**全部一致** |
| **分区脚本** | ✅ 覆盖 2025-11 → 2028-12 逐月 + DEFAULT 兜底分区；且脚本注释里已写明"兜底分区有数据后再建同区间分区会失败"这个 PG 坑及 M4 的处理办法 |
| **种子数据与造数器对齐** | ✅ `sensor_install.serial_no='WIM01'`（active）↔ 造数器 `device_code="WIM01"` ↔ `MAP_SQL` 的 join 路径，链路自洽 |
| **国标条款引用准确性（抽检）** | ✅ 报告 9.1 引「48000.2 第 6 章」—— 原文第 6 章确为能力等级 L1–L5+；引「48000.1 第 7 章」—— 原文第 7 章确为"内容数字化"（含电子化/结构化/语义化）。**不是编的** |
| **Grafana provisioning 的 `${PG_PASSWORD}` 写法** | ✅ **不是 bug**。我查了 Grafana 官方 provisioning 文档与 grafana#97185，datasource 配置文件里 `$VAR` 与 `${VAR}` 都支持插值（compose 也已在 grafana 服务注入该变量）。我一开始怀疑这里有问题，核对后撤回 |
| **`.pyc` 是否会被提交** | ✅ `.gitignore` 已排除 `__pycache__/`、`*.pyc`、`.env`、`uv.lock` 等 |
| **json schema 表达力边界的结论** | ✅ 方向正确，且被 B3 的自查加固过 |

---

## 四、建议的修复顺序

**P0（今天就能改，改完再交付）**
1. 修 `services/api/app.py` 的路由顺序（A1），并加一条 `tests/contract/test_api_routes.py` 断言 4 条路径。
2. 把 C1 的"几何主线 + 创新点核查 + 文献起点"补进研究生任务 Word（这是用户的原始诉求，目前 0 落地），顺手把 Word 的标题样式改成 Heading 1/2（A6）。
3. `git init` + 首次提交当前 `output/` 与 `scaffold/`（D1），此后所有删除走 git。

**P1（跑一次真实验证）**
4. 在有审批的前提下 `cp .env.example .env && docker compose up -d --build`，把 README 第 125 行的"**7 条判据**"逐条打勾 —— 这是把"骨架栈"从"写完了"变成"跑通了"的唯一路径。
5. 统一 A2/A3 的口径：Grafana 面板改走 M6（或规约开例外）；api 补 `/metrics`。

**P2（流程固化，避免 B 类问题复发）**
6. 立一条规则：**任何进入交付物的第三方事实（仓库 star/许可、文献存在性、行业百分比）必须当场核验并把核验方式写进报告脚注**；核不到的只写"方向性参考"，不写数字。
7. 把 C2 的深研结论补落成 `output/深度调研报告-四个路面课题可行性.md`（带 DOI 与被引数），避免零沉淀。
8. 自报数字（文件数/行数/块数/表数）一律**先跑命令再写**。

---

## 五、一句话总结

会话的**领域判断质量相当高**（国标缺口定位、对标 Palantir 的取舍、契约驱动的骨架设计、诚实标注检索局限），**自我纠错意愿也强**（三次主动回头核实并更正）。真正的风险集中在三处：**① 唯一一次端到端验证被审批拦掉，导致交付物带着一个必然 404 的接口和两个自相矛盾的规约进入"已完成"状态；② 事实性数字存在"先写后核"的固定模式，靠自觉而非流程兜底；③ 用户最关心的"几何维度 + 创新点"评估至今没进任何交付文件，且项目全程没有版本控制。**
