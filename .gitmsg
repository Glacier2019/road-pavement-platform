plan(001): 实施计划 + Phase 0 研究 + Phase 1 设计（数据模型/契约/验收脚本）

## 计划的核心判断：归因必须跨模块拼装（硬线逼出来的，不是设计偏好）

空因归因需要三类事实，分属两个模块，谁也不能替谁：
  表里有几行 / 表的归属模块   -> M3（唯一接触存储）
  适配器实现了哪些段 / 源在不在 -> M2（解析器与导入目录都在它肚子里）

M3 不许读磁盘、M2 不许查库、M6 不许 import M2 —— 所以只能：
  M3 出「表->行数+归属」  +  M2 出「段->实现状态+源状态」  ->  M6 拼装
更快的写法（让 M3 直接读磁盘）会破坏「平台内唯一接触存储处」，
而那条线正是四组学生能并行开发的前提。

## Phase 0 三个未知，全部查证后解决（research.md）

R1 段<->表映射：**不新建映射表**。IR schema 里已明文写着
   「段名一律用 GE 域物理表名，使段<->表映射唯一且可自动核对」，
   唯一例外 design_control（1 段 9 表）也已登记，9 张表实测全部存在。
   新建一张映射会成为**第四份**对同一件事的陈述，必然漂 ——
   本仓库已因「手抄的表会漂」栽过（_IMPLEMENTED_SUFFIX 漏了三个后缀）。

R2 M2 接口形态：新增 GET /v1/design/capabilities。
   必须是 HTTP 而不是 import —— 后者会造成 M6->M2 代码依赖，破坏可拔插。
   措辞定死：source_present=true 只能说「源已具备」，
   **不得**说「可以导入」—— 那是会落空的承诺。

R3 排序判据：三键稳定排序（改动模块数 -> 源是否具备 -> 域序），
   同分以表名字典序决胜 => 完全确定可复算（SC-003 要求复核者能独立重算），
   因此**不许**保留任何手工调序。

## Phase 1 产出

· data-model.md —— 6 类空因 + 9 条不变量。其中 INV-1（先判 source_present
  再判 implemented）来自仓库里记着的真实踩坑：顺序反了会把
  「源里没有」误报成「适配器没做」，**盖掉真正的原因**。
· contracts/ —— /v1/catalog/gaps 与 /v1/design/capabilities，含反例表
· quickstart.md —— 7 组可粘贴的验收，其中验收 2 是 SC-002 硬断言

## 宪法闸门

五条原则逐条对照，**无需要豁免的条款**。特别确认：零 schema 变更、
零新迁移（原则 IV）；若实现中真需要新表，计划明确写了要退回重新立规格 ——
因为「缺口」应当是算出来的，存下来就会与实库漂移。

## 验证

· 契约测试 12/12 全绿（本次只加文档）
· 占位符残留 0（research.md 里那处是「未决项: 无」这个小节标题）
