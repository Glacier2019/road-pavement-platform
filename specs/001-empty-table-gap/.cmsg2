feat(001): M9 空表页呈现缺口归因 —— T027~T032（US3）

## 页面

空表页从「一串表名」变成「一张待办单」：每行给出空因、归属模块、建议动作，
并按**建议次序**排 —— 能马上做的排前面。

实测次序（前 7 条全是可以立刻动手的）：

  borrow_pit           源已收到·待转换   M2  先跑 tools/tsf2txt.py 转换，再导入
  design_control_text  源已收到·未导入   M2  跑一次设计导入
  earthwork_fill_stat  源已收到·待转换   M2  先跑 tools/tsf2txt.py 转换，再导入
  earthwork_haul_stat  源已收到·待转换   M2  先跑 tools/tsf2txt.py 转换，再导入
  earthwork_transfer   源已收到·待转换   M2  先跑 tools/tsf2txt.py 转换，再导入
  extra_fill           源已收到·未导入   M2  跑一次设计导入
  spoil_pit            源已收到·待转换   M2  先跑 tools/tsf2txt.py 转换，再导入
  ...
  geometry_point       适配器未实现      M2  为该段写适配器

## 三处刻意的设计

① **次序不自作主张**。页面直接展示接口给的次序，**不自己再排一遍**。
   再排一遍就会出现「页面看到的」与「接口声称的」不一致，
   复核者拿 priority_keys 复算时对不上 —— 排序就不可证伪了。

② **归因坏了不拖垮整页**。归因的依赖链比盘点长一跳（多经 M2）。
   取不到时降级为「只列空表、不显示空因」，但**必须把降级说出来**：
   页面上写着「这不是『这些表没有空因』」。
   不说，读者就会把那 26 行理解成「这些表本来就没原因」。

③ **措辞卡死在「源已收到」**，不许写「可以导入」。
   source_present 只说明收到过该文件，**不**保证导入能成。
   写「可以导入」就是给用户一个会落空的承诺 —— 契约测试钉住了这条。

## 测试

test_console.py 新增 3e 组（11 条 + 2 处元测试），钉的是**呈现**：
判定逻辑错了会给出错误建议；呈现错了会**把正确判定藏起来**。
后者更隐蔽 —— 数据全对，但用户看不到。

两处元测试都是**先写错才写对**的（实测踩到）：
  · 判「页面有没有自己重排」，查 priority_keys 字样会**误报** ——
    注释里正解释着「为何不自己排」。改成查代码里有没有 sort 调用。
  · 判「unknown 是否被删」不能查「判不出来」字样（页头说明里也有），
    要对着同一条判据（七值齐全）删，否则元测试永远是绿的。

## 验证

· 契约套件 **13/13**，无回归
· 端到端实测：M9 页面 → /gw → M6 → M3+M2 → 实库
· 排序复核：priority_keys 单调不减，26 张空表次序可独立复算
