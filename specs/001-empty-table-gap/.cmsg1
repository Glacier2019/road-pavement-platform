feat(001): 空表缺口归因打通 —— T001~T018（地基 + US1 归因）

## 一句话

平台现在能回答「这 26 张空表各自为什么空、归谁、先动哪条」。
实测输出：62 张逻辑表 / 36 有数据 / 26 空 / **0 张判不出来**。

## ★ 实现时撞出的一个规格错误（已修订，见 spec-amendment-01.md）

原规格写「源文件就在磁盘上，扫一下就知道在不在」。**这是错的**。

M2 的导入入口是**上传**（POST /v1/design/import），文件落在
tempfile.TemporaryDirectory 里、请求结束即销毁。服务端**没有**任何持久的
设计文件目录，compose 也没挂载 docpipe/materials/。

若照原设计实现，source_present 会**恒为 false**，五个土方段会被判成
「源缺失·待接入」—— 正好是 SC-002 明令禁止的那个错。而且它不报错，
看起来完全合理。

真源是 design_file 表：它记着每次导入**收到过**哪些文件、解析成什么样。
（M2 本就是七域数据的唯一写入方，读自己写入的记录不越线。）

教训：那句话是我**看着仓库里的 docpipe/materials/ 目录**写的 ——
那是开发机上的素材目录，不是运行中服务的输入目录。
两者同名同内容，一个在仓库里、一个在容器里。我把前者当成了后者。
「没去看」≠「看了没有」：我看了目录，但没看**服务是怎么拿到文件的**。

## ★ 由此得到的结论比原规格更锐利

实测：design_file 里有原始 .tsf（parse_status=pending），
但**转换产物 .tsftxt 从未收到过** —— 全库 0 行。

所以五个土方段的正确定类是 **source_needs_conversion**（源已收到·待转换），
不是 source_absent，也不是 source_ready_not_imported。
建议动作因此是「**先跑 tools/tsf2txt.py 转换，再导入**」。

差这一步：若按原规格提示「跑一次导入」，用户会直接导，适配器找不到
.tsftxt，**白跑一趟还不知道为什么**。

## 判定短路顺序（被元测试钉住）

  ① 源已收到 + 已实现 ⇒ source_ready_not_imported
  ② 源已收到 + 未实现 ⇒ module_not_built（解析器是瓶颈）
  ③ 源没有   + 已实现 ⇒ source_absent（文件是瓶颈）
  ④ 源没有   + 未实现 ⇒ module_not_built（先补解析器，找文件也没用）

③ 是最容易被写错的一格 —— 它正是 weidi/__init__.py 里记着的那个坑：
早先写成「在 IMPLEMENTED 里才报 source_absent」，于是 .3DR（本工程没生成）
被报成 not_supported，**盖掉了真正的原因**。

## 顺带修掉一个潜伏缺陷

rpdao.__all__ 里写着 TABLE_OWNER 与 WriteGuardError，但**从没 import 过** ——
from rpdao import TABLE_OWNER 直接 ImportError。
能潜伏这么久是因为 __all__ 只在 import * 时生效，而全仓库没人那么写。
已修，并加了一条回归钉子（+ 元测试）。

## 实现清单

  T004  M2 段↔表映射唯一入口（SEGMENT_TABLE_GROUPS 两个例外 + 反查）
  T005  M2 GET /v1/design/capabilities（源状态取自 design_file，非磁盘）
  T006  M3 design_files_received() / design_parse_status_counts()
  T008  tests/contract/test_gap_attribution.py（8 组，9 处元测试）
  T014  M6 gaps.py 判定逻辑（七值封闭枚举 + 三键稳定排序）
  T015  M6 GET /v1/catalog/gaps（只读拼装，不碰库、不 import M2）
  T016  登记进 OpenAPI 契约（否则 m6-路由 报「实现缺契约」）
  T017  503 分别点名 M2 / M3（两者故障含义不同）
  T018  契约套件 13/13

## 验证

· 契约套件 **13/13**（原 12 套 + 新增缺口归因），无回归
· SC-002 硬断言实测通过：五个土方段零张被判为 module_not_built / source_absent
· 排序可复现：同输入不同到达顺序 ⇒ 次序完全一致
· unknown_count = 0（26 张空表全部有确定归因）
· 硬线未破：gaps.py 经 AST 核对，不 import psycopg / rpdao / M2 任何模块
